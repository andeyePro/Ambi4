# Changelog

## 2026-09-25

- [x] **v0.0.199 — the recipe schema, and the honest table of what the engine still decides in secret** — `src/scripts/recipe.js` (pure: `RECIPE_FIELDS`, 96 fields in canonical order — piece, harmony, every track, arp, patches; `recipeFromParams`, `recipeToText`, `recipeFromText`, a text a person could follow by hand) and `engine.getRecipe()` / `engine.applyRecipe()`. `tests/recipe-roundtrip.mjs` plays every stock genre at three seeds, rebuilds each from its own recipe on a Blank-slate base and replays: all twelve rebuild byte-identical over sixteen bars — which says the eight secret layers are deterministic functions of the recipe's fields PLUS the rng sequence, not that the recipe names them. So the suite carries a second, honest ratchet: the secret-layers table (`tests/fixtures/recipe-secret-layers.json`) asks per layer whether the recipe carries the realised choice, so a rebuild at ANY seed would keep it — today one of nine is named (the voice rule) and eight are secret (hook voicing, motif, bass groove, arp under auto, kit variants and fills, walks, the auto ladder, preset blocks); a layer may move from secret to named and never back. Built in a worktree by a sonnet agent (which found and fixed a real trap: a Blank-slate base that zeroes every voiceRule leaks into a rebuild, because the rule is sparse); the secret-layers table added by the chair at merge.

- [x] **v0.0.198 — a spread dial prints where its walk is, as a number** — every ranged field already showed a live mark inside its span (v0.0.56 on the globals, v0.0.70 on every track and patch dial) but a pointer is not a number, and the Recipe work needs a Now a person can read. knob.js now prints "now 0.42" under the label of any dial in range mode that is being fed its resolved value, and reads it aloud in aria-valuetext ("min 0.2, max 0.6, now 0.42"); it is overlaid out of the flow so no dial cell grows, and it lives inside the label so the readout stays the knob's third child and the click-to-type input its fourth, which is what the knob suites and the page read by index (the first attempt broke eight knobscope checks that way). Gate: knob-gesture (the number appears only in a span, follows setLive, hides on null, and the span reads as drifting again).

- [x] **v0.0.197 — a typed note fills its beat, and the grid says where a step's pitch comes from** — the bass-walk diagnosis found that "one note per beat" wrote one SIXTEENTH per token, so at Blank slate's 60 bpm an unheld note was a 0.25 s blip through a 0.01 s release. A plain token now ties its beat's four slots (the same tie the dash hold builds), so C2 - G2 A2 is two beats of C2 then a beat each of G2 and A2; lyrics (their own per-syllable placement) and the typed beat (one token per sixteenth by design) are untouched. And the grid's tooltip now says what a tapped step plays until you pin it — the chord's root or fifth — and how to give it its own note (focus and P, or Alt-drag), with the "switched on" tip saying the same on a tuned track; pinning itself has existed since v0.0.109. Gate: blank-slate-sounds gains exact slot-occupancy checks (3d/4d), red on the old writer for every unheld note. Built in a worktree by a sonnet agent, merged by the chair.

- [x] **v0.0.196 — the rule primitive: one shape for every rule** — `src/scripts/rule-control.js`, imported beside knob.js and optional like it: one component that shows any rule the engine obeys as three layers — Now (the value playing, a press goes to its control), Chance (the dial the page mounts, with the primitive's When of bar, section or piece), Pool (one line naming what a redraw may pick from, in order, by weight or in turn, with Edit…) — a global level on the Advanced tab ("Rules show": what plays / + chance / + pool, a preference) deciding which layers open by default, "more" on any rule opening the next in place, and a mark on Now when something is set beneath. The voice rule of v0.0.195 now mounts on it, above the Randomise row, its knob in the Chance slot and its ids kept; nothing about what it does changed. From here every later rule — chords, motif, groove, arp, kit, walks, ladder, blocks — mounts on this module and nothing else, so the one logic he ruled holds by construction. Gated by `tests/rule-control-smoke.mjs` (the module in jsdom) and page-boot's new block (three layers, the level select, "more", the dial in its slot); voice-rule-page and blank-slate-sounds unchanged and green.

- [x] **v0.0.195 — the voice rule: the first Now / Chance / Pool** — his report: Synthwave's melody kept moving to Organ stab and nothing could stop it. The cause was the anti-monotony wander — p = 0.25 × Randomness per bar over every other voice in the bank, ephemeral, with the only hold a Randomise mini-knob nothing named, and the Blend voices editor a separate mechanism again. His ruling of the same day: every decision the engine makes for you is ONE control — Now (the value playing), Chance (0 holds it, plus when a redraw may happen), Pool (an ordered, weighted list, by weight or in turn) — revealed by level and in place, with Chance 0 meaning yours across Next. The voice is the first rule built that way: `tracks[t].voiceRule` at the engine; in the editor the Randomise row's Voice knob is Chance (0 reads Hold), with a When selector and a Pool… button beside it; the Pool editor is the old blend editor with remove, reorder, add and By weight / In turn; the picker says "· drawn" and where the dial is; an explicit pick holds; Next keeps a held rule and the pick under it; Blank slate holds every track with an empty pool; Save as my genre and share links carry the rule. A track with no rule keeps the old wander and blend to the byte (the engine suites hold every stream); a legacy blend becomes a pool with Chance 1 per section, drawn by weight from the whole pool as the blend was, the first time it is touched. Reviewed before merge (opus): a knob drag no longer re-rolls other tracks' section draws, the Pool editor edits a copy until Done, a saved genre's rule survives the rules form, and a pick of your own voice holds like any other. Gated at the engine by `tests/voice-rule-smoke.mjs` — Keys held for 64 bars at Randomness 1 with Organ stab out of the pool, across three fresh compiles — six of eight checks red on the old engine; and at the page by `tests/voice-rule-page.mjs` (the built page in jsdom: knob, Next, Pool editor, When, the picker, Blank slate). Browser drives for both units are parked in `tests/pending/` until the bridge key returns. The shared rule primitive every later rule must use is the next TODO item; this one is built inside the page.

- [x] **v0.0.194 — the Blank slate first minute** — his report: "I tried blank slate and then entering a melody and got nothing." A harness that drives the built page click by click found the reasons, and none of them was the one first guessed. A step tapped on the grid of a silent track never switched the track on, so Blank slate then the grid was silence forever with the cells lit; now the tap switches the track on in the same payload as the step and the tip says so. A typed line waited its staged turn — one silent bar for bass, two for melody, at 60 bpm eight seconds of nothing with no sign of it — so a sequencer a person writes now carries `hand` and enters at once, while every generated track and every stored piece keeps ruling 7's staged intro exactly as before, and the transport line reads "Bass enters in 1 bar · Melody enters in 2 bars" while a track is still to come. Write it starts the piece if nothing is playing. The typed tool's picker has a visible label and Write it's tooltip names the instrument it points at, not "the melody grid". And the blank bass opens on a triangle instead of a sine — still one oscillator with the filter open, as his 117 ruled — because a bare 65 Hz sine is below what a laptop speaker can make (fromClaude 21 asks him to bless the shape). Gated by `tests/blank-slate-sounds.mjs`, red on the old code for every one of these.

- [x] **Three owner calls actually asked** — the Resonance curve, the Octave dial's range and the spread's screen-reader contract sat in TODO as decisions but were never put to him; they are fromClaude 15, 16 and 17 now, each a one-letter choice, and the TODO rows name them. The graph's error-states row is corrected: two of its three cases have been handled since it was written. No code changed.

- [x] **Phase 2c closed** — the TODO row that has driven the registry-renderer work since v0.0.166 is closed: every dial's domain, curve and hint come from the registry in both editors, the uniform dials are loops, and both layouts are pinned against the pre-loop pages. The Source section stays literal by ruling rather than omission — each of its dials carries its own gate, format, parser and marks, so a loop would add indirection without removing a fact — and the row now says so. No code changed.

- [x] **v0.0.193 — the dial witness sees spread and format too** — the optimiser's audit after v0.0.192 found the layout fixtures still blind to three things a registry loop derives: whether a dial may spread, and the format its readout is printed in (the third, whether it opens as a span, turns out to be state a genre sets, so it is not pinned). knob.js now states `data-spread` on every dial (a test seam in the spirit of the page's `data-field`), and both fixtures pin each dial's spreadability and the UNIT its readout is printed in — the value itself is state, since the genre a fresh boot draws sets the patches. Both fixtures were recaptured from the pages BEFORE the loops (v0.0.186, v0.0.188) and the current page passes them twice over, which proves v0.0.187 and v0.0.189 — with v0.0.192's Resonance correction — match the old literals in domain, spreadability and format on all 36 voices in both editors. A mutation that makes the knob loop refuse every spread turns all 36 red.

- [x] **v0.0.192 — correction: Resonance was not byte-identical, and now is** — v0.0.187 and v0.0.189 were described as byte-identical and they were not: their loops read each dial's curve from its registry row, and `patch.filter.q`'s row says log while Resonance has always been drawn linear, so both editors quietly turned Resonance into a log dial on every voice. The witness missed it because the fixtures pinned fields and labels, not domains. Found by the optimiser's audit of the gate. Fixed at the source of the miss first: both fixtures now record each dial's own domain as the DOM states it (a log dial reads 0–1), and were recaptured from the pages BEFORE the loops (v0.0.186 for the knob editor, v0.0.188 for the fallback), so the gate went red on exactly one dial on all 36 voices in both editors and nothing else. Then the shipped behaviour was restored as an explicit per-dial exception (`curve: 'linear'`) in both loops, and the gate is green against the pre-loop pin. Whether Resonance should be log is a ruling, filed beside the row that first named the discrepancy. The two entries below that say "byte-identical" and "no dial moved" were wrong for this dial until this version.

- [x] **TODO and the dial plan catch up with the keyboard path** — the open row "Specify the keyboard path — it is inadequate today" described v0.0.55: two of its three asks shipped in v0.0.56 (Shift+Left/Right opens and sizes a spread from any dial, Backspace/Delete resets), held by `tests/knob-gesture.mjs`. The row is closed with that named, the plan's matching risk bullet says the same, and the remaining third — a dual-thumb ARIA contract, which needs two focusable elements per spread dial and so changes every panel's tab order — is re-filed as a decision rather than built blind. No code changed.

- [x] **v0.0.191 — the engine's genre table is registered at last** — the ninth finding of the arc's review, older than today: the page asked the engine INSTANCE for `setGenreTable`, which is a module export, so the call never ran and the engine kept any slug-shaped genre tag, including one a link invented. `registerGenreTable()` now registers the stock genres and the person's own through the export, and re-runs whenever that list changes — a link's genre adopted, a genre saved, one forgotten — so a user genre's `u-…` slug still tags the engine while a slug no genre has is refused like any other unknown value. `page-boot` sets a made-up slug and reads it refused (red on v0.0.190, where the engine kept it); its save, Next, forget and carried-link checks hold the other half.

- [x] **v0.0.190 — the arc's review, and its eight page faults fixed** — an adversarial review of everything shipped today (v0.0.172–189) found the engine, the compiler and the pure modules clean and eight faults in the page, all fixed here: **(1)** a genre or voice a share link brought was pushed into the same list a person's own are persisted from, so the next save or forget of anything wrote the arrival to the device for good; arrivals are now this visit's until saved, and saving over one replaces it rather than listing it twice. **(2)** an arriving (or stored) voice's base was never checked to be a voice of its set — a crafted link naming `'nope'` put a voice the engine refuses into the picker, and `'__proto__'` reached `settings.patches` through its setter; bases must now be own keys of the set's voice table, and genre and voice ids must have the shape the page mints (the engine's own slug bound), names capped at forty characters. **(3)** a ruled line-up row re-landed on EVERY Apply, because it was compared against the genre file, so a pad ruled Off and later set by hand went back at the next chord edit; a row now lands when it changes against the rules last applied, and only the fields it sets. **(4)** Apply dropped a track's own-voice tag, so the picker read "Warm · edited" instead of the voice's name. **(5)** Save as my genre read the rules form, not the applied rules, so an unapplied edit saved a genre that was not the piece playing; it now asks you to Apply first. **(6)** the fallback number boxes each built on the render-time pair, so a second edit undid the first. **(7)** covered by (2). **(8)** the share size race could trade a compact link carrying a genre for a whole-tree link with nowhere to put it; a link carrying a genre now always stays compact. `page-boot` gains a check for each of (1)–(5), each red on v0.0.189. One more finding, outside today's diff, is filed in TODO: the engine's genre table is never actually registered, which is what lets a user genre's slug tag the engine.

- [x] **v0.0.189 — the fallback editor's nine literals become the same loop** — phase 2c's other half. The slider editor the page falls back to without knob.js carried its own nine hand-written Filter, Envelope and Sends controls; they are one table and one loop over the registry now, exactly as the knob editor's became in v0.0.187 — domain, curve and unit from the row, and per control only the label, the fallback domain, the initial value, the slider step and cutoff's whole-hertz write. Proven byte-identical by the witness v0.0.188 built for exactly this: `page-boot` boots all 36 stock voices through the fallback and asserts the pinned control layout and every control's hint. `registry-contract`'s count of literal hint call sites, which stood in for "every control is described", now checks the loop itself. What remains of 2c is the Source section in both editors, where no two dials are the same shape.

- [x] **v0.0.188 — the slider fallback editor can be booted on purpose, and is** — the editor the page falls back to when knob.js is missing or throws had no gate: nothing could reach it deliberately, so it could rot silently, and its own share of phase 2c had no witness. `window.__ambi4SliderEditor` is a deliberate test seam on the same terms as `window.__ambi4Engine`; every fallback control now names its field in the DOM as the knob cells do; and `page-boot` sets the seam, renders all 36 stock voices through the fallback, pins each one's control layout — field, label, kind, in order — in `tests/fixtures/editor-controls-fallback.json` (written when absent, asserted when present), and holds that every control's input or select carries its registry hint as its accessible description, over two hundred of them. The collapse of those literals into a loop can now be proven byte-identical the way the knob editor's was in v0.0.187. No dial moved; the knob editor is untouched.

- [x] **The manual release pass catches up** — `docs/TESTING.md` gains the v0.0.172–187 delta in its own style: one check per surface a person can do and see (hints and chips, the words line, the three engine sections and their unchanged defaults, the Rules panel's whole essence and line-up, a genre of your own and its link, a voice of your own and its link, the lessons, the finder), the Node gate they run under, and the browser drives owed to the bridge key. No code changed.

- [x] **TODO hygiene** — three rows that described shipped work still read open and are closed with the version named on each: the July audit's row (its two residuals landed as v0.0.159 and v0.0.161), the compose brief's guided start (v0.0.135) and its Simple/Advanced parity (the Create door he reviewed). No code changed.

- [x] **v0.0.187 — nine dial literals become one loop over the registry (phase 2c, part)** — the knob editor's Filter, Envelope and Sends dials — Cutoff, Resonance, Env amount, Attack, Decay, Sustain, Release, Reverb, Delay — were nine hand-written `addKnob` literals, each repeating what its registry row already said. They are one table and one loop now: the domain, the curve (log dials take the log path) and the unit (which picks the format) come from the row, and whether a dial may spread is the row's `rangeable`, derived rather than hand-passed. What stays per dial is what is genuinely a page fact: its label, its pre-registry fallback domain, whether it opens as a span by default (cutoff and the sends, since v7) and cutoff's whole-hertz write; the Type dial and the kit's envelope note keep their own code inside the loop's own section. Byte-identical, and witnessed: `tests/fixtures/editor-dials.json` pins every stock voice's dial layout — every section heading, every dial's field and label in order — captured from the page before the change, and `page-boot` asserts it after, beside its existing per-dial field, hint and domain checks. No dial moved.

- [x] **v0.0.186 — the share wire's version stamp has its test** — the backlog row v0.0.171 left open, now more pressing because today's units put essence re-rulings, genres of the person's own and named voices on that wire. `page-boot` mints both kinds of link through the real Share button and its stubbed clipboard and reads the stamp back: a plain Share, with everything the earlier checks made forgotten and the rules reset, is stamped schema 1 and carries no genre — the link every older build reads in full; a Share with an essence re-ruling in place is stamped 2 and its diff carries the re-ruling; and (since v0.0.181) a Share of a genre of the person's own is stamped 2 and carries the genre. A stamp that always wrote 1 or always 2 fails one of the three, which is the bite. No page code changed.

- [x] **v0.0.185 — a sound in mind: pick the words, and the table says where to start** — unit 16 of the synthesis programme, the last layer of its learning design. Under every voice editor's dials: *A sound in mind?* — twelve words in six pairs (bright or dark, plucky or sustained, clean or gritty, warm or glassy, thick or thin, still or moving). Press one or two and the row answers for THIS track: "Try Bell (FM): ↑ drive the wobble harder, ↑ keep it bright longer, ↑ an inharmonic ratio. Brightness is top end: a filter open, an FM depth up, hard mallets." Set it up puts that voice on the track and moves the dials it names a third of the way, so the words become a sound you can hear and ⌘Z takes it back. It is a table, not an AI (`src/scripts/sound-finder.js`): one entry per word, a starting voice and two moves for every tuned voice set, chosen so a person who wants "glassy" meets stretched partials or an inharmonic FM ratio rather than a filter; a kit is spoken for by no word and says so plainly rather than guessing. `tests/sound-finder-smoke.mjs` holds the table's referential integrity — every voice named exists, every dial named is a registry row that voice actually shows (it refused one row on the way in: Grains has no filter envelope dial), every word has its opposite, every tuned set is spoken for — and the lookup's laws (the voice most words agree on, ties to the first word, one move per dial, a third of the way and never past the end, both ends of a span). `page-boot` presses bright and glassy on the melody, reads Bell (FM), presses Set it up and reads Bell with a raised FM depth from the ENGINE; presses dark and sees bright released; presses bright on the kit and reads the honest refusal. The CLAIMS themselves — that bright is brighter by spectral centroid, plucky shorter to peak, sustained louder at one second, moving more modulated — are the parked render drive's, with one row broken on purpose to prove it bites (fromClaude 13).

- [x] **v0.0.184 — the engine lessons: press the chip, learn the engine on its own dials** — unit 15 of the synthesis programme, layer 3 of its learning design. The chip at the top of every voice editor is a button now: press it and the tour panel opens a chapter on that engine — Subtractive, FM, Additive, Modal or Noise — three or four moves long, each move on a real dial of the editor you are in, highlighted, saying what to drag and what to listen for ("Ratio is how fast the wobble runs, as a multiple of the note: drag it onto a whole number and the tone turns harmonic, organ-like; between whole numbers it rings like a bell"). Next and Back step through it, closing the panel ends it, and the tour's own fourteen steps are untouched (its test caps them, which is why lessons are chapters of their own). A hybrid voice opens the chapter of the section it publishes; a voice lacking a chapter's dials says so under the dials rather than highlighting nothing. `tests/tutorial-smoke.mjs` reads the chapters out of the page as it reads the tour and holds them to the tour's copy rules — five chapters, one per engine, each on a voice of that engine, every move naming what to drag, step or press — with a mutation check that refuses a chapter on the wrong voice; `page-boot` opens each chapter's home editor on its home voice, resolves every target to exactly one dial, presses the chip, sees the first dial highlighted and the progress read 1 of N, presses Next and sees the second, closes and sees the tour return. The gesture drive is parked (fromClaude 13).

- [x] **v0.0.183 — a voice of your own travels in a link** — unit 9 of the synthesis programme, and the smallest of them, because the sound already travelled: a track's voice and its patch have always been in a link. What travels now is the NAME: the track's tag naming its voice of the person's own rides the diff, and on arrival a tag naming a voice the device has never seen becomes a voice of that visit's own, built from the arriving voice and patch, listed under My voices with the pad reading as it — not persisted by arriving; Save as my voice keeps it — while a tag naming one the device has keeps the device's copy. No schema stamp: the tag is a page field a pre-183 build carries untouched and ignores, and what it hears is identical. No code travels; there is nothing to refuse. `page-boot` decodes the link on the clipboard after saving Dusk pad and finds the tag naming it, then boots a page on a link whose pad is tagged Arrived pad on Strings at 654 Hz and reads the voice and patch from the ENGINE with Arrived pad under My voices and selected.

- [x] **v0.0.182 — My voices: a sound you made, kept under a name** — unit 8 of the synthesis programme. Every voice editor ends with a save row: name the sound and Save as my voice, and it appears in that track's picker under **My voices** — and in the picker of every track that plays the same voice set — selected, with Forget this voice beside it while it plays. A voice of your own is the stock voice you started from plus the dials as you left them: a base and a patch, nothing the engine has not always played, so the engine plays it exactly as it plays that stock voice with that patch, and every engine section it grew (FM, Additive, Modal) rides along as part of the patch. The engine template is simply the stock voice of that engine you begin on: Warm for subtractive, Bell for FM, Glass for additive, Chimes for modal, Coloured noise for noise. It lives on this device under the consent-gated prefs (fifty at most); the picker reads it by name while its dials stand where it saved them and as "name · edited" once one has moved; picking a stock voice ends it. This is deliberately the picker shape and not the contract's user-track instrument manifest — a manifest is a user track's own document, and a voice made on the pad belongs in the pad's picker, where a person looks for it (the programme doc says so). `page-boot` pins the pad on Warm, types 900 into Cutoff through the real readout, saves it as Dusk pad, finds it selected, picks Glass and picks it back and reads Warm at 900 Hz from the ENGINE, then Forgets; a second boot with a voice already in storage and consent recorded lists it and plays its base and patch when picked. The tour's tracks step names the save row. The live reload drive is parked (fromClaude 13).

- [x] **v0.0.181 — a genre of your own travels in a link** — unit 13 of the synthesis programme, closing the genre half: a link minted on a genre you saved rebuilds on a device that has never seen it. The compact share payload carries the genre JSON itself under one more key when the origin names a user genre — data, never code, exactly the v10 rule — and on arrival the base rebuilds from it as from a stock file, the setup is tagged with it, and it sits under My genres for that visit so Next draws from it and Save as my genre keeps it (arriving does not persist it; that is the person's Save, as for any setup a link brings). A carried genre is accepted only if it is a user genre whose slug the origin names, of the compiler's shape, and not a slug the stock set holds, so a link cannot shadow a stock genre with a body of its own. Such a link takes the latest schema stamp; a pre-181 build reads past the genre and plays the diff on the defaults, as it does for any genre it lacks; and the size race still picks the whole setup when that is shorter. `page-boot` proves both ends: after saving Night pads it presses Share, decodes the link on the clipboard and finds the origin naming the genre, the genre under `g` with its ruled pad Off, and the stamp at 2; then boots a second page on a link carrying a genre with tempo 200 and the pad Off, and reads 200 and Off from the ENGINE with Carried listed under My genres. Unit 14 (defiance dial authoring) is recorded as blocked on fromClaude 12: a defiance dial's `param` is a dotted address into the params tree that becomes permanent in a saved or shared genre — the grammar that ask is about — and the stock genres' own defiance dials are not surfaced on the page yet.

- [x] **v0.0.180 — Save as my genre** — unit 12 of the synthesis programme, and the point of the genre half: the rules a person re-ruled in the panel, plus the voices and dials they built, become a genre of their own that draws fresh pieces, exactly as Synthwave does. The Rules panel gained a save row: name it, Save as my genre, and it appears in the picker under **My genres**, favourites-aware, with Next drawing new setups from it and Forget this genre on its own entries. Under the hood a user genre is a genre JSON of precisely the stock files' shape — the compiler cannot tell them apart — with a slug no stock file may use, the genre it was made from, its mood cluster, and the current patches captured into its instrumentation; it lives on this device under the consent-gated prefs (fifty at most). Saving bakes the panel's overrides into the genre and clears them from the setup, so the Rules button reads plain again; the piece playing IS a draw of the new genre, so nothing changes while it sounds. Apply and Save share one diff (`diffRuleOverrides`), so a saved genre carries exactly what Apply would have laid on. `page-boot` proves it end to end: rules the pad Off and the tempo 200, saves, finds the entry selected under My genres and the engine tagged with it, presses Next and reads a fresh piece with the pad still Off at 200 bpm — the rule lives in the genre, not the setup — then Forgets and reads Synthwave back. A share link naming a user genre is not rebuildable elsewhere yet; the share base falls back to the whole setup, as it does for a hidden genre, so the link still plays — unit 13 carries the genre as data. The reload drive is parked (fromClaude 13).

- [x] **v0.0.179 — the rest of the essence: shape, colour, dissonance, density, hall, and the line-up** — unit 11 of the synthesis programme. The Rules panel now holds everything a genre file's `essence` says: **Shape** (the structures a piece may take — drone, waves, build, A B A B, journey — weighted), **Colour** (the chord extension bias that feeds Complexity), **Dissonance** (a span the tuned tracks drift inside), **Density** (the multiplier on every rhythmic track), **Hall** (the shared reverb's tail), and the **Line-up**: one row per built-in track with whether it plays (Off, Auto, On, or as the app decides), which voice from that track's own bank, its level and its variation, the last two as spread dials. A ruled line-up row LANDS on Apply, and only for the tracks the person ruled: since v0.0.149 Apply has kept a recompile's voice, state and level away from the setup you built, because chord rules are not the mixer — but a row in Rules IS the person saying so, and the guard now steps aside for exactly those tracks and no other. `applyGenreOverrides` cleans every field against the engine's own lists and bounds (structures without `custom`, the three track states, levels and variations clamped to 0–1 as numbers or spans, a track the engine lacks dropped, a ruled row replacing the file's row whole so nothing leaks under it); `tests/genre-smoke.mjs` holds it (a drone-only arc compiles to drone, colour 1 raises complexity, dissonance 0.5–0.6 reaches the pad as a span, density 0.5 halves melody's density, hall 7 lands, the ruled pad is off and on Glass at 0.25, bad values clamp or drop). `page-boot` counts Synthwave's three structures and six line-up rows, finds Poly saw on the pad row, rules the pad Off, Applies, reads Off from the ENGINE, and resets to On. The stamp rule covers the new keys. Capturing the current patches into a genre is unit 12's, where a genre is made from a setup.

- [x] **v0.0.178 — the Rules panel learns the genre's essence: tempo, swing, metres, modes, chord changes** — unit 10 of the synthesis programme, the first of the genre half of the brief. When a genre was written in code, its `essence` said what tempo range to draw from, how much swing, which time signatures and modes at what weights, and how many bars each chord holds; the Rules panel showed none of it. It now leads with all of it: **Tempo** and **Swing** as spread dials — the same knob every other dial is, so the sideways drag that opens a span everywhere opens the draw range here, and the typed readout takes "84-104" — and **Time signatures**, **Modes** and **Chord changes** as weighted rows (a value from the engine's own list, a weight, a ×, and + to add), where a weight of 0 means never and an emptied section stops ruling and the compiler falls to the engine's default, exactly as the chord rules have worked since v0.0.161. Apply stores only what differs from the genre's own file, re-draws with the same dice, and the Rules button says "edited"; Back to the genre's rules clears it. The merge of a person's overrides onto a genre moved out of the page into the pure compiler as `applyGenreOverrides` — a deep copy, laid on by Array.isArray so an empty list is a ruling, every weighted value checked against the engine's vocabulary (`ESSENCE_CHOICES`), a malformed range ignored rather than refused — so `tests/genre-smoke.mjs` holds it in Node: forty seeds land inside an edited tempo span, a zero-weight mode is never drawn, an emptied list falls to the default, a stranger is dropped, and the chord-rule overrides keep their v0.0.161 semantics after the move (red on the old code, where the function did not exist). `page-boot` pins Synthwave, opens the panel, counts its one metre, two modes and three rates as the file declares, types 200-200 into the Tempo readout, Applies, and reads 200 from the ENGINE — then resets and reads the genre's own draw back. A link carrying an essence re-ruling takes the schema-2 stamp, because a pre-178 build would read past those keys. The tour's genre step now names tempo, swing, metres and modes among the rules. The gesture drive (drag the Tempo dial sideways, Apply, engine inside the span) is parked as `tests/pending/genre-essence-drive.mjs` (fromClaude 13).

- [x] **v0.0.177 — a struck object you can choose: the Modal section** — unit 6 of the synthesis programme. Chimes and Marimba have always been modal models — a handful of sines at a real object's overtone ratios, each dying at its own rate, with a mallet click on the front — with the tables as literals. They now publish a `modal` section: **Material** (metal, wood, glass, bell — overtone tables, where metal IS chimes' own table and wood IS marimba's, verbatim, so each voice's default is the literal it always rang with, and glass and bell are the two struck objects those did not cover), **Hardness** (0–2, default 1: lifts the upper partials by `1 + (h − 1) × 0.5 × i` with the fundamental fixed, and scales the mallet click, so a hard strike is bright and sharp and a soft one dull and quiet) and **Damping** (0.25–4, log, default 1: divides every partial's ring). Three registry rows with hints; both editors draw the row on those two voices and no other; Material is an enumeration dial like Type, built from the registry's own value list. `voices-smoke` proves the defaults rebuild the shipped stack frequency for frequency, level for level and ring for ring, then that wood puts Chimes on the bar table (1, 4, 9.2), bell rings five partials, hardness 2 lifts the second partial and doubles the click while the fundamental stays, hardness 0 loses the top partial, and damping 2 shortens every ring. The words line names the material ("a struck-object model (modal, wood)"). The membrane kits deliberately do not publish the section — a drum's skin is a pitched bend over noise with a per-sound envelope the kit editor already owns, and a material table has nothing to select there; the programme doc's unit table now says so. Render proof parked as `tests/pending/modal-render-drive.mjs` (fromClaude 13).

- [x] **v0.0.176 — drawbars: the Additive section** — unit 5 of the synthesis programme. Glass (five stretched sine partials) and Organ stab (six tonewheel drawbars) have always been additive with their partial levels as literals. They now publish an `additive` section — one level per partial they actually have, plus Stretch — as registry rows with hints, and both editors draw exactly those bars (Glass P1–P5, Stab P1–P6, never a bar that moves nothing) on those two voices and no other. Each level is a MULTIPLIER (0–2, default 1) over the voice's own table, and Stretch (−10%..+10%, default 0) offsets the i-th partial's ratio by `1 + stretch × i` with the fundamental fixed — because Glass's partials are already stretched (3.01, 4.98, 6.97) and a plain drawbar value would have changed the shipped sound. A bar pulled to zero sits at SILENCE, never 0: an exponential ramp to zero is an error and a level that is not built cannot be turned back up. `voices-smoke` proves the defaults rebuild the shipped partial stack frequency for frequency and level for level, then that p2 0.5 halves the octave and nothing else, p3 0 silences the twelfth, stretch +5% moves partial 2 by one step and partial 3 by two, and Glass's top partial squeezes by four. The words line counts the bars ("6 partials summed at their own levels") and names only the ones moved. `page-boot` holds the disclosure rule on Glass and Warm. Render proof parked as `tests/pending/additive-render-drive.mjs` (fromClaude 13).

- [x] **v0.0.175 — FM becomes something you can turn: the first engine section** — unit 4 of the synthesis programme. Five voices (Bell, Keys, Tines, Sparkle, Crystal) have always been two-operator FM with their ratios and index decays as literals inside their recipes. They now publish an `fm` section — Ratio, Depth, Bite — as three registry rows with hints, and both editors grow an FM row on exactly those five voices and no other: the disclosure rule (docs/synthesis-programme.md § 3), the same one the Spectral, Motion and Burst rows follow. Ratio (0.25–16, log; whole numbers organ-like, in-between bell-like) and Bite (the index decay) default to each voice's own literal; Depth is a MULTIPLIER over the voice's velocity-scaled index law with a default of exactly 1, because `f * (1.5 + 2.5 * v)` is what Bell ships and no flat field could reproduce it — so at the defaults the graph is the one that shipped, and `tests/voices-smoke.mjs` proves it node for node (the modulator's frequency and every automation event on its depth gain identical between "no patch" and "the voice's defaults"), then proves each dial moves what it says: ratio 2 puts the modulator at 880 Hz on A4, depth 2 doubles the index, depth 0 silences it, bite 0.1 ends the decay 0.1 s after onset. The words line names the ratio ("at 3.47× the note, bright for 0.8 s"). `page-boot` shows the FM row on Bell with every dial carrying its hint, and no FM row on Pluck. The frozen note-event reference is unchanged without `--update`. The render proof — depth raises Bell's spectral centroid, ratio moves the first sideband to f·ratio, bite darkens the tail — is written as `tests/pending/fm-render-drive.mjs` and parked until the bridge key returns (fromClaude 13).

- [x] **v0.0.174 — the four engine faults the v0.0.171 review parked are fixed** — unit 2 of the synthesis programme, and the one that was user-visible: **(a)** `quantiseCapture` built its blank lane as a fixed twenty slots while the grid it recorded into was sized by resolution, so a take on a track finer than a sixteenth wrote holes the lane sanitiser read as "absent" and inherited — half the previous pattern kept sounding under the new take. The lane is now sized by `laneSlotsFor(stepBeats)`, the same law `sanitiseStepLane` sizes by, and `captureGrid` threads `stepBeats` through both of its branches; a caller that passes none keeps the old twenty byte for byte. **(b)** `forgetTrack` cleared `wanderedVoice` but not `sectionVoice`, so a re-added track id sounded the dead track's drawn voice until the next section. **(c)** a partial `lfo1` patch reset `bars` to 4 instead of inheriting the stored value; it merges the way `sanitiseArp` merges its nested numbers. **(d)** `'04/4'` and `'4/4 + 3/4'` were stored as typed, so a named metre showed as custom and the first switch to its canonical spelling restarted every generator; `normaliseMetre` (exported, built on `metrePulses` rather than a second parser) stores the canonical form and an unusable value keeps the stored spelling rather than falling to the default. Each of the four tests was red on the old engine with the exact message recorded (`20 !== 40`, `'glass' !== 'warm'`, `4 !== 16`, `normaliseMetre is not a function`) and is green now — 260/260, nothing weakened, the frozen note-event reference unchanged. One fixture correction on the way in: the parked lane-mask assertion used an `x`/`-` spelling no `laneMask` in the file has ever produced; it now uses the `1`/`0` mask every other call site compares. The end-to-end recording drive for (a) stays owed to the browser gate (fromClaude 13).

- [x] **v0.0.173 — what makes this sound: the patch, in words, under every editor's dials** — unit 3 of the synthesis programme (docs/synthesis-programme.md § 4, layer 2). A line under the dials of every voice editor now says what the sound is made of, written from the SAME patch object the dials edit and rewritten on every store, so the words and the readouts cannot disagree: "Two saws 6 cents apart. Low-pass at 3520 Hz, ringing (resonance 4), opening fully with each note. Strikes in 6 ms, falls away over 1.1 s, fades over 50 ms. In a room (reverb 40%), echo 35%." The engine class opens the sentence in its own words (an FM voice is "a sine carrier wobbled by a sine modulator", drawbars are "partials summed at their own levels", chimes "a struck-object model", a kit "a bending skin over noise"), the controls rule hides what the voice hides, a spread prints as its two ends, and every number uses the editor's own unit and rounding. `src/scripts/patch-words.js` is pure — no DOM, no audio — which is what lets `tests/patch-words-smoke.mjs` hold the one law in Node: **every number the sentence prints is a value of the patch it describes**, checked on all 36 stock voices, on a spread and after a change, with a mutation check that proves the checker refuses a wrong number. `page-boot` opens the six editors and holds each line equal to the module's words for the sounding voice, so the page's wiring (voice, controls, detune mode, live patch) is proven against the module. The browser gate stays owed (fromClaude 13).

- [x] **v0.0.172 — the synthesis programme is decided, and every dial says what you will hear** — the first commit of the programme the owner's brief opened: everything the author could do in code when writing the stock genres and their voices, a human can do in the UI; which methods of synthesis the app should have; and a way to learn synthesis that reaches the sound in your head. `docs/synthesis-programme.md` is the ruling — method by method, with the reason each is kept, added, declined or deferred (Subtractive stays the base; FM, Additive and Modal become engine sections a voice shows only when it carries them; sample-based, wavetable, vector and the research methods are declined; plucked string, PWM and hard sync wait for an AudioWorklet, because a Web Audio feedback loop cannot go below 128 samples and the platform has no oscillator sync) — plus the disclosure rule that keeps breadth out of a beginner's way, the four-layer learning design, and a sixteen-unit build sequence, each unit one commit with its proof named. The slice that ships with it is the foundation the learning layer stands on: **every dial now says what you will hear.** The registry row gained a `hint` column, so a dial's domain and its words live in one place; both editors read it for every dial (the knob editor once per dial at construction, the slider fallback once per control), the sixteen sculpt and call hints the page carried moved into their rows word for word, and the core Source, Filter, Envelope and Sends dials — Cutoff, Resonance, Attack, Release, Reverb and the rest, which had been bare labels — have words for the first time, reaching the accessible description as well as the tooltip. **The editor names its engine**: a chip beside the track name (Subtractive, FM, Additive, Modal, Noise, Hybrid) and one sentence under the header saying what that engine does, so the first time someone meets an FM section they have already been told what FM is; the picker's "Custom (…)" label uses the same word. Organ stab is reclassified from `physical` to `additive`, the sixth class, because drawbars are the one engine a listener reads at a glance. Gates: `registry-contract` refuses a row without a hint and a page that keeps a hint literal of its own; `page-boot` opens all six editors and checks every dial's description against its row and every chip against the voice table — both red on the old source, green on this. Node 22 toolchain restored in the container from actions/node-versions (22.23.3). 22/22 node suites green. **The browser gate is owed**: the Mac test account refused the repo's bridge key (`Permission denied (publickey)` for ambitick, connection established), so neither the drive sweep nor the layout sweeps could run; the ask to restore the key is fromClaude 13, and the sweep runs the moment it lands — before anything of this reaches main.

## 2026-08-22

- [x] **v0.0.171 — the page's half of the registry stops lying, and the wire says when it has grown** — the second commit off the routing-arc review, all of it on the side phases 2a and 2b claimed and had not finished. Four write clamps still held their own literal while the bounds beside them came from the registry (both sends in the knob editor; noise and fold in the fallback editor, which had pitch RIGHT thirty lines above, so the two editors disagreed about the same field) — a dial that reports what the patch never stored is the failure mode this whole phase exists to end, and it was one row-move away from being live. The two morph dials (OSC 1, OSC 2) now read their rows, which is the pair 2a's own summary said it had converted. The oscillator and filter MENUS are built from the registry rows too, so a type the engine would refuse cannot be offered and one it accepts cannot be missing; only the display names stay on the page, because a label is a page fact and an id is a wire fact. A field with no registry row no longer renders at all: 2a deleted the spec tables' own min/max, which quietly turned a missing row into `clamp(x, undefined, undefined)` — NaN, into the patch — so the dial is dropped with a console line instead, and `tests/registry-contract.mjs` scans both spec tables against the registry so one cannot reach a build. The octave comment claimed a `patchNumber(v, -1, 1)` clamp that has never existed on this code: the row, the sanitiser and every voice's own table all take ±2 and only the DIAL is narrow, which is now written down as the unexplained cap it is, with a decision in TODO. On the wire, v0.0.167 and v0.0.168 added four keys without touching the schema stamp, so a link carrying a cable met an older build as a link it silently dropped keys from; the stamp is now per-payload — schema 2 only when a value an older build would drop is actually set, so every link without one is still a schema-1 link that every older build reads in full. Two new test seams, on the same terms as the engine seam that has been there since v0.0.69: the registry reaches the page as `window.__ambi4Registry`, and every dial names its parameter in the DOM — a dial's bounds cannot be checked against the row they come from unless something can pair the two up. `tests/routing-contract.mjs` (new, 11 cases) holds the routing and sampling wire the commit messages described and nothing asserted: one slot per destination proven at the sanitiser AND in playback, the 64-edge cap, every refusal, `note` sampling refused rather than played as bar, hold and freeze pinning a routed dial, and an edge's removal genuinely detaching. And `nearestResolution`'s fallback for a rung on neither ladder maps by POSITION again — it had been looking the sixteenth up in the triplet ladder, finding nothing, and clamping to crotchet triplets, four note values away. 22/22 suites green.

- [x] **v0.0.170 — a cable to nowhere stops the music: the routing arc's first fault, found by review** — a four-agent review of the whole routing arc (v0.0.165–169) turned up one fault that stops playback dead, and it shipped in v0.0.168 with the first cable. A routing edge's destination is a walk key, and the sanitiser checks its GRAMMAR — which deliberately admits the `@global` pseudo-track every spread global dial already walks on, and cannot know which track names a given piece holds, because a stored link outlives the setup it was made in. `advanceRouting` then asked that name for its randomness to see whether it was frozen, and a name that is not a track answered with nothing: a TypeError inside the bar draw, uncaught in the timer tick, the clock stopped on the first bar. The walk side never had this — `advanceWalks` reaches the same answer through a Set built from the live track order — so the fix states the same law track-wise: nothing that is not a track can be frozen, so an edge addressing no live track is INERT rather than fatal, and the edges beside it still deliver. Three cases in engine-smoke, all of which kill the process on the old engine: an `@global` destination (the class the plan's own energy-drives-openness axis lands on), an unknown track's destination, and a dead edge sharing its list with a good one, where the good one must still deliver its macro's exact value.
- [x] **the registry's drift guarantee stops being a tautology** — `tests/registry-contract.mjs` (new, 149 cases) holds what two production comments already claimed was held. The v0.0.165 test named "the registry and the patch schema cannot drift" built BOTH sides of its comparison from the registry itself — `f(REG) === REG`, which would pass against an empty schema — so nothing actually pinned the derivation that turns the table into the sanitiser. The new suite drives `sanitiseParams` instead: every numeric row round-trips a mid-domain value and clamps at both ends, every enum accepts each of its own values and drops a stranger, every rangeable row takes a span and every non-rangeable row refuses one, the integer row rounds its span ENDS as well as its value, and the reserved sampling and modulation-source spellings are asserted against what the engine actually accepts. Proven to bite: breaking the derivation's upper bound turns 31 of the 149 red, and restoring it turns them green. It also gives `isRangeable()` its first caller.
- [x] **the contract doc catches up with the code it describes** — the octave bound is stated as the three different numbers it really is (registry and sanitiser ±2, every voice's own table ±2, the dial alone ±1), the routing edge list documents its 64-edge cap and its inert-edge law, and the rangeable section names the registry column and the test that holds it rather than the hand-kept list it replaced.

## 2026-08-14

- [x] **v0.0.169 — routing phase 2b: the fallback editor joins the registry** — the no-knob.js slider editor carried its own fourteen copies of the dial domains the knob editor had just stopped duplicating; both editors now ask one shared `patchDomain()` — the registry row the engine's sanitiser is built from — with the literals kept only as no-registry fallbacks. Same invisibility contract as every foundation commit: no dial moves, the full gate is the proof.

- [x] **v0.0.168 — the first cable: a dial can follow a source** — the modulation graph his 137 ruling freed begins to exist, engine-first like every half of this programme. `params.routing` is a serialisable list of edges, `{ source, destination }`, with exactly ONE slot per destination — the plan's D4 law, enforced at the sanitiser (the last edge naming a destination wins, because the newest patch replaces the occupant; nothing sums, nothing fights). Two sources exist to route: `lfo.1`, a global sine whose length is set in bars (1–64, advancing once a bar like everything that breathes here), and `macro.1`, a plain 0–1 value — the seed of the macro system that will one day be what the track Randomness dial patches through. While a destination is routed, its walk position IS the source's value, which buys the whole feature its coherence for free: the resolved value, the live readouts and the dial's own moving mark all follow the same number, hold and freeze pin a routed dial exactly as they pin a walk, and v0.0.167's sampling composes — a section-sampled route holds what its source delivered until the next section begins. Spread stays depth (D3): the edge drives WHERE in the span the value sits; the span's width is still the musician's statement of how far it may go. No depth dial, no summing junctions, no UI yet — the sockets wait on the owner's phase-3 gesture ruling, and envelopes, further LFOs and macros, and the power-governor accounting are the phase's remaining slices. engine-smoke pins the laws: a macro-routed level equals the macro's own value to nine decimals and follows an edit next bar, a four-bar LFO repeats every four bars exactly, and the sampling gate holds a routed value all section (254/254).
- [x] **v0.0.167 — a spread can now say WHEN it moves: routing phase 4's engine half** — the plan's own finding was that the owner's proposed sampling control "is the missing control over a mechanism that already exists and is currently hard-coded to bar", and this unhardcodes it. `params.sampling` is a sparse map, keyed exactly like the walks, saying when each spread's walk advances: `chord` holds a drifting value still until a bar that begins a fresh chord, `section` until a bar that begins a section — so a filter that opens with each chord change, or a level that re-settles per section, is now an engine truth rather than a wish. `bar` stays the default every build has had and is never stored; the global dials' spans gate through the same map; the shaped drifts (rise, fall, swell, cycle, step) hold their phase counters under the same law, so a section-sampled swell is one swell per section. Two honesty rulings shaped the wire: `note` is refused at the sanitiser rather than accepted-and-played-as-bar (the per-note path is structurally later — every resolution cache is bar-cadenced), and the tiny sampling dial waits for the owner's phase-3 gesture ruling because its bottom-left bay belongs to the new dial face — until then the feature is reachable to share links and the console, the same engine-half-first pattern as the pitch pin and the voice blend. engine-smoke pins the law with the event-order subtlety documented in the test itself: changes may surface only at indices ≡ 1 mod the period (253/253).
- [x] **v0.0.166 — routing phase 2a: every dial's domain is stated once** — the second foundation commit, invisible like the first. The knob editor named each dial's range three times over (the min/max pair, the value clamp, the write clamp) and the sculpt/call spec tables named seventeen more, every one a private copy of a fact the sanitiser also held. All of them now read the registry row — the sculpt and call tables keep only what is genuinely per-dial (label, format, hint, marks, step), the detune ceiling is the registry's own, and the literal pairs survive only as the fallback an engine without a registry would boot on. One deliberate exception, now documented where it lives instead of being a silent mismatch: the octave DIAL ships ±1 while the registry and sanitiser accept ±2, because every voice's own OCTAVES table clamps at ±1 — a wider dial would draw stops the sound can never reach. Still open for 2b: the full literal-to-loop collapse and the slider fallback's copies. No behaviour moves; the full gate is the proof.
- [x] **v0.0.165 — the parameter registry: routing phase 1 lands** — the first commit of the modulation-routing programme his 137 ruling unblocked, and deliberately invisible: no dial moves, no sound changes, the full gate is the proof. Every voice-patch parameter's facts — domain, curve, unit, whether a span is legal, where it sits in the dial→voice→instrument→track→bus→master chain, when a stepped source samples it — now live in ONE frozen table (`src/scripts/param-registry.js`), and the engine's PATCH_SCHEMA is BUILT from it rather than written beside it. The page's eight boot-time patch probes (`probePatchSource` and family) are gone: they existed to ask the engine questions at runtime that the shared table now answers statically, and their `engineCaps` keys survive with the registry as their source, so every reader gates exactly as before. The plan's reserved wire vocabulary (path levels, env/lfo/macro, sampling points, step rules) moved from prose into code as `RESERVED_TOKENS`, where the serialisation format will read it. engine-smoke pins the registry and the schema against each other — every row has a sanitiser and vice versa, the derivation keeps the old table's corner laws (meaningful-null osc2, octave rounding value AND span ends, enum refusals), and D9's rangeable-iff-not-enum rule is asserted per row. Phase 2 (the knob editor rendering over the registry) is next; the cables come after the foundations.

## 2026-08-13

- [x] **v0.0.164 — a performance now outlives the tab** — closing the raw-take half v0.0.107 left open. The raw timing of a tapped or played take has survived in beat-domain since then, so "Re-fit the last take" could land the same performance on a changed grid — but it lived in an engine closure, and closing the tab threw the performance away, which made the button a promise the next visit broke. The engine now hands the raw take out and takes it back (`getLastTake`/`setLastTake`, the setter sanitising hard — bounded finite rows, a hard cap, garbage clears), the page persists it inside this device's settings on every capture, restores it at boot, and offers Re-fit again. Deliberately narrow: the take rides settings ONLY — `paramsSnapshot` strips it the way it strips the Energy follow's bookkeeping, so no share link, no preset and no undo entry ever carries someone's key-taps (a minted link is byte-identical either way), and loading a preset or link does not throw your performance away. `tests/refit-persist-drive.mjs` (9 checks): the take lands in the engine and the persisted settings, a minted link carries none of it, it survives a REAL reload at the engine, and the restored performance still re-fits onto the grid — five of the nine fail on the pre-change build, the reload law among them.
- [x] **v0.0.163 — the blend editor: several voices on one track, from the page** — the second half of his 128, one version behind the engine half. Every multi-voice track's picker gains a Blend voices… entry (capability-probed, so it only renders where the engine can hold weights) opening a small dialog: one weight per voice, 0 to 10. Two or more nonzero weights store the blend; exactly one is not a blend, it is a choice, and it is honoured as one; none clears it. While a blend is set the picker's live option always names the voice this section actually drew — "Warm · blend" — even when the draw lands on your stored pick, with the way out in its tooltip; and picking any voice explicitly ends the blend, exactly as it ends the wander. `tests/voice-blend-drive.mjs` (9 checks, all at the engine seam): the weights land in the engine, the drawn voice comes from the pool while playing, the label carries the state, the blend survives a real reload, and an explicit pick clears it.
- [x] **v0.0.162 — several voices on one track, weighted by section: the engine half** — his 128, answered with one letter (c: weighted-by-section first, by-register later), built at the seam first the way the pitch pin was. A track may now carry `voiceWeights` — several voices, each with a weight — and every new SECTION draws the sounding voice from them, on the engine's own rng so a share link replays its exact draws. The stored `voice` stays the user's configured anchor (`getParams` reports it untouched); the drawn voice shows through `getResolved`, the same honest seam the voice wander uses — and that anti-monotony wander stands down entirely on a blended track, because an authored blend IS the track's voice policy and a wander on top would overrule it. A track with no blend spends no rng draw, so every existing piece is byte-identical — the frozen reference and all twenty suites hold unchanged. Editing a blend re-draws immediately rather than waiting a section out. engine-smoke pins the laws (251/251, the new one failing on the old engine): containment (a blended pad may only sound its weighted voices, even at full vary.voice), config honesty, per-section variety, and seed determinism. The weights EDITOR — a Blend entry on the voice picker — is the next half; until it lands the feature is reachable to a share link or the console, not the page.
- [x] **v0.0.161 — the genre rules stop being text, and every rule can be made or unmade** — his 133 ("the genre rules are still horribly text based") and his 130 ("the user should be able to edit create and zero all rules from within the app"), built together because they are one complaint: rules lived in three text boxes, and a whole layer of them — the SUBSTITUTION rules, exactly where his dead-rule find lived — was not on screen at all. The panel is graphical now. Chords are chips (each a compact native select, so keyboard and touch work for free, with a remove entry so a chord deletes without a second button); drum patterns and syncopation cells are tap-grids with a heavier edge on each beat, a length control, and a × per lane, pattern and cell; substitutions render as real rules — this chord → that chord at odds you set — addable and deletable like everything else. Deleting a section's every rule now MEANS something: the empty list is stored explicitly and the section stops ruling (chords settle home on I, the kit falls back to the auto kit), instead of silently reverting to the genre's own rules as the old sparse storage did. Storage, share links and the same-dice law are untouched — the model serialises through the same validate/compile path the text boxes fed. `tests/genre-rules-drive.mjs` was rewritten for the controls (21 checks, all at the engine seam): the genre's own grammar renders as chips, tapping the kit grid re-draws the kit from exactly the pattern left standing while bpm and metre keep their draw, zeroed substitutions SURVIVE a close-and-reopen, a one-chord grammar seeds a one-degree loop, and the v0.0.149 promise — rules decide what is played, the voice editor how it sounds — still holds either side of an Apply.
- [x] **v0.0.160 — additive metres: 4/4+3/4 gives 7/4, exactly as he proposed** — his 134, answering the five-beat storage bound his 120 ran into: "Lets get around this by allowing multiple bars - for example: 4/4+3/4 gives 7/4 -problem?" No problem — and his design is the musically standard one (additive metre), so it is built literally. A time signature may now be a CYCLE of up to four bars, each its own legal metre, written with a plus; the engine plays them in order, repeating — bar one 4 beats, bar two 3, which FEELS like one bar of 7/4 while no single bar ever outgrows the step grid's five-beat window. Mixed denominators are legal too (6/8+3/4). The widest component answers for storage and lane sizing (shorter bars play the front of the lane, the law shorter metres have always had), and the scheduler, the planners, the bass groove, the motif and the capture grid all read the bar they are actually planning. The custom-metre control's numerator now takes the cycle directly — type 4+3 over 4 — and restores it after a reload. Plain metres are untouched to the byte: the reference suite and all 20 node suites are green unchanged. `tests/additive-metre-drive.mjs` (7 checks) pins it in a real browser at the ENGINE seam — stored value, alternating bar events at their own lengths, and the reload round-trip; engine-smoke pins the parser, the cycle arithmetic (negative bars included), the widest-bar law and the sanitiser's refusals (trailing plus, an illegal component, a five-bar cycle).
- [x] **v0.0.159 — every drum sound owns its envelope, and the kit dial becomes a scale** — his 129 ruling, option a, closing the last engine residual from the July audit. A kit publishes ONE envelope but plays three sounds, so any patch — a dial nudge, even Reset — pulled every sound toward one number. Now each kit publishes each sound's OWN authored envelope (`defaults.perKind`), the kit editor's per-sound tabs open on that truth and their envelope dials mean exactly what they say, and the Common tab's **Decay dial is a SCALE over all of them** — at its published value nothing changes, at double every sound rings twice its own length, which is what a kit-wide dial honestly means. Attack, sustain and release stay kit-wide values any sound's tab can override, because a dial that says 0.001 s must mean 0.001 s. Underneath, the engine now keeps envelope PROVENANCE across the per-lane merge (the kit-wide dials and a lane's own overrides arrive at the voice separately instead of folded into one indistinguishable number), and every layer's ring is anchored to its own sound's published decay rather than the kit-wide one — which also quietly fixes the secondaries that were up to 16% off under any patch. Measured, not asserted: voices-smoke gains two laws that fail on the old engine and pass now — a kit patched with its own defaults must fade layer-for-layer at the same times as the bare kit (all four kits, all three sounds), and a doubled kit Decay must double each sound's own ring while a sound's own decay override means exactly that number. 291/291, engine-smoke 249/249 with the provenance pinned at the play() seam.
- [x] **v0.0.158 — the detune dial stops meaning three different things with one label** — his 135 asked, of a two-oscillator voice, "give the exact detune between them - is that what people expect", and whether the eighties saw's dial should be called something other than detune. Both halves answered by making the engine's own numbers honest. Every voice now declares what its dial IS: on the eleven genuine two-oscillator voices it is called **Detune and the dial value IS the signed gap between osc 1 and osc 2 in cents** — the layer spreads were normalised so one cent of dial is one cent of gap (choir's were -1/+0.5556 per unit; nylon's second string is 5 cents FLAT and its dial now reads -5 rather than a 5 that secretly meant minus), and it hides when OSC 2 is off, because a gap needs two — which is his 117 instinct, now true by construction. On the three unison stacks (warm, strings, poly saw) the dial keeps the conventional name **Spread** — the answer to his "chorus..?" is no: unison detune/spread is what synths call a widened stack, chorus is delayed modulated copies, a different mechanism — and on the three jitter voices (glass, bell, chimes) Spread scatters per note as before. The three re-voiced defaults moved WITH their spreads in the same commit, so every voice sounds bit-identical at its own defaults, proven by a new voices-smoke test that renders each pair voice and reads the layers' cents off the actual graph: one cent of dial must measure one cent of gap (289/289). The three factory presets that pin a choir detune were rescaled by the same factor, so their sound is unchanged too. The one honest cost: a share link or saved setup that stored its own detune on choir, nylon or worn keys now plays that number as an exact gap rather than the old wider scaling — a slightly narrower voice, traded for a dial that tells the truth from here on.
- [x] **v0.0.157 — the processor column lines up with the transport** — his 136, the item gating the production ship: PROCESSOR was one stacked block floating in the button row, and he asked for a right-aligned column instead — the title level with TRANSPORT, the dial level with the transport buttons, and the tier/notes/fps readout level with the genre picker. Built exactly so, reversing v0.0.96's one-block merge: each of the three elements is now the last item of its own row, pushed right, and the three right edges meet at the panel edge — measured at 919.0px all three, not eyeballed. The one fault the measurement caught: the button row centred its 56px buttons against the dial's taller block (knob plus caption), dropping every button 8.5px below the knob; top-aligning the row puts knob, Play, Pause and the icon buttons all on one top edge (168.4px, all four). The title may overhang the clock button below it, which he allowed explicitly. The no-governor path now removes all three elements, and the guided tour points at the dial itself.

- [x] **v0.0.156 — the edit-swallow item closes as a measurement, not a fix** — the consult follow-up said complexity edits were swallowed by a frozen plan for ~2 bars and prescribed invalidating those plans on an upward move. Both halves are out of date, and the honest close was to prove it rather than ship the prescription. Re-measured today: 1.25 bars to the first audible difference at low energy, and **1.01 bars at mid energy — the differing note is the next bar's own downbeat**, 8 ms behind the barline because the engine humanises timing. The prescribed fix was then built exactly as filed — an upward complexity edit cleared the percussion bank, the craft bass groove and the auto-arp pattern at the next barline — and measured INERT: the groove it clears is default-off, the bank held at most one entry in every seeded run, and the treatment run's schedule was byte-identical to the untouched engine's at both energies. A fix that changes nothing measurable does not ship, so it was reverted, and what ships instead is the pin: `energy-measure` gains D2b, holding the mid-energy landing at one bar (plus the stated humanisation allowance), so any future cache that replays rhythm material across an edit goes red the day it lands. The low-energy quarter-note tail — identical rng draws crossing decision thresholds under a live complexity read, not a replayed plan — stays visible, unasserted, in D2's KNOWN SHORT line.

- [x] **v0.0.155 — Fit chords to the tune: the derivation now runs the other way** — his compose brief's first entry point, "Play or type a melody, chords derived algorithmically on request", has had the request half missing since the day typed melody landed: you could hand the app a tune but not ask it for the harmony. A new button in the Create panel reads whatever tune the chosen instrument already carries — typed, sung from words, imported from a MIDI file, or drawn a step at a time; it reads the GRID, not any text box, so how the notes got there does not matter — and writes the diatonic chord loop that fits them, through the chord editor's own commit funnel so the editor re-reads what the engine actually accepted. The derivation is a pure module (`src/scripts/harmonise.js`) with its rules pinned in `tests/harmonise-smoke.mjs` (13 cases): notes ON the beat decide each bar's chord and the downbeat decides most of all, notes between the beats are passing notes and cost nothing, notes off the scale are chromatic and carry no harmonic evidence at all, an empty bar CARRIES the chord it is inside rather than wandering, fourth-and-fifth root motion is worth a little and sitting still costs a little, the loop prefers to start and end at home, and four bars of the same chord collapse to one chord (I V I V to I V) because a loop is its period. No draw anywhere — the same tune always yields the same chords, which is what lets a person argue with the result; and every result is ordinary chord-editor data, each chord a card you can press to hear and change. `tests/derive-chords-drive.mjs` (14 checks) asserts at the ENGINE: the loop lands in harmony.seed, C-E-G then G-B-D reads I then V with the key pinned and verified, F-A-C on the BASS reads IV (any instrument's tune can be the source), an unpitched track is refused with a reason and leaves the loop that was set untouched — and writing it re-confirmed a repo trap in a new coat: the Root and Scale selects sit on the hidden Advanced tab, where Playwright's selectOption fails SILENTLY, so the drive dispatches the page's own change events and then verifies the pin at the engine before asserting anything that depends on it.

## 2026-08-04

- [x] **v0.0.154 — the compose panel stops growing under its own pickers** — the three instrument pickers added over the last two versions inherited the big genre control's 44-pixel minimum height, which is a touch-target rule and the wrong one here: beside a 34-pixel button it wrapped each of those rows onto two lines and made the Create panel 150 pixels taller, far enough that its lower rows sat under the sticky transport on a short window. Nobody saw it, because nobody in this container can see: the full drive sweep caught it as `typed-melody-drive` going **RED** with "play-along-toggle intercepts pointer events" — a click that could not land, which is exactly what a real click would have done too. The pickers now match the buttons beside them (the rule is two classes deep so it beats `.genre-select`, which is declared later) and the text inputs give up a little width so each row fits on one line; the panel is back to its old height. The select font stays at 16px on purpose — anything smaller makes iOS Safari zoom the page when a select takes focus. Every compose drive is green again and the layout sweep is clean at both the desktop and phone viewports.

- [x] **v0.0.153 — and the beat row names its kit too** — the same follow-through one version on: Type a beat now carries a picker like every other compose row, and it lists KITS rather than instruments. Which set a track belongs to is decided by its voice set rather than its id, so a kit a listener built themselves belongs in that list and a tuned track cannot appear in it — the same reasoning that keeps the kit out of the melody and words pickers, since a kit has no note names to type or sing. The typed beat writes onto the chosen kit's OWN lanes, so the letters land on that kit's sounds instead of on three lane ids assumed to exist. `typed-beat-drive` 17 → 20 checks.

- [x] **v0.0.152 — every compose tool can write onto any instrument** — his open item, in his words: "chords=pad, melody=melody, drums=percussion, bass, arp, or even texture if they like… Diving in on any track must be easy." The compose tools were each hard-wired: typed notes went to the melody, the words went to the melody, and only MIDI import had a picker. Now every row that writes notes has the same picker, filled by one populator so the three cannot drift apart — typed melody and words offer the tuned sequenced tracks (a kit has no note names to type or sing), while MIDI import keeps the whole sequenced set, since a file can carry a drum part. It is not just a label: the grid resolution, the metre and the pulse positions all come from the TARGET track, so pointing the words at the bass scans them onto the bass's own grid at its own resolution. `words-drive` grows 18 → 24 checks and asserts the useful half at the engine — the words land on the instrument the picker names, that instrument comes on, the melody is left exactly as it was, and the report says which instrument sang. What is left of his item is the derivation half ("play a melody and it just makes chords"), which is the planner layer rather than a picker.

- [x] **v0.0.151 — the syllable counter now has a number, not a shrug** — v0.0.148 shipped the words method on a heuristic and said honestly that it is sometimes wrong, which is not a specification. So its suite gained a fifty-word corpus of ordinary English with the counts a singer would need, plus an allowed-miss number that can be tightened and must never be loosened. The first run scored 46/50 and printed which words it missed — and those four pointed straight at one missing rule: an `i` before another vowel is a syllable of its own (pi-A-no, am-BI-ent, ra-di-o, cu-ri-ous) unless it follows t, s or c, where the pair is a single sound (na-tion, mis-sion, spe-cial). With that rule the corpus is **50/50** and the allowance is zero. The other two apparent misses were not misses: "every" and "evening" are sung as two syllables or three depending on the singer, so the corpus asks for the count a singer actually needs rather than pretending there is one right answer.

- [x] **v0.0.150 — every grid gesture tested twice over, and Clear stops leaving its ties behind** — his 119's view bug had a general shape worth closing off: the sideways tie DRAG updated the cells and never redrew the merged boxes, while the keyboard shortcut for the same edit went through a path that did — and the drive guarding ties had only ever pressed the key, so the gesture a person actually uses was the untested one. Every other pointer gesture in the grid carried the same risk. `tests/gesture-integrity-drive.mjs` now performs each one with real pointer events — the velocity band in a cell's upper half, the note length in its lower half, the tie sideways, the pitch pin on alt+drag, and the probability dot — and holds both halves of the contract at once: the engine's stored step must change, and the grid must show it with nothing re-rendering in between. Writing it found a fault of exactly the same family: **the mass-edit buttons rewrote a lane's rhythm and left its ties in place**, so a lane you had just Cleared still drew merged wide boxes and still had cells hidden inside them — unclickable, and indistinguishable from a grid that had ignored you. Fill, Every 2nd and Clear all untie now, which is the only honest reading of what they say they do.

- [x] **v0.0.149 — the rules editor stops overwriting the sound you built** — his 124 asked what the difference between a Genre and a Preset actually is, and whether the genre rules "really override the much easier to use GUI of the Preset editor". They did, and the honest answer was a fix rather than an explanation. Apply rebuilt the entire setup from the genre, so every voice choice went back to whatever the genre names — silently, under a button whose own message promises that only what you changed changes. Measured either side: on the old code a pad set by hand to Warm came back as the genre's Polysaw the moment Apply was pressed. The division is now the one the answer states — the RULES decide what is PLAYED (which chords may follow which, which patterns the kit is written from), the voice and preset editors decide what it SOUNDS LIKE — so Apply takes only the rule-derived material from the recompile and leaves voices, patches, levels, states and mono where you put them. The message under the dials says so, at the length it takes to read. `genre-rules-drive` grows 16 → 20 checks and the new ones fail on the old code; writing them caught two traps of my own making, since the voice select is `track-voice-<id>` and its "live" entry is a sentinel the page ignores by design — a first version of the test changed nothing and then congratulated itself, which is the failure mode every test in here is written to avoid.

- [x] **v0.0.148 — you can type the words now, and the app reads the tune out of them** — his 116, the third compose method, settled in his own words: "c) lyrics that you enter - like a poem - and the system determines the meter, so can, for example, generate a melody based on the chosen chord sequence." That sentence is the whole design and it is built as exactly three steps. **The words are scanned** — a new pure module counts syllables by vowel groups with the corrections English needs (silent final e, -le, -ed, the hiatus in cre-a-tion, the missing vowel in rhy-thm) and marks which are stressed, using a function-word list, because what makes "the sky" a rhythm is that "the" carries no stress. **The metre comes out of the stresses**: the commonest gap between them is the foot, so twos are 4/4 and threes are 6/8 — and a confident reading is applied and named ("The words scan in threes, so the metre is now 6/8"), while a line with nothing to go on leaves the metre alone rather than asserting one nobody typed. **Then the melody**: every stressed syllable lands on one of the metre's own pulses, each line starts its own bar because a line break is a phrase, and the pitches come from your chord sequence — a chord tone under a stressed syllable, taken root-third-fifth in turn so the line moves, and the scale step between under an unstressed one, which is what a passing note is. With no chords set it sits on the tonic and says so rather than implying a progression. What this rests on is stated rather than hidden: English stress cannot be computed exactly without a pronouncing dictionary that would cost a megabyte, so this is a heuristic, its known misses are pinned in its own suite, and every note it writes is an ordinary editable step — a stress in the wrong place is a step you drag, which is the only reason it is allowed to guess at all. `tests/lyrics-smoke.mjs` (8 cases) guards the reading; `tests/words-drive.mjs` (18 checks) guards the writing at the ENGINE, computing the chord tones it expects from the engine's own scale table — a first version asserted C major while the visit was in C aeolian, which is exactly the trap this repo's own notes warn about.

- [x] **v0.0.147 — you can type a drum beat now** — his 118, "Much better, how do I type percussion?", where the honest answer was "you cannot": typing wrote note names and a kit has none. A drum line is now typed the way drum machines and trackers have always written one — **one token per step**, letters naming the sound, a dot for a rest: `K H s H KH H S H` is four to the floor with hats between, a ghost snare on three and a hard one on seven. Two letters in one token are two sounds on the same step, `|` is a bar break you can see, and **case is the accent** — capitals hit hard, lower case is a ghost note, which is exactly what a tracker's notation has always meant by it. A line longer than a bar writes more bars chained in order, capped at eight, through the same machinery the typed melody already uses, because a drum line is not a special case; and a letter naming a lane the kit hasn't got falls to the nearest one it has, so the three-letter notation still lands on a bigger kit. `tests/typed-beat-drive.mjs` (17 checks) asserts the engine's own stored lanes rather than the drawn grid, including the accents, the chained overflow, the explicit bar break, and that a line with no drums in it says what the letters are and leaves the beat that was there alone.

- [x] **v0.0.146 — a note you can finish reading, and two more time signatures** — his 123, "Good but you barely have time to read the note": a note's life was a flat number chosen at each call site, and the Tempo refusal ran seven seconds for forty-five words, which is about half the time it takes to read them. Notes now last as long as they take to READ — 140 words a minute, a floor for the one-word ones like "Undone." and a ceiling so nothing camps on screen — and clicking a note dismisses it early, which is what the pointer wants to do anyway. Then his 120, "Time signatures, is there a reason not to allow any positive non-zero rational number in numerator and denominator?" The reason was never the ratio: a step lane stores five quarter-note beats, and a bar longer than that has nowhere to be drawn. Inside five beats every denominator that names a real note value now works — **halves and sixteenths join quarters and eighths**, so 2/2 gives a cut-time feel with two slow pulses where 4/4 has four, and 3/16 to 20/16 give the odd metres a great deal of Balkan and progressive music is written in: 5/16, 7/16, 11/16. Sixteenths group by the same rule as eighths one note value finer (threes where the count divides by three, otherwise pairs with a three at the end), so those two denominators are one rule at two scales rather than two tables that can drift apart. What is still refused is refused with its reason said out loud rather than hidden: a denominator that is not a power of two — sevenths, thirds — has no note value to draw a beat with, so the grid could not label a single one of its own cells.

- [x] **v0.0.145 — the probability dots grow as you use them** — the last of his 119: "Probability groups better, but once one dot is selected, a second empty dot should appear next to it to allow a second probability group, then third, etc." The old row had one dot per step and a selection mode you could not see: pressing a dot started a group or selected one, and starting a SECOND group meant knowing to press Escape first. Now every step shows one dot per group the lane uses **plus an empty one for the next**, so the affordance is on the grid: press the empty dot to start a group, press a coloured dot to put that step in that group — gaps still allowed, which was his earlier ruling — and press the dot a step already holds to take it out. No mode, nothing to remember. Six groups is the cap, one per colour, and the empty dot stops appearing there, because a group with no colour of its own cannot be read off the grid. The drive was rewritten for the new gesture and now addresses dots by (step, group) rather than by a flat index — 23 checks, including the progressive appearance itself, and the cap exercised by pressing dots the way a person would rather than by writing groups straight into the engine.

- [x] **v0.0.144 — a tie now sounds like what it draws, and the boxes merge while you are dragging them** — his 119: "Better although there's a view bug, you need to go out and in to see the wide boxes. Also trying this with a bass line having 4 1/16ths as a merged note sounds no different to a single 1/16th." Both true, both measured, both fixed. **The view bug had a telling cause:** the merge into one wide box is drawn by one function, and the sideways DRAG never called it — only the keyboard shortcut did, through a path that happened to redraw. So the boxes stayed separate until something else rebuilt the grid, which is exactly what closing and reopening the editor does. The drive that guards ties had only ever pressed the key, so the gesture he actually uses was the untested one; it now performs a real pointer drag, and both new checks fail on the old code. **The sound was two faults with one symptom:** a manual bass step rang until the next onset, so a lone sixteenth already sustained to the end of the bar — 1.8 s at 120 bpm, the same either way, which is precisely what he heard — and a manual melody step was a flat one beat, which a four-slot tie at sixteenths exactly equalled. A note a grid draws now lasts the slots it spans, times its gate if it names one: one box is one slot, four tied are four, and sustaining a sparse grid is something you ASK for by tying the steps or setting the gate rather than the only thing on offer. A grid that draws a one-slot box and plays eight is the same fault as a readout that lies about its dial. Two factory presets (deep-focus, push) move deliberately with it — a bass note drawn as a sixteenth was 0.362 beats and is now the 0.134 it was drawn as — and the frozen reference was re-taken at 0.0.144 in this same commit.

- [x] **v0.0.143 — a blank slate that is actually blank, and a button that was only pretending to clear** — his 117, in his words: "When I click blank slate, I still get a chord sequence, and if I press play and turn on all instruments, stuff happens - that's not particularly blank. Also, none of the instruments seem to be at sensible blanks. For example I would expect only one OSC to be selected, detune to be zero, the filter cutoff to be fully open with resonance zero, likewise envelope should be at whatever people would expect of a zeroed state." Every part of that is now true. The chord sequence was the engine writing its own progression because nothing had said otherwise — a pad and a texture have no step grid, so they played a composition nobody asked for; a blank now carries ONE tonic chord, which is the least an instrument that is switched on can do while still being an instrument. The arp was generating a pattern over its own cleared lane, because the engine only reads those steps when the arp's mode is manual. And every tuned track now opens on an init patch: one oscillator, no spread, the filter as open as the schema goes with resonance and filter-envelope at their floors, and a plain gate for an envelope — on the instant a note starts, full while it is held, off the instant it ends. The kit is deliberately left alone: it publishes one envelope for three sounds, so a zeroed envelope there would leave three clicks and no per-sound way back until that ruling lands. **Then the test found something bigger than the button.** Measuring the kit's patch either side of a Blank slate — rather than assuming it was empty — passed on one random genre draw and failed on the next, because **a patch could only ever accumulate**: the engine merges patches, which is what lets one dial move without disturbing the rest, but nothing in the API could take one back. So a genre's kit patch survived a blank slate, and the **Zero voices** button, whose own message reads "patches cleared", cleared them in the page's settings while the engine went on playing the patch it already had. `patches: { pad: null }` now drops a track's whole bank and `{ pad: { warm: null } }` drops one voice's entry, absent still inherits, and junk that is not null still leaves the stored patch alone; both buttons clear at the engine behind a capability probe, so an older bundle does less rather than lying. His detune question gets an answer and a rename rather than an argument: the dial is the SPREAD of the oscillator stack — several voices are a stack of one waveform, the eighties saw being five saws spread apart — which is why it still acts with OSC 2 off, and the voices that genuinely cannot spread already hide it. `create-drive` goes 29 → 42 checks, including "turn every instrument on after a blank and nothing was written", and seven of the new ones were proven to fail on the old code by stashing the fix and re-running.

## 2026-07-31

- [x] **v0.0.142 — the sweep stops calling a tired machine a regression** — `tests/sweep-drives.sh` re-runs a failed drive alone to tell a load flake from a real fault, and it waited the same three seconds it waits between drives. That is not a rest: the re-run lands on a Mac that has just driven thirty browsers back to back, inherits the load that caused the failure, and reports **RED**. It did exactly that to `submit-drive` twice this session, a drive that passes solo on a quiet machine every time. The solo re-run now rests twenty seconds (`SWEEP_SOLO_COOLDOWN`), and a drive that still fails gets a SECOND rested run before the runner will call it red. A gate that cries wolf is a gate that gets switched off, which is the whole reason this runner exists — and the same reasoning that made it split RED from FLAKY in the first place applies to how it measures the difference.

- [x] **v0.0.141 — the last click: a slur was arriving at its volume instantly** — `bass/breath`'s legato handover was the one finding left, and the loudest of the three: **0.147 of full scale in one sample, 92% of the note's own loudness, on a SINE voice — so none of it could be the waveform.** The takeover was cancelling the amp envelope and SETTING it to the held level at the handover instant. On a slow-attack voice that is nowhere near where the envelope actually is: `breath` takes 180 ms to open and the second note lands at 120 ms, so the gain jumped most of the way to full in a single sample — a click precisely where a slur is supposed to be seamless. It now holds the value the envelope really has at that instant (`cancelAndHoldAtTime`, the primitive that exists for this, versus a bare cancel which drops back to the last completed event) and arrives at the level over the same glide the pitch is taking. A slur is one continuous sound; its loudness should arrive the way its pitch does. Every declared parallel layer — the breath, air and shimmer a slurred phrase keeps — is re-held the same way. Where the method is missing the old behaviour stands, because a cancel with no anchor is worse than a step. **The harness's recorded-click list is now EMPTY**, which is the result worth stating: every click the corrected measurement found has been fixed, the frozen reference did not move for any of them, and a new one anywhere fails the gate.

- [x] **v0.0.140 — two of the clicks he reported are fixed, and the cause was a source being switched off while it was still making sound** — with the harness finally measuring the right 50 milliseconds (v0.0.138) and its law able to tell a defect from an instrument (v0.0.139), the two loudest findings gave up their cause to one experiment. `noiseBurst` — the key click on an organ stab, the mallet on a marimba bar, the fingertip on a string — stopped its noise SOURCE twenty milliseconds after the burst's envelope finished. Stopping a buffer source is instantaneous, and unlike an envelope it is quantised to the render block, which is exactly why two voices whose bursts are different lengths lost their signal **at the same sample**: both stops rounded up to the same block boundary, 30 ms in. Moving the stop half a second later made the step vanish (marimba: 0.103 of full scale in one sample, 82% of the note's own loudness right there, down to 0.005), which identified it; the fix is to let the source stop with the rest of the note, through the teardown every other node already uses. A looping noise buffer sitting at its envelope floor costs one node's CPU for the length of one note; a click costs the listener the note. **arp/marimba 0.103 → 0.005 and melody/stab 0.031 → 0.005**, both now below the gate's floor, and the frozen audio reference did not move at all — a source's stop time is not part of the rendered stream, so this is a fix with no cost to anything already made. One click is left, recorded as a live baseline: `bass/breath`'s slur handover, 92% of its own loudness against 1.4% a few cycles on, on a SINE voice, so nothing about it can be waveform — that one lives in the legato takeover path rather than in an envelope. And three of the original suspects are cleared with numbers, including `bass/fingered`, the voice he named: its onset steps recur at the waveform period, so a note of it in isolation is clean and the Soul Groove report belongs to the playing rather than the voice.

- [x] **v0.0.139 — the click hunt gets its last two discriminators, and half the suspects turn out to be the instrument** — v0.0.138 corrected the window and left six findings. Two of them looked identical on paper to a defect and were not, so the law grew two more clauses, each a fact about what a click IS rather than a threshold: a lone step is suspicious but a DENSE CLUSTER of steep steps is a noise transient the voice was written to have (a mallet, a fingertip on a string, a hand on a skin), and a step that RECURS one waveform period later is a sawtooth or pulse wrap heard through an attack that has the filter open — the instrument, not a fault. Three events survive all four filters, and they are now the whole of the open defect: **arp/marimba's onset** (one step of 0.103, 82% of its own loudness against 2.3% a few cycles on, at exactly 30.0 ms — to the sample, and at the same sample on melody/stab, whose envelope constants are different; it does not recur at the waveform period, the next five cycles carrying 0.1–0.7% of it, and it does not move when the note's length changes. The obvious suspect, a buffer source stopped while still audible, was tested by ramping that gain to true zero before the stop: the step did not move, so it is ruled out and the change was reverted rather than shipped as a placebo), **melody/stab's onset** (38% against 5.7%, same shape), and **bass/breath's slur handover** (92% against 1.4%, on a SINE voice, so nothing about it can be waveform). Cleared with measurements, which is the more interesting half: **bass/fingered — the voice he named — melody/nylon and texture/chimes** all show big onset steps that recur at the waveform period, so a note of fingered bass in isolation is clean. Its fast-line, mono-cancel and slur renders are clean too. That moves the Soul Groove report off the voice and onto the PLAYING — fast lines, re-strikes, the scheduling around them — which is a different search with a different tool, and the TODO says so rather than leaving a fixed-looking entry open on a voice that measures fine.

- [x] **v0.0.138 — the click he reported twice is located, because the test that hunted it had been measuring the wrong 50 milliseconds** — `tests/onset-render.mjs` renders voices into an offline context and measures the sample-to-sample steps at note onset. It passed `time: 0.25` in the note object. The shipped voices read `when:`. So every note it has ever rendered started at zero, its "onset" window sat a quarter of a second into the body, and its "body" window sat in the tail — a gate hardened only one version ago, green, asserting five numbers per voice, none of which described the event it was named after. The harness now proves it found the note (nothing may sound before the scheduled onset, which is exactly what the mix-up broke) and renders **every voice in the library — 37, not the four basses it began with**, since his second report was not about bass at all. Then the law had to be rebuilt twice more, because the first honest measurement produced 68 findings and most were not clicks: printing the samples around each one showed a rise to full amplitude and a drop back to zero, once per cycle — a sawtooth's wrap, which is the instrument, not a defect. So a click is now a step that stands out from the SAME note's own waveform: it must clear 0.02 of full scale, be more than 1.6× as steep for its own loudness as the note is a few cycles later, AND be a lone step rather than a dense cluster — because a mallet strike, a fingertip on a string and a hand on a skin are all deliberate noise transients, dozens of steep steps in a row. What survives all three is six single discontinuities, each one steep step in an otherwise smooth window: **bass/fingered at onset — the voice he named — 0.053 of full scale, 49% steepness against 6.5% a few cycles on**, plus melody/nylon (15× its own), arp/marimba (35×), melody/stab (6.7×), texture/chimes (3.6×) and bass/breath's slur handover (64×). All six share one signature — the gain drops to silence for a single sample mid-ramp and resumes — which is a value being re-asserted on a param that is already ramping, not an attack. They are recorded as a live baseline with their measured numbers: a NEW click anywhere fails the gate, a known one getting a fifth worse fails, and one that gets FIXED must be removed from the list, so the baseline cannot quietly outlive the defect. The fixes are voice-envelope surgery that moves the frozen reference and land one at a time; the TODO carries the numbers to beat.

- [x] **v0.0.137 — triplets, the one division his ladder could not reach** — item 89 is finished. He drew its own boundary a year of features ago: "a x2 /2 controller could easily control all eventualities **other than triplets**." So triplets are not bent onto the halving ladder — they get a **second ladder** (crotchet, quaver, semiquaver and demi-semiquaver triplets: three in the space of two, at each note value) and their own **Triplets** button beside the ×2/÷2 pair. Press it and the grid keeps the note value you were on: quavers become quaver triplets, twelve cells to a 4/4 bar where eight sat. ×2/÷2 then walks the TRIPLET note values rather than straightening the grid under you, and pressing Triplets again returns to straight time at the same value. Three decisions worth naming: it is a second ladder rather than a per-step flag, because a triplet grid is a different grid and a per-step flag would mean two step sizes in one lane and a playhead nobody could draw honestly; switching feel maps across the ladders BY POSITION, not by nearest number, because ⅔ is numerically nearer a quaver than ⅓ is and "quavers → crotchet triplets" is not the swap anyone asked for; and what a bar plays now FLOORS, so no rung can draw a step that sounds past the barline (a crotchet-triplet grid is four and a half slots to a 3/4 bar, and a rounded-up fifth cell would have been a lie). A beat that lands BETWEEN two slots now gets no beat mark at all, for the same reason — every straight rung divides a beat exactly, so this only removes marks that were never true. Ladder membership is matched with a tolerance rather than `===`, because a rung that has been through JSON in a share link can differ from ⅓ in its last bit and would otherwise read as "not a resolution" and silently draw as semiquavers. Probed separately from the straight ladder, so an older engine bundle ships ×2/÷2 and simply has no Triplets button. `tests/resolution-drive.mjs` grows to 37 checks (readout, cell count, the engine's stored rung and lane length, beat marks, a step flipped on a triplet grid, and the round trip back to straight); the engine's ladder laws — including that every rung of every metre plays inside its own bar and stores no more than its lane holds — are pinned in engine-smoke. The panel legend, the guided tour and `docs/engine-v2-contract.md` all say it in the same commit. Frozen reference untouched: the default is still the sixteenth, and it is still not written into the tree.

- [x] **v0.0.136 — the modulation graph's first two sources: an LFO, and a sample-and-hold** — his reason for wanting the patching system was one sentence ("so you can have a voice crescendo when you want it to rather than just setting it to be a random volume between min and max"), and v0.0.72 answered the crescendo half with span SHAPES. Two more shapes finish the part that needs no new UI at all: **Cycle** is an LFO — a sine between the span's two ends, endlessly, at the rate and bar-length the two dials beside it already name, which is vibrato, tremolo, or a filter breathing depending on which dial you put it on — and **Step** is a sample-and-hold, one fresh random value per pass held flat until the next. Because they arrive on the machinery that already exists, EVERY spreadable dial gets both at once rather than waiting for patch sockets. Each option is offered only if the engine really keeps it (the list grew this version, and an older engine bundle would silently drop a value, leaving a dead control). Pinned in engine-smoke through the engine's own published values — a cycle must rise AND fall, a hold must hold and must eventually move, and Rise is still a one-way ramp within each pass — and in `driftshape-drive` at the engine seam. What is still to come is the ROUTING half: a source driving a dial other than its own, envelopes and macros, and the sockets; Energy's filter axis waits for it deliberately, because a hidden multiplier on a visible dial is the thing this project refuses to ship.

- [x] **v0.0.135 — Help me start actually asks** — his guided start, as he described it: "ask if people want to produce a certain genre (possibly giving them a genre tree, with other as final answer with most) then let them know which instruments composers in that genre most commonly start with. That would help the many people who simply don't know where to start." Two questions, never more: the FEEL first (the same mood grouping the genre list already uses, so nothing new has to be learned, and only moods that actually have listed genres are offered — a question with no answers is not a question), then the genre inside it. **"Something else" ends every level**, exactly as he asked, and the last one is not a dead end: it offers every listed genre at once plus a way in with no genre at all, which names the three routes that need none (tap a rhythm, type the chords, the keys). Answering seeds that genre's voices through the SAME path the seed picker uses — not a second implementation — and switches nothing on or off, because a guided start that began playing would be making the choice for the person it is helping. `tests/guided-start-drive.mjs` (18 checks) walks both branches and the escape; writing it caught two of my own wrong assumptions (only two genres are public, by his own v0.0.64 ruling, so only one mood has any; and a fresh visit already has a genre playing, so the law is that the pick changes nothing that sounds).

- [x] **v0.0.134 — a MIDI file is a way in: import lands on the compose side, as editable steps** — his item 96 settled where this belongs ("MIDI file import is a compose option IMHO, the assumption is that the user composed the MIDI in another app"), and the pieces it needed — per-step pitch pins, chained bars, a choosable grid resolution — are all in place now. Create gains **Import a MIDI file** with an instrument picker: the file's notes arrive as ordinary PINNED steps on that track's grid, at whatever resolution the grid is set to, one bar per tab played in order (cap eight bars), so an imported piece is exactly as editable as a typed one and nothing about it is a special case. The reader is ours — no third-party code reaches this page and the container cannot fetch any — and covers what real files contain: format 0 and 1, running status, a note-on at velocity 0 as a note-off, a note left open at a track's end, several tracks merged in time order, per-track channels, and tempo/time-signature meta. It REFUSES the two kinds that cannot honestly be imported, with a reason a musician can act on: format 2 (a set of independent songs) and SMPTE-timed files (video timecode, no bar to land on). What could not fit is always reported — a chord collapsing to one note per slot because a step grid holds one, notes past eight bars, and the file's own metre differing from the piece's. `tests/midi-file-smoke.mjs` (8 cases, files built in the test rather than committed as unreviewable binaries) and `tests/midi-import-drive.mjs` (11 checks, asserting the imported steps at the ENGINE and that a non-MIDI file leaves the previous import untouched). The TODO entry claiming import belonged on the play side was stale against his own ruling and is corrected with it.

- [x] **v0.0.133 — a composed sequence starts at its own beginning** — the filed chain-offset finding, fixed as it asked ("advance-only-while-sounding"). Two faults produced one symptom: the sequencer advance runs at the TOP of a bar, before anything reads a lane, so a track's very first bar was already one advance past slot 0 — and it ran for tracks that were still SILENT, and melody waits two bars at the defaults, so a typed multi-bar melody opened several bars into its own composition and the user's sequence was heard rotated. A silent track now leaves its sequence where it is, and the first bar a track sounds is slot 0. `engine-smoke` pins the opening bar as well as the cycle, and a DUPLICATE of that whole test — which had crept in during an earlier edit and would have passed forever without asserting anything new — is gone. Five multi-groove genres' frozen streams move DELIBERATELY (a skipped bar spends no rng draw, so their pattern order shifts by one) and the baseline is re-frozen at 0.0.132 in this same commit.

- [x] **v0.0.132 — the ×2 / ÷2 control, and a grid that draws what the engine plays** — his item 89's editing resolution, finished. A ×2 / ÷2 pair sits under each melodic grid with a readout naming the NOTE VALUE, because that is what a musician is choosing: crotchets, quavers, semiquavers, demi-semiquavers, hemi-demi-semiquavers. Pressing one asks the ENGINE to resize the lane and then adopts its answer rather than guessing at a length — the readout-lying failure class this project keeps catching — and the grid redraws to exactly the slots the bar plays at that rung (16 cells at the default in 4/4, 32 one rung finer, 64 at the bottom), with the beat marks moving with it instead of staying every fourth cell. Capability-probed like every engine seam: the control appears only where the sanitiser both keeps the value AND sizes the lane to it, so a build whose engine drops `stepBeats` shows no control instead of a grid over steps that never sound. The arp is excluded — its lane is indexed by arp step at its own rate, which is its own resolution already. `tests/resolution-drive.mjs` (20 checks) walks the whole ladder in a real browser, asserting the cell count, the note-value readout, the engine's stored rung and lane length at each step, both end-stops, and that a step drawn on a non-default grid reaches the engine at the slot it was drawn on. The legend and the manual release pass document it; triplets remain the division the ladder cannot reach, and the tooltip says so rather than pretending.

- [x] **v0.0.131 — the engine learns his ×2/÷2 editing resolution** — the blocker under the last unbuilt half of his item 89 ("add to the todo the ability to choose the editing resolution, so you can input whole notes, semi-brieves, quavers, semi-quavers, demi-semi-quavers, triplets… a x2 /2 controller could easily control all eventualities other than triplets"). A track now carries its own `stepBeats` on that exact ladder — 1, ½, ¼, ⅛, 1⁄16 of a beat — and everything downstream reads it: lanes SIZE to the resolution (5, 10, 20, 40 or 80 slots, dimensioned by the widest metre the sequencer accepts, with a shorter bar playing a prefix exactly as it always has), and the bass, melody, percussion and user-track manual planners, the gate/tie note lengths, and the capture quantiser all take a slot to mean the same thing. The default is the sixteenth every version has baked, and it is not even WRITTEN into the tree — so a stored setup, a share link and the frozen reference are byte-identical (24/24), which is the whole point of doing it this way rather than widening every lane for everyone. Pinned in engine-smoke (247 checks): the ladder's exact rungs, lane and per-bar sizing at each, one track's finer grid leaving its siblings alone, a kit's every lane following its own track, and an off-ladder ask (a triplet, junk, zero) keeping the stored rung instead of silently snapping. The ×2/÷2 CONTROL and the grid drawing the live resolution are the next build; triplets remain the case his own ladder cannot reach.

- [x] **v0.0.130 — the wall of sound arrives, on the instrument that can hold it** — the filed doubling defect turned out not to need a ruling. The `!mono` guard was not the bug: melody ships MONO, and mono means one sounding note per track, so doubling the tune was *impossible* rather than blocked — the feature had been shipped in a place it could never happen. His brief is "doubling in MANY", which is instruments and not the tune, so it moved to the polyphonic ARP, where a doubled octave genuinely sounds beside its own note (the pad and texture are already stacked washes). From complexity 0.85 every arp note gains its octave at half velocity, mirrored across the field; deterministic, no draw; it stops if the arp is set mono. There is no pinned-step exemption and the code says why: the arp's pitches are sequence-derived, which is exactly why v0.0.109 excluded it from per-step pins, so there is no stated note to preserve. One frozen config moves DELIBERATELY — bossa, whose compiled complexity sits above 0.85, i.e. the feature becoming audible — and the baseline is re-frozen at 0.0.129 in this same commit. A doubled TUNE would need a voice path mono does not cut, which is a voice-layer design change and is noted at the site rather than left as a dead guard. Three stale TODO entries closed with it. 246 engine checks; all 17 suites green.

- [x] **v0.0.129 — the drive sweep can tell a flake from a regression, and three copies became one** — `tests/sweep-drives.sh` runs every browser drive in one command and, when one fails, re-runs it ALONE: **RED** (failed alone too) fails the gate, **FLAKY** (green alone) is listed loudly and does not. That distinction is the whole point — a hand-run sweep produced seven failures out of twenty-eight and all seven passed solo, because the drives' waits are tuned for an idle Mac, and a sweep that cries wolf is a sweep that stops being run. A cooldown between drives removes most of the load; `SWEEP_RETRY=0` gives the strictest reading. Cleanup, all three from the audit: `mapRangeValue` was a second copy of `mapRV` with a looser object test (one helper now, aliased so every call site still reads at its own altitude); note names had TWO spellings on screen — a pinned step said "C#5" while the piano roll said "C♯5" for the same note (one typographic spelling now; typing still accepts either, and the scale-root list stays ASCII because those strings are engine values, not display text); and the custom-structure builder took its block labels from the ENGINE's `STRUCTURE_LABELS` instead of a page-side copy that could drift into offering a label the sanitiser drops. Docs: `TESTING.md`'s hand-run suite list — the one whose staleness let two suites rot — is replaced by the runner, with its per-suite counts removed because a stale count reads as a lost test.

- [x] **v0.0.128 — the audit's minors, the one security-flavoured finding, and every doc that was lying** — a crafted metre name could ride in through the sanitiser: `metrePulses('constructor')` returned a FUNCTION (a plain property read reaching an inherited key) and the sanitiser stored it as the piece's time signature, from a share link whose payload is attacker-authored JSON. Own-keys-only now, array-checked at both layers, pinned against six crafted names. The grid minors: a tie stored past the end of a SHORTER bar no longer overruns its row (spans are bounded by the visible metre, not the fixed twenty-slot lane), and focus can no longer land on an absorbed cell (the old two-walk search left slot 0 exactly where it started — the keyboard was editing an invisible step); both pinned by switching 4/4 → 3/4 with a tie near the old end. A note still HELD when the transport stops keeps its end time, so a take captured across a stop is no longer quantised with no length. The favourites and rules popovers stop being open at once (they share the genre row as an anchor, and "inside my anchor" was counting a press inside the *other* panel as inside me). A held Shift+arrow on a single-value dial says its refusal ONCE instead of at the key-repeat rate. And the genre kit's fill weight is normalised by groove count, so the documented visit rate — 20% at 0.75, 50% at full — now holds for the three-groove genres too instead of a third of it (pinned across three genres). **Docs:** README told contributors to install Node 20, which cannot build the project at all, and omitted the REQUIRED `artwork` frontmatter key; CLAUDE.md claimed this container cannot see either site (it can, through the Mac bridge — that is how the stale deploy was caught) and miscounted the layout sweep's viewports; the engine contract's bpm bounds, mode enum and metre enum contradicted the shipped sanitiser; and the dial plan and compose spec both still said "not built" after most of each had shipped. All 17 suites and eight drives green.

- [x] **v0.0.127 — slurs keep their breath, the Fold dial goes where it works, and nine dead genre rules come alive** — a legato takeover re-held only the main amp, so a slurred mono line abandoned every parallel layer with its own envelope: the flute's breath band went silent from note 2 and bass `breath` lost the very layer it is named for (melody and bass both ship mono+glide, so this was every slurred note of two shipped voices). Voices can now declare such a layer with `rig.holdAlso()` and a takeover carries it. The **Fold dial** was rendering on seven voices that publish no fold — wash, both call voices and all four kits — where it moved, wrote into the patch, and persisted into presets and share links while doing nothing; it is gated on the voice publishing a fold now. In the compiler, `substituteToken` matched `from` case-SENSITIVELY while this project's own vocabulary rule says case is colour and not an instruction: nine substitution rules across five genres could never fire, two of them prob-1 "this chord must never survive" rules (ambient's and new age's), and the leading-tone chord that survived made the engine drop whole harmony seeds — ambient's dropped-seed rate falls from 14.6% to 12.1%. **This DELIBERATELY moves one frozen config** — bossa, whose `V7 → V13` rule now fires on the `v7` its director wrote — so the baseline is re-frozen at 0.0.126 in this same commit. And `compileGenre` deep-copies a genre's patches instead of aliasing them, so a defiance dial can no longer rewrite the imported genre data for the rest of the session. Pinned: the legato law fails on the old code and passes here (286 voice checks); all 17 suites green.

- [x] **v0.0.126 — a patch RE-TUNES a voice instead of overwriting it** — the audit's flagship voices finding: a percussion voice publishes ONE filter, ONE ADSR and ONE shape but plays three kinds, each with its own authored character — so ANY patch (a single dial nudge, or Reset to default) swapped each kind's character for the kit-wide one. Measured: the shipped Soft kit's hi-hat — a 6.75 kHz highpass in front of a 9 kHz burst — went behind the mid kind's 1.39 kHz lowpass and vanished, about 32 dB down. Fixed at the principle: a patch's filter now RE-TUNES the layer's own filter (its type is kept, its cutoff moves by the ratio the dial moved from the voice's published cutoff), so a patch that leaves the dial alone sounds exactly like no patch at all; a layer that DECLARES its waveform keeps it while the shape dial is untouched (the hand drum's triangle ring, and sawbass's sine sub-octave — which was becoming a sawtooth and adding the exact mud its own comment promises to avoid); and a struck layer's ring is expressed against the voice's published decay rather than replaced by it. Two further variants for the remaining ADSR residual were built and REVERTED because each broke a law the suite already pins — that residual (a kit's shortest layers still ringing long under a patch) is filed with measured numbers for a per-kind ruling, since `perKind` already exists in the schema. Pinned by two new laws that fail on the old code and pass on this one: a kind's own filters survive its own defaults as a patch, and a declared waveform survives an untouched dial. 285/285 voices, all 17 suites, reference untouched.

- [x] **v0.0.125 — the gates stop lying, and three more clusters close** — the audit's test-honesty findings, fixed at the root: **`tests/all.mjs`** runs every Node suite and fails non-zero if any is red, discovering them rather than listing them, because a list in a doc cannot fail — and two suites were proof: `voices-smoke` (red ~30 versions) and **`power-smoke`, red since the Node 22 move and found only by running it** (Node 21+ ships a getter-only `navigator`, so its fake-navigator helper threw and six of twelve tests never ran; the suite also asserted Node had no `navigator` at all). The vacuous assertions are now real: `onset-render` MEASURED five click metrics and asserted none — it now enforces an absolute click floor and a share-of-own-peak ceiling on every onset, fast-line re-attack, cancel cut and slur handover (25 assertions; proven to bite by tightening the floor until nine fired); `loudness-drive`'s "nothing clips" could never see positive-side clipping (byte data tops out at +0.992) and now counts samples at either rail; engine-smoke's sparsity cap was the generator's own structural maximum (a real groove-density law replaces it) and its fill floor sat below what the code always produces. **The pad Breath dial has been dead since v0.0.23** — the page probed a per-track `padBreath` while the engine ships it global, so the dial never rendered and page-boot's assertion about it was dead code behind the same false gate; both now look where the param lives. Plus: the piano roll's lane set follows the engine again (`visualiser.js` never exported the `refreshTracks()` the page has always called); the power governor ignores implausible frame deltas, so a tab-hide no longer walks the quality tier down to eco; the consent card announces its real message instead of "[object Object]"; and in the grid, clicking into a pitch input no longer toggles the step off, a tie re-drag measures its step from a plain cell rather than the span-wide head, and ⌘/Ctrl chords stop editing the pattern on their way past. All 17 suites and eight drives green; reference untouched.

- [x] **v0.0.124 — grooves, share links, undo — and one test that was hiding a live bug** — the bass groove's identity key banded its complexity and density to the groove's own quarter resolution: a spread Energy or a ranged Density writes a new number every bar, so `round3` re-rolled the groove every bar, which is the random walk the key exists to prevent. The auto kit's pattern bank now empties when Energy leaves the band its patterns were drawn in — at high Repetition the bank was reused almost every bar, so the Energy dial was ignored for as long as the piece ran. Share bases stopped being a function of the receiver's session: `settingsFromGenre` gained an explicit no-keep-user mode, so a sender's custom tracks no longer vanish from a compact link and a receiver's own tracks are no longer smuggled into the sender's piece. `genreKitClean` (about 10 KB of kit JSON) left the snapshot, so it stops riding every link, undo entry and preset. The undo baseline is seeded at boot, so the first ⌘Z of a session works. **And the honest part:** de-vacuuming `submit-drive` (every diff assertion sat inside an `if (parsed)` that skipped silently) immediately caught a regression from this session's own hotfix — an Energy move re-shapes the kit, which was landing every kit lane in the diff and pushing submissions past the URL ceiling into the clipboard fallback. The kit is a function of (genre, seed, shaping), so the shaping is recorded in the ORIGIN (`kitAt`) and rebuilt from it, and the follow now writes the kit through the same normalisation a share base uses: the edited-submit body went 44 KB → 523 bytes, pinned. All gates and seven drives green; reference untouched.

- [x] **v0.0.123 — the dial cluster: six audit majors** — typed entry finally speaks the dial's own units: click-to-type read and wrote the knob's INTERNAL domain, so typing "80" into a bpm readout gave 220 bpm (the log position clamped to 1) — knobs now take a `parse` (format's inverse), wired on every log dial, both Tempo views (type bpm), all eighteen percent dials (type 62 for 62%) and the dB volume view (type 6 for 6 dB down), with the editor prefilling the number the readout shows. The percussion Pitch dial survives a sideways drag (its handler clamped the {min,max} object itself — NaN into the patch, pitch silently reverted; now mapped per end and span-safe on render too). A paused Complexity range-drag no longer teleports its held thumb (the debounced commit's display write-back is skipped while the pointer is down), and an Advanced Complexity edit updates the Energy dial, so the next Energy touch starts from the truth instead of jumping. The sequencer playhead times its sweep from the engine's own bpm×speed: the old maths clamped to a 40–120 range that predates 20–220 (desync above 120) and read settings.bpm raw, which for a spread tempo is an object whose clamp is NaN — every step timer fired at once. All gates and the four dial-facing drives green; reference untouched.

- [x] **v0.0.122 — the capture cluster: four audit majors** — an arp take now lands on the ARP's own grid (indexed by arp step at the current rate; quantising to sixteenths wrote slots the lane never plays — engine-smoke pins x--x--x- at 1/8); a stopped-engine take anchors at the ARMING moment again (stopCapture nulled `capture` before reading the grid, so the armedAt anchor was dead code and idle takes smeared from zero — pinned); Tap a rhythm re-points an already-armed capture onto the kit (it re-pointed only the keys, so every Space tap was silently discarded); and a due alarm now starts through an exact-pause hold (the engine reads as "running" while held, so the alarm nulled its target and started nothing). The raw-take beat conversion also stopped deriving seconds-per-beat from the slot size, which the arp grid would have mis-scaled. All gates green; reference untouched.

- [x] **v0.0.121 — the last critical: voices-smoke is green again, and it is a gate nobody can skip** — red since v0.0.88 and run by nothing, which is how ~30 versions shipped over a failing suite. Split honestly: SIX of the nine failures were the suite pinning sounds the owner deliberately retuned (the v0.0.80 wash, the v0.0.81/88 call — his ruling deleted the breath layer) — those pins moved to the approved shapes, with his words quoted at the noise assertion so a breath quietly returning fails by name. THREE were real: the loudness law statically summed every source a note ever starts, over-counting sequential chirps that never sound together — it now measures the maximum CONCURRENT sum, which is what loud means; the call's engineType still claimed "hybrid" with one engine left (now subtractive — vowel filtering, by the file's own law); and the Release dial was DEAD on the call — chirp rings ignored the patch's adsr entirely, the exact dead-control class — so the ring now scales with release, ratio-anchored to the default (an unpatched call is byte-identical; the frozen reference is untouched, 24/24). The gate list in CLAUDE.md now names voices-smoke and genre-smoke, so red-and-unrun cannot recur silently. 283/283.

- [x] **v0.0.120 — three of the audit's four criticals: nothing bakes, nothing is deleted, nothing is dropped** — the Zero buttons now build their snapshot from the PAGE's settings instead of round-tripping `engine.getParams()`, which was baking the Eco tier's half-levels and capped reverb tail into stored settings (halving again on every press) and collapsing every spread global dial to wherever its walk sat; Zero chords also finally works — it wrote `harmony.seed` as a bare string the sanitiser rejects whole, and writes the one-chord array now. The legacy v2 storage key is only deleted once its data has somewhere durable to live: the old unconditional delete destroyed a returning user's entire setup on any pre-consent visit. And `normaliseV6Tracks` stops fighting the engine: percussion keeps its density (the genre compiler's kit density finally reaches the engine — only the pad has none), and glide/swing/density spans survive a reload instead of being silently collapsed (all rangeable since v0.0.74; the repair still said "not rangeable"). Drive-pinned: stored level and reverb-ask byte-exact across a Zero press, the one-chord seed accepted at the engine. All gates green; reference untouched.

- [x] **v0.0.119 — hotfix: a fresh genre pick plays its kit as authored again** — the audit's execution proof (50k seeds) showed v0.0.118's shaped-pick change fed the kit ladder ABSOLUTE chord-colour complexity, which is the wrong measure of a kit's energy: synthwave's all-triad grammar compiles 0.275 forever, so both public genres' authored kits were softened and thinned on every fresh pick. The ladder is now anchored to the genre's OWN compiled Energy: at the anchor the kit is byte-identical to what the director wrote, the dial's travel below the anchor spends the entire softness ladder, and the travel above it spends the entire doubling/fill ladder — so the Techno-at-0% contradiction stays closed without the authored sound ever being mis-shaped at pick. `kitFloorDouble` also gained the owner's own precondition, verbatim — "possibly 8 or 16 to the floor IF you started with 4 to the floor" — so only an even-pulse lane doubles and lofi's boom-bap keeps its shape at any Energy (pinned in genre-smoke). Share links minted during the experiment window still rebuild byte-faithfully through a kept legacy path. All gates and the kit drive green; reference untouched.

- [x] **v0.0.118 — the overnight max review: twelve fixes, and the deploy's real culprit** — a full-depth adversarial review of the session's seventeen versions found fifteen defects and fixed twelve. The headline: a stale `.node-version` file still pinned Node 20 — the file most build tooling reads FIRST, which is very likely why Cloudflare kept failing while `.nvmrc` said 22. The rest: custom metres now draw the grid the engine plays (the slot table missed every v0.0.98 metre); a chained melody no longer collapses to bar 1 under Blank slate's randomness-0 (freeze guard ordered before the chain branch) and no longer walks on during repeat-bracket replay; grid key shortcuts beat armed play-along keys (a hand edit can no longer be recorded over); closing the keys panel stands tap mode down (Space stops being a drum app-wide); the Zero buttons clear every sequencer tab and stop dropping origin/genreRules/genreKitClean through their round-trip; captures no longer inherit the previous take's pitch pins (`explicitStep` spells `midi: null`) and typed melodies no longer inherit stale tie/gate/group; `sequencerAdvance` is stated on every params push so a preset load cannot leave the engine chained while the UI says Shuffle; genre picks and rules-applies compile the kit SHAPED to the genre's own Energy (the kit and the dial now agree from the first bar — the owner's original Techno complaint class); the record button works off-iOS again (a silent output tap, the latency path untouched); and Simple's Tempo collapses an Advanced-made span to one steady value on first touch instead of secretly editing it. Three findings are filed, not fixed: the octave-doubling mono guard (needs a ruling), the chain first-bar offset (scheduler design), and a confirmed remainder list in the TODO. All gates and all ten affected browser drives green; the frozen reference is untouched.

## 2026-07-30

- [x] **v0.0.117 — the grid tells the truth about the chain** — v0.0.116's chain mode gets its face: a **Shuffle | In order** toggle sits beside the sequencer tabs (probed like every engine seam — no toggle on a build whose sanitiser drops the mode), and while a list is chained the Weight box HIDES, because the engine ignores a chained list's weights and a control that does nothing on screen is the exact failure the owner keeps catching. Typing a multi-bar melody now leaves a grid that says what will happen: tabs 1 and 2, In order selected, no weight to misread. typed-melody-drive grows to 15 checks (toggle state and hidden weight asserted in the DOM the user sees). All gates green.

- [x] **v0.0.116 — sequences that play IN ORDER: chain mode, and typed melodies grow past the bar** — the structural gap under MIDI import and long typed phrases, closed: a track's sequencer list can now advance as a CHAIN (`sequencerAdvance: 'chain'`) — the tabs play in order and wrap, a composed sequence rather than the Markov shuffle, drawing nothing so a chained track never moves anyone else's stream; 'weights' stays the default and nothing existing changes. Type a melody uses it at once: a phrase longer than a bar writes more bars — one grid tab each, chained — with holds re-stated across barlines (a lane cannot tie into the next sequencer), an eight-bar cap that says so, and the writer reporting "6 notes across 2 bars (played in order)". Pinned in engine-smoke (243: the 60/62/64 three-tab chain cycles exactly in order for the whole run) and typed-melody-drive (13: bar 2 starts on the fifth note, chain mode reached the engine). All gates green; references untouched.

- [x] **v0.0.115 — Energy stage 2d: the wall of sound — the tune doubles at the top** — "something filling every pitch section, with doubling in many for a wall of sound": from 0.85 every generated tune note gains its octave, at just over half the velocity and mirrored across the stereo field, so the top of the dial widens as well as thickens. Deterministic and draw-free — no stream moves anywhere, and the frozen reference is untouched (24/24). Two principled exemptions: a MONO line stays single (its own overlap-trim would eat the double), and a PINNED note stays solo — the user named exactly that note, and doubling it would be adding one they never wrote. Pinned in engine-smoke (242 checks): at 0.9 each tune note is two notes at one moment, +12 apart, the octave under the stated note; at 0.7 it is one; a pinned step is one even at 0.9. Of his max-Energy brief only filter openness now remains, deferred to the modulation graph with its reason on record.

- [x] **v0.0.114 — Energy stage 2c: the bass rides the doubled floor** — "Bass would likely follow kick, with incidentals": above 0.75 the groove takes the kick's OFF-pulse onsets too — a doubled floor puts kicks between the pulses, and at the top the rhythm section reads as one instrument — each taken with a chance rising from nothing at 0.75 to 0.9 at full, as root or octave incidentals a shade shorter than the pulses. Drawn only at or above 0.75, so every stream below keeps its exact old draws (pinned in engine-smoke as a DIFFERENCE measure: a doubled floor adds no off-pulse follow at 0.7, and more than two extra notes a bar at full). One frozen config moved — acid-jazz, whose compiled complexity sits at the top — DELIBERATELY, and the baseline re-froze at 0.0.114 in this same commit. The remaining top-half axis, filter openness, is deferred to the modulation-graph build: an Energy-scaled cutoff without patch sockets would be a hidden multiplier on a dial the user can see, the exact lie this repo tests against.

- [x] **v0.0.113 — Energy stage 2b: the genre kit gets its fills, as a tab you can open** — the second axis of the top half, landed on the honest side again: from 0.75 a compiled genre kit gains ONE MORE sequencer — the first groove with its last beat cleared for a crescendo run on the mid lane — and the Markov weights the grid already shows decide how often it is visited: about one bar in five at 0.75, one in two at full, and the fill always hands straight back to a main pattern. It is a TAB in the step grid — the user can open it, edit it, or zero its weight to silence it — never an engine improvising over user data. Deterministic, draw-free, riding the same-seed recompile; the identity window [0.5, 0.75) compiles exactly as authored, pinned in genre-smoke alongside the crescendo, the hand-back and the rising visit rate. All gates green; references untouched.

- [x] **v0.0.112 — Energy stage 2a: the top of the dial opens — the floor doubles and the fills arrive** — his brief for maximum Energy, verbatim: "drum fills all over the place, possibly 8 or 16 to the floor if you started with 4 to the floor." Genre kits: from 0.75 the LOW lane doubles (a hit midway between each written pair, sitting a shade under the authored accents so the accents stay the accents) and from 0.92 every slot of the bar kicks — deterministic, in the compiled lane, riding the v0.0.106 same-seed recompile so it is VISIBLE in the grid and stands down after a hand edit. Auto kits: from 0.75 a crescendo fill runs into the barline, more often the higher the dial (measured: 0 fill-bars at 0.7, all-over-the-place at 1). The identity window [0.5, 0.75) is byte-exact on both paths — pinned seed-for-seed in engine-smoke and compile-for-compile in genre-smoke — and the one sparsity cap that predated his ruling was retired with a note saying whose ruling replaced it. References untouched (24/24: the frozen configs never run auto percussion at the top). kit-softness-drive grows the top-half check: Energy full up, the Techno floor doubles on screen.

- [x] **v0.0.111 — pitch is editable in the grid: the readout gap closes** — v0.0.75 drew pitch and said so ("a READOUT, not editable"); the v0.0.109 pin makes the edit real and this build puts it under the fingers. On a melody or bass step: **P** opens an in-place box — type C#5 (or a MIDI number), Enter, and that step plays exactly C#5, every bar; an empty box clears the pin (as a null, because the engine's merge law ignores deletions). **Alt+drag** a cell up or down nudges the pin by semitones for the pointer path, so keyboard-only and mouse-only both work. The cell wears a small note-name tag while pinned and the screen reader hears "pinned to C#5 — plays exactly that note"; the legend documents both routes. Offered only where the engine honours the pin — melody, bass and user melodic lanes; the arp is excluded rather than given a dial the engine ignores. The capability is probed like every other engine seam (a build whose sanitiser drops `midi` never shows the affordance). `tests/grid-pitch-drive.mjs` (8 checks) asserts the ENGINE: the typed pin lands, the drag lands, the clear lands. All gates green; reference untouched.

- [x] **v0.0.110 — Type a melody: the second compose method lands** — a text box in the Create door's compose row: write note names (C4 E♭4 G3 — one per beat, a dash holds the note, a dot rests) and Write it puts them onto the melody grid as PINNED steps through the v0.0.109 schema, so each plays exactly the note named — no re-pitch, ever. Holds become one tied note across their beats (the v0.0.92 merged-box rendering shows them as one wide box); the lane goes manual; a silent melody track switches on; the writer says what it wrote and names anything it dropped, including tokens past the bar (a bar holds as many tokens as it has beats). Garbage is refused in place with the reason and the engine keeps what it had. One bar per write for now — the honest cap until linear multi-bar sequences have an answer (the Markov tabs shuffle, they do not sequence). `tests/typed-melody-drive.mjs` asserts the ENGINE's stored steps: pins at their beats, the tied hold, nothing past the phrase. All gates green; reference untouched.

- [x] **v0.0.109 — the step schema carries PITCH: the engine half of typed melody, editable grid pitch and MIDI import** — the one schema gap three owner asks funnel through, closed at the engine. A step may now carry an optional `midi` pin (0–127): a pinned step plays exactly the note it names — no chord re-pitch, no register fold, no cadence rewrite, because folding a stated pitch would be the readout lying — while a step without one keeps today's harmonic logic untouched (the frozen reference is byte-identical, 24/24). The pin follows the step-field merge law the tie/group fix taught this morning: absent inherits the stored value, null clears it, garbage clears it like a garbage gate. Honoured by the melody, bass and user-track manual planners (arp deferred: its pitches are sequence-derived and deserve their own ruling); the page's compact persistence round-trips the pin. Proven in engine-smoke (241 checks) at the REALISED note: a pinned 97 — outside both C major and the melody band — sounds as 97, every time, on melody and bass alike, and an unpinned grid never lands on it. UI writers (grid pitch editing, typed melody, MIDI import) are the next, now-unblocked builds.

- [x] **v0.0.108 — Energy 1c completes the low half: the AUTO kit arrives gently too** — the same ladder the genre kits got, now for the four grammar-path genres whose percussion composes itself: below the midpoint every auto hit's velocity scales down, the kick-anchor YIELDS (a gentle bar no longer opens on the kick by default), and a soft ride tick becomes the low-Energy opening instead — his words built literally: "a ride first, then soft kicks." The new draws only exist below the midpoint, so streams at 0.5 and up are byte-identical to the previous build — pinned in engine-smoke by comparing the shaped and unshaped calls seed-for-seed (240 checks now) — and the frozen reference is untouched (24/24; the low half of the ladder never fires in those configs' quiet sections because the track ladder holds percussion out there). With 1a (kick-lock), 1b (articulation fade) and both halves of 1c, the LOW half of the Energy redesign is complete; the top half (fills, bass-follows-kick, filter openness, doubling) is next.

- [x] **v0.0.107 — the raw take survives, and captures finally reach the page** — his ask, verbatim: "Keep the raw tap data so the user can re-quantise if we're doing a bad job." Every capture (tap-a-rhythm and play-along alike) now keeps its take RAW, in beat-domain — quantisation-independent — and a "Re-fit the last take" button in the Create door lands the same performance on whatever grid is current: change the time signature or the tempo, press it, and the note played past the old barline lands where it was really played instead of wrapping (engine-smoke pins exactly that, 4/4 wrap → 5/4 slot 17). Undo covers a re-fit like any capture write. **The bigger find was the missing half of every capture since v0.0.82:** the engine had the take and the page never learned — the grid drew the pre-take lane, the next step click silently wiped the take, and a reload lost it outright, unnoticed because every earlier check asserted at the engine alone. All three capture paths (take, undo, re-fit) now adopt the engine's sequencers back into settings, refresh an open editor and persist; `create-drive` asserts the take on BOTH sides of the seam (engine hits == persisted hits). All gates green; audio-reference untouched.

- [x] **v0.0.106 — Energy stage 1c: the genre's kit softens below the midpoint, in the grid where you can see it** — the ladder his words asked for ("a kit can arrive very gently"), landed on the honest side of the boundary: never a hidden note-on multiplier (a grid showing one velocity while another plays is the exact lie this project tests against), but a RECOMPILE of the genre's kit at the dial's value, same seed, written into the grid. Below the midpoint every lane's velocity band scales down (soft kicks are real kicks) and the high then mid lanes thin on an even deterministic stride — the LOW lane never loses a hit, because four-to-the-floor is the genre's identity and his steer said exactly that. Genre-appropriate by construction: Techno thins and softens, and never grows a brush. The dial only re-shapes a kit that is provably still the genre's own — the first hand edit to the grid breaks the provenance string and the follow stands down for good, so the user's data always wins. Reversible while clean: Energy back up restores the full kit byte-for-byte. No rng is consumed and nothing above the midpoint changes; `audio-reference` is untouched (24/24 — reference configs never move the dial). Pinned three ways: the compiler contract in `genre-smoke` (30 checks), the live loop in `tests/kit-softness-drive.mjs` (11 checks: thins, softens, restores, stands down after a hand edit), and every prior gate green. Still open in the low half: the AUTO kit's ladder for grammar-path genres, and ride-first instrumentation.

- [x] **v0.0.105 — Energy stage 1b: the bass's off-pulse articulation fades below the midpoint and is gone at 0%** — his steer, verbatim: "0% would remove the acid articulation if 4 to the floor is genre-defining." The groove builder always hung at least ONE syncopation cell off a pulse — fifth-and-octave off-beats, the acid figure on a Techno bass — unconditionally, at any Energy, which made the line's articulation the second thing (after 1a's kick-lock) the dial could not thin. The first cell now fades with complexity below the midpoint and is gone at the bottom, while the accented anchor spine keeps the genre's identity; from 0.5 up no extra rng draw is made, and byte-identity with the previous build is PROVEN against the pre-change engine and pinned as a golden stream in engine-smoke (238 checks now). Measured at the groove: off-pulse notes per bar 1.50 → 0.00 at complexity 0, 0.11 at 0.04 (where Energy 0 lands), unchanged from the midpoint up. The seven sub-midpoint reference configs move DELIBERATELY — thinning quiet pieces' articulation is the point — and the baseline is re-frozen at 0.0.105 in this same commit. Next in the ladder: the genre-appropriate kit-softness pass ("I don't imagine Techno ever uses brushes and rim shots").

- [x] **v0.0.104 — the genre's rules are on screen, and editing them re-draws the piece: the everything-editable build opens at the layer he chose** — his steer was option (a), the GENRE layer: "show the grammar of the current genre as editable text/controls, so changing 'x---x---x---' changes every bar drawn from it." A Rules button now sits beside the genre picker: the current genre's chord grammar (roman-numeral lines), its kit patterns and its syncopation cells, each an editable text box; Apply deep-copies the genre, overlays the edits and **recompiles at the same seed** — the compiler's fixed draw order spends the bpm/mode/metre/swing/structure dice before the grammar does, so only what you change, changes (drive-proven: bpm and time signature survive the edit byte-for-byte). Honesty travels with it: the picker entry and the button both read "edited rules", garbage is refused in place with the reason, an emptied box falls back to the genre's own rules, and Back to the genre's rules recompiles clean at the same seed. Two compiler truths shaped the panel: kit-path genres write their kit from `fallbackLists.grooves` (three lanes per pattern — so that is what the box edits there, not the anchor guidance the kit never reads), and the progression pool weighs `fallbackLists.progressions` double, so a chord-grammar edit empties that pool or the old chords keep being drawn. `tests/genre-rules-drive.mjs` (16 checks) pins the whole loop at the engine seam: the one-kick kit lands lane-exact, a one-chord grammar seeds a one-degree loop, refusal keeps the engine untouched, reset restores the genre's own kit. All gates green; audio-reference untouched.

- [x] **v0.0.103 — probability groups paint into a selected group, gaps and all — and unties finally reach the engine** — his ruling landed both halves of the groups question: the chain semantics stay exactly as shipped (each member rolls in turn; a member that stays silent silences the rest of its group for the bar — his own description, option a), and the JOIN rule is rebuilt, because "the probability group is not always contiguous … a gap is NOT a probability group boundary." Press any dot to start a group or select an existing one — its dots ring — then press any other dots, gaps and all, to paint them in; press a selected member again to remove it; Esc drops the selection; G does the same from the keyboard, per lane (the engine rolls each lane's chain on its own, so groups do not cross lanes). **The build surfaced a real stored-state defect**: the engine's step sanitiser MERGES against the stored step, so the page's `delete step.tie` / `delete step.group` never cleared anything engine-side — the boxes split while the engine played on tied. Proven live by a new engine-seam check (red on the old bundle), fixed by sending an explicit null everywhere a tie or group is cleared. `tests/prob-group-drive.mjs` (16 checks, red on the old bundle) pins the new model; `tests/tie-merge-drive.mjs` now asserts the untie at the engine. All gates green; audio-reference untouched.

- [x] **v0.0.102 — Simple's Tempo is the one dial that stays single** — his ruling, verbatim: "it's the one dial that shouldn't be variable." A sideways drag on the Simple tab's Tempo no longer opens a span — instead the message he asked for appears under the dials: drag up or down for speed; a randomly varying tempo is rarely a good thing; the Advanced tab's Tempo dial keeps the sideways drag for the piece that wants one. The refusal is never silent: `knob.js` gains `onRangeRefused`, fired once per gesture for pointer and keyboard alike, because a dial that silently ignores the spread gesture teaches that the gesture does nothing anywhere (the v0.0.57 worry, resolved the other way for this one dial by his ruling). A span created in Advanced still displays on the Simple view — refusing to draw state that exists would be the readout lying. Drive-proven at the engine seam both ways: the refusal sends no `spans.bpm`, the Advanced drag stores one; the three new checks fail on the pre-change bundle. All gates green; audio-reference untouched.

- [x] **The "intermittent shape-span loss" is closed — no engine defect existed; the drive's genre pin was dead code** — the session's opener, hunted with an instrumented twin of `tests/spread-all-drive.mjs` that logged every `setParams` payload. The filed theory ("the shape dial draws a span the engine never stores — the commit path, racing something") was disproven by the first captured failure: in failing runs **OSC 1 was never dragged at all**. The pin matched option value `synthwave`, but the picker's options are prefixed (`g:synthwave`) — `.find()` matched nothing, so every run played on the random draw, and roughly one draw in three deals the pad a voice with no shape dials (glass), which the named-dial loop then skipped **silently**. The engine seam is clean: every span the page sent, the engine stored, in every logged run. The drive now pins by the real option value, VERIFIES the pin at the engine (`tracks.pad.voice === 'polysaw'`, loud failure naming the picker's actual options if it does not take), and treats a missing named dial as a named failure rather than a skip. 5/5 consecutive green after; the same pin mistake had now been made twice ('ambient', then bare 'synthwave'), so the drive carries the history in a comment.

- [x] **v0.0.101 — the dev deploy is unstuck: Cloudflare could not build Astro 7** — his morning report was the symptom: mcdev hard-refreshed and still serving v0.0.85, which is exactly the last commit before the Astro 7 migration. Reproduced in the container: on Node 20, `astro build` refuses outright ("Node.js v20.20.2 is not supported by Astro!"), and a failed Cloudflare build leaves the previous deploy serving — silently. The v0.0.86 `.nvmrc` pinned the exact patch `22.23.2`, the newest 22.x in existence that day, which a build image's version cache can easily not carry yet; it now says just `22`, the loosest spec that satisfies Astro's `>=22.12` floor by resolving to whatever current 22.x the builder has. The version bump itself is the test: mcdev showing v0.0.101 proves the pipeline. Fallback if it stays stale (dashboard-side, owner only): set `NODE_VERSION=22` on the Worker's build settings and read the failed build log. Also fixed en route: `tests/page-boot.mjs` assigned jsdom globals by `=`, which Node 21+ rejects for its own getter-only `navigator` — the gate only ever passed under Node 20; it now defines the property and passes on the same Node 22 the builds use.

- [x] **v0.0.100 — a wandering voice says so** — the close of his "I never selected Call myself". Both routes were real: one factory preset (Dawn Song) chooses Call by design, and the deliberate anti-monotony voice wander on pad and texture can land on any voice in the bank — correct behaviour, kept. The genuine defect was display: the track row's live option labelled a wandered voice exactly like a chosen one, which is precisely how "Texture: Call" read as a setting he never made. It now reads **"Call · wandering"**, with the tooltip carrying the escape hatch (your own choice is unchanged; picking any voice ends the wander at once). Engine-smoke pins the whole seam while the engine RUNS — `getResolved` reports the wander, `getParams` holds the user's choice, the wander never leaves the track's own bank, and an explicit pick ends it immediately (the first draft asserted on a stopped engine, which has no wander to show, and was caught by its own failure). All gates green; reference unchanged.

- [x] **v0.0.99 — Energy redesign, stage 1a: low Energy finally thins the bass** — his Techno-at-0% report, run to ground: the bass takes any pulse the kick lands on with probability **1**, at any Energy — the kick-lock that makes a rhythm section read as one instrument also made the bass the one line Energy could not thin, and a four-on-the-floor kit locks every pulse. Below the dial's midpoint the lock now loosens with complexity (the accented anchor itself never goes); from 0.5 up the chance is exactly the old 1 with an unchanged rng draw count, so every stream at or above the midpoint is byte-identical. Measured on the live engine: **Techno bass at Energy 0 drops 4.4 → 2.98 notes per bar**, and 0.55 is untouched. This DELIBERATELY moves the five frozen reference configs whose complexity sits below the midpoint — thinning quiet pieces' bass at low Energy is the point — so `tests/audio-reference.mjs --update` re-freezes the baseline at 0.0.98 in this same commit. Stages 1b onward (the kit-softness ladder, fills, coupling, filter, doubling) are staged in the TODO as the next session's opener.

- [x] **v0.0.98 — custom time signatures** — the fixed five-metre list gains **Custom…**: an N/D pair beside the select accepting any N/4 up to 5 and any N/8 up to 10, which is the step grid's own twenty-step bound stated as a rule rather than a surprise. Compound /8 bars group in threes when they divide that way (6/8, 9/8), otherwise twos with the odd three last — exactly how the shipped 7/8 has always grouped, generalised. The engine is the judge: `metrePulses()` grids named and custom metres alike, the sanitiser accepts precisely what can be gridded and keeps the stored metre otherwise, and the commit path re-reads the engine so the inputs always show what actually stuck. One landmine stepped on and defused: an early `describe()` call re-created the recorded blank-page TDZ crash (6105e0d) and page-boot caught it before it could ship. Behaviour pinned in engine-smoke; every gate green, `audio-reference` unchanged.

- [x] **v0.0.97 — sections can be verses, and they can carry their own names** — the custom structure builder gains **V and V1–V4** labels alongside A–D, and every block takes an optional **title** (up to 24 characters) so B can double as "bridge" and C as "chorus". The title is additive on the wire — absent when empty, so every structure stored before tonight round-trips byte-identically — travels with the engine's section event, and the piano roll's section strip draws it in place of "Section B" when one exists. Sanitiser behaviour pinned in engine-smoke (trim, cap, drop-when-empty, V-family legality); visualiser 45/45; layout sweep clean; `audio-reference` unchanged.

- [x] **v0.0.96 — Processor becomes one block, and the guided tour is back inside its own spec** — the no-orphan rule's worst offender reversed: the Processor label lived in the transport header, its dial three containers later, its readout inside the genre row; all three are one labelled column now, and the no-JS removal path is one removal instead of three. Running `tutorial-smoke` (listed as a gate, quietly unrun for a stretch) then surfaced real drift: the tour had grown to fifteen steps and two steps had swollen past tour length with the night's additions. Play and Pause merged into one transport step, the dials and Create steps were cut back to tour size — their detail lives in the panel ⓘs, where he ruled instructions belong — and the coverage list learned the keyboard's real name, Musical typing. `tutorial-smoke` 8 checks + 8/8 mutations at 14 steps; every other gate green.

- [x] **v0.0.95 — the preset-slug constraint is self-enforcing, and the drive suite is documented** — two hygiene closes. The advisory review's one live caveat (a preset slug is interpolated into an inline script, safe only while slugs are machine-made) is now a BUILD-time assert: any slug beyond lowercase/digits/hyphens fails `astro build` with a message naming why, so a future submission-sourced preset can never quietly carry markup to the page. And `docs/TESTING.md` gains the browser-drive inventory — twenty-one drives and three offline render harnesses, what each holds the line on — which existed only as filenames until now.

- [x] **v0.0.94 — mass edit: Fill, Every 2nd, Clear** — the first slice of his 89's mass-edit ask ("fill a whole row, or every second note"). Three actions above every step grid, acting on the focused lane (which is the only lane everywhere but the kit, where it is the lane last touched). They act on the VISIBLE bar — the stored lane is longer than the metre shows, and the first cut walked off the rendered cells and died before committing, which the engine-seam drive caught (the settings changed, the engine never heard). Asserted at the engine's stored lane: all on, alternating, all off. Setting a probability across a selection is the remaining half and lands with the resolution control. All gates green.

- [x] **v0.0.93 — notes on top, chance and grouping at the bottom** — his reading order, asked for twice: the probability-group dots move from above the cells to BELOW them, beside the probability bars, so a lane reads notes-and-pitch at the top and everything about chance together underneath. The grid legend and keyboard help follow in the same commit, and the drive asserts the ordering by geometry.

- [x] **v0.0.92 — a tie is one wide box** — his 105: *"it should merge two boxes into one double width box, three into triple… The current implementation is very hard to see."* A tied run now renders as ONE box spanning its steps — in all three rows at once (the cell, its group dot and its probability bar), because the engine genuinely drops an absorbed step from the bar, so showing it a live-looking bar of its own was a lie. A thin warm underline marks tied-versus-just-long. Keyboard focus walks over absorbed steps in the direction of travel instead of landing on nothing. `tests/tie-merge-drive.mjs` proves it by geometry — head box ≥1.85× a plain cell after T, two boxes again after untying — because "double width" is a number, not an impression. All suites, sweeps and the frozen reference green.

- [x] **v0.0.91 — the mix goes straight to the speakers everywhere but iOS** — his latency report ("intolerable, for a percussionist"), traced to a real buffering stage rather than guessed at. Since the mute-switch work, EVERY platform's mix routed through a MediaStream → `<audio>` element — but only iOS needs that (its hardware mute silences a bare AudioContext), and the media pipeline adds its own buffering on top of the context's on machines that never asked for it. The element sink is now gated to iOS (including iPadOS-as-Macintosh); everywhere else the limiter feeds the destination directly. New `getOutputInfo()` on the engine reports the route and the context's own figures; `tests/latency-drive.mjs` asserts the desktop route is DIRECT and measured the honest floor on the Mac test machine: **5.3 ms base + 40 ms output at 48 kHz** — the browser's floor, which no scheduling change can go below (and which Bluetooth output would add 100–300 ms on top of, wherever it is in use). The engine-smoke iOS-route test now dresses as iOS to exercise the element path. 237/237, `audio-reference` unchanged.

- [x] **v0.0.90 — guided start is a visible option, and silence after Blank slate is a tested promise** — two halves of his 105/103 follow-ups. **Help me start** now sits first among the create options: it points at the genre seed (which names where that genre's composers usually begin) and the three dive-in doors. And his "press blank slate then play, I get a complex bassline I didn't programme" — reproduced against the old stub's behaviour, unreproducible against the real blank: the drive now presses Blank slate, presses Play, listens for three and a half seconds at the engine's own note stream and asserts **zero notes arrive**, locked in as a permanent regression check.

- [x] **v0.0.89 — Create rebuilt to his correction: an icon, Zero buttons, and a blank slate that is actually blank** — his 103, point by point. The wide orange button that "forced everything into two lines" is gone: Create is back in the icon-row slot, **icon-only, orange, CREATE in the tooltip**, and the drive now gates on the transport buttons sharing one line. The duplicate Create title is gone. The create options are his: **[Blank slate] [Zero voices] [Zero chords] [Zero notes] [Zero rhythms] [Zero FX]** — the Zero buttons strip one layer at a time so ANY preset can be the starting material. **Blank slate is real now**: it was a stub that only switched track states off, which is why he still had "massive reverb and delay" — it now loads the same full blank snapshot as the Advanced button, and that snapshot itself learned to zero the FX (room to its smallest, every default voice's sends to zero). The instruction prose is out of the panel and into two ⓘs — including that Blank slate or a zero-AI preset is what makes a piece eligible for a zero-AI badge — and the keyboard section is named what the industry names it: **Musical typing**, its toggle labelled Keys. A MIDI keyboard needs no setup (the ⓘ says so). `tests/create-drive.mjs` grew to 21 checks, all at the engine seam where it matters. All sweeps and suites green; `audio-reference` unchanged.

- [x] **v0.0.88 — the noise leaves the Call voice, deleted not rebalanced** — his ruling in full: *"The whistle is nice, I don't know why you need the static pink noise in there at all… it's just not a natural part of a bird call, or a noise we hear in reality with that duration and spacing, other than IMHO someone sawing — please just delete the noise from the Call instrument and be done with it."* Done: the breath layer (a pink-noise band gated with every chirp) is removed from `callVoice`, for both the melody and texture readings. The bare whistle then measured five times quieter than the two-layer voice — the noise had been carrying most of the level — so the tone gets a ×3 makeup that lands the voice near its previous place in the mix (0.016 peak against 0.027 before, 0.005 bare). `tests/call-breath-render.mjs` now asserts the whistle stands ALONE (a noise layer sneaking back would drag the top-band share below its floor), still rises, and still sits in the texture register. Engine 237/237, `audio-reference` 24/24 unchanged — no frozen config reaches the call defaults.

- [x] **v0.0.87 — Tap a rhythm: the Space bar is a drum** — the create door's next piece (his items 96 and 89, "space bar or button — love it"). One press of Tap a rhythm points the play-along keys at the kit, arms them AND arms Capture, so every Space tap sounds the kit live and lands in the drum grid at its current resolution; press again to stop, and Capture reports what it wrote. Space is only a drum while tap mode is on — everywhere else it stays what it was, including toggling grid steps under focus. Drive-proven at the engine seam: taps arrive on the percussion track, arming and disarming both work, and the guided tour sentence rides in the same commit. All sweeps and suites green; `audio-reference` untouched.

- [x] **v0.0.86 — Astro 5 → 7, and the security page's root fix is in: zero vulnerabilities** — his ruling (93: a) on the Dependabot investigation. `astro@7.1.6` builds this site with **no source changes at all** — the fifteen pages compile as they were — and an npm override lifts astro's bundled `sharp` to 0.35.3, which closes the last advisory: `npm audit` reads **0 vulnerabilities** across all 373 packages, against 3-high/4-moderate/3-low before. Astro 7 requires Node ≥ 22.12: the container builds with a checksummed Node 22.23.2 toolchain, and `.nvmrc` rides in the repo so Cloudflare Pages builds with the same — if mcdev ever shows a stale version after this, the Pages project needs `NODE_VERSION=22` set in its dashboard. Proven the expensive way, because a build-system major deserves it: the full browser-drive suite — all 21 drives — plus the layout sweep at three viewports, engine 237/237, and `tests/audio-reference.mjs` **24/24 byte-unchanged**. The ten GitHub alerts close on their own once main carries this lockfile (main moves only on the owner's go, as ever).

- [x] **v0.0.85 — Create is the app's second main door** — his item 96, built to his structure. The piano icon is gone from the icon row; in its place an **orange Create button sits beside Play, at Play's own height**, opening a panel with his three doors: **Create** — a Blank slate button that switches every instrument off at the engine so you build from silence, plus a genre seed that writes a genre's instrument voices into the engine without playing a note, and a guided line that names what that genre's composers usually start with (techno starts with the drums, ambient with the pad, minimalism with one repeating cell…); **Compose** — typed chords are live today (the roman-numeral editor, one press to hear a chord), with typed melody, words and MIDI import named as landing here; **Play along** — the whole existing keyboard, unchanged. Popovers also learned to pick the side with room on open, since the new button moved every anchor. Drive-proven at the engine seam (`tests/create-drive.mjs`, 11 checks: states really go off, Synthwave's exact voices really arrive, states stay off through seeding); page-boot's location assertion updated for the deliberate move; all sweeps and suites green, `audio-reference` untouched.

- [x] **v0.0.84 — popovers stay on screen, and the play-along keys stay armed** — both from his item 95. The timer and play-along popovers anchored `left: 0` and grew rightward from icons at the page's right edge, straight off the screen; they right-align and grow leftward now, and below 560px — where no anchored placement can fit a 240px-plus panel beside a right-edge icon — the popover takes the viewport instead (fixed, inset 16px). Proven by box geometry at 1280px and 390px in `tests/popover-drive.mjs`, because on-screen and one pixel off-screen are the same screenshot. And closing the play-along popover no longer switches the keys off: the v0.0.40 disarm-on-close is deleted, with its worry (a live instrument with nothing on screen saying so) answered by the keyboard icon staying lit while the keys are armed. The play-along drive now also proves a key still reaches the engine after the popover shuts. Engine 237/237, `audio-reference` 24/24 unchanged, layout sweep clean.

- [x] **v0.0.83 — Simple gets Tempo back** — his item 90, and he was right in a way the code had hidden: when v0.0.71 separated Energy from tempo, the Simple tab was left with **no tempo control at all** — Energy stopped writing bpm and nothing replaced it. Simple now has four dials: Tempo, Energy, Change, Volume. The new dial is a second view of the same bpm as Advanced's — the two mirror — but it lands its change on the next **beat** (a beginner needs to hear the thing they just dragged do something) where Advanced lands on the barline, which is v0.0.57's A/B preserved. The Advanced ⓘ that claimed "Tempo, Complexity and Volume are the same three values as the Simple tab" was already false and is corrected. `tests/simple-tempo-drive.mjs` asserts the ENGINE's bpm moves from a drag on each view and that each lands where it should; layout sweep clean at all three viewports; engine 237/237; `audio-reference` 24/24 unchanged.

## 2026-07-29

- [x] **The click-on-onset hunt: every voice-layer interaction now measures clean** — `tests/onset-render.mjs` gained the three cases no render had exercised, built exactly as the engine builds them: a real three-note `legatoFrom` slur chain (first coverage ever of the shipped `takeOver` path — fingered, sub and sawbass all take the slur, handover steps at 0.09–0.62 of the body's own; upright refuses by design), an engine-style `handle.cancel(at)` mid-note (no step — a plausible stale-value hypothesis measured and killed), and a bass+pad+kit same-instant sum (ratio 1.0). With isolated onsets, fast lines, slurs, cancels and summing all clean, the owner's click cannot live at the voice layer; the TODO item now names the live-context candidates (voice stealing, governor transitions, the master compressor) and the next harness (full-engine offline render), so nobody re-tunes a healthy voice on this complaint.

- [x] **The compose/create mode has a spec** — `docs/compose-mode-spec.md`, drafted to the owner's two briefs: create starts from a blank slate, any track can be the front door (chords, melody, beat, bass, arp, texture), guided questions sit beside the dive-in paths, tap-a-rhythm arrives on space bar or button, hum/whistle comes before singing, and the everything-editable ruling is the spine — derivations land as chosen data, authored data is never rewritten. Phased; phase one needs only what already exists plus the pitch-editable grid. Three open questions bundled for the owner's review.

- [x] **v0.0.82 — play-along plays again** — his report: "play-along hasn't worked since some time after my asking for the latency to be improved." Right, and the break predates the latency item (which was never built): **v0.0.40 turned the panel into a popover with `role="dialog"`, and the instrument's own typing guard treats any dialog as "someone is typing — don't play"**. Enabling the keys leaves focus on the Enable button inside that dialog, so every note key was swallowed precisely when the panel was open — and the popover disarms on close by design, so there was no state left in which a key made a sound. The panel that exists to make the keys work is what silenced them. Fix: the play-along popover is the one dialog that is NOT typing; the guard still blocks the track select, real text fields, contenteditable and every other dialog (the Timers popover included). `tests/play-along-drive.mjs` proves it at the engine seam — `noteOn`/`noteOff` are counted via the test seam, never the readout — failing on the pre-fix build with zero calls and passing after, with a regression check that typing in the track select still plays nothing. Gates: engine 237/237, `audio-reference` 24/24 unchanged, page boots, transport and ⌘Z drives untouched.

- [x] **v0.0.81 — the call voice is a bird, not a saw** — his item 85 closed the hunt: "I heard the sawing again in Synthwave, and saw that Texture was set to Call." The texture reading of call was DESIGNED as the saw without anyone noticing: "slow, low and falling" — a −9-semitone glide through formants at 620 and 1400 Hz, repeated every 1⅓ seconds, which is a low resonance falling and dying over and over: a saw stroke. It now rises (+6 semitones) through formants at 1300/2600 — still the slow, settled reading, an octave and more below melody call's quick bright bird. Measured in `tests/call-breath-render.mjs` against the shipped bundle: chirp centres 662 → ~1130 Hz, each call rising ~0.2 octaves, with a ceiling assertion keeping texture call below melody call's register. The breath layer's makeup gain is also capped at 2× (it could reach 6×, sitting the noise at twice the whistle it was meant to sit beside) — measured alone that was NOT the saw (top-band share moved 0.46 → 0.48), which is why the defaults moved too; the hypothesis was kept honest by measuring it before believing it. Full gates: 237/237 engine, 24/24 `audio-reference` unchanged (no frozen config reaches the call defaults), page boots.

- [x] **The 10 Dependabot alerts are investigated, named and judged — nothing upgraded, nothing dismissed** — `docs/dependabot-2026-07-29.md`. The PAT cannot read the alerts API, so the view was rebuilt from the outside: all 373 lockfile packages swept against the GitHub Advisory Database, returning exactly the ten findings GitHub counts (3 high, 4 moderate, 3 low). `npm audit`'s "3" is per-package grouping of eight astro advisories, not hidden alerts. Nine are inert for a prebuilt static site with no server and no untrusted input at build; the tenth (XSS via `define:vars` `</script>` sanitisation) touches the one vulnerable feature actually in use — `[preset].astro` passing the preset slug — and stays inert only while slugs are machine-generated, which makes preset-name moderation a security control rather than taste. Root fix for all ten is the astro 5→7 major, filed as a deliberate pre-release migration; the timing decision is with the owner (fromClaude 93).

- [x] **TODO.md restructured on the owner's rulings (his items 78–87)** — the v0.0.35→v0.0.42 version ladder is retired; open work now lives in unversioned Backlog groups and nothing open sits under a version older than the shipped v0.0.80. Folded in the same pass: the everything-visible-everything-editable ruling replaces the auto/manual question; his max-energy description replaces my three options as the Energy design brief; the drum-grid rebuild is closed ("good enough for now"); the scale list is parked to v0.2.x as an Other-opens-a-scales-editor flow; the sawing is pinned on the `call` voice and filed as the top defect; and the private-repo location assessment he asked for is delivered as an a/b in his channel.

- [x] **v0.0.80 — the Ambient wash stops sawing, and the full-screen bar is one row of one kind of button** — two of his smaller notes, both measured rather than eyeballed.

  **The saw.** His words: *"the whistling is fine, it's called 'call' so let it be a bird sound not a sawing sound."* Two things were moving in the wash voice and only one of them is the bird. The Q wobble is the bird and is untouched. The band CENTRE was travelling from 320 Hz up past f × 2.4 × brightness and back down again over every single note — a filter sweep, and a filter sweep repeated forever is what a saw sounds like. It holds still now, at the geometric mean of the two ends it used to travel between: the centre of the region it spent the note passing through, which is the least-surprising place to stop it, and the frequency the noise makeup gain was **already** computed at — so the level is exact now rather than an average of a moving target.

  **Measured, because "it sounds better" is not a result anybody can check** and nobody in this container can hear it anyway. A sweep is the spectral centroid moving over time. `tests/wash-sweep-render.mjs` renders one six-second note of the shipped voice offline and measures the centroid in eight windows across the body: **1.11 octaves of travel before, 0.27 after**. The lower bound is asserted too — a completely static spectrum would mean the Q wobble had gone with the sweep, which is the bird. The test was run against the old code first and fails on it.

  **The full-screen bar.** His note on approving it: one consistent button style on one line; it had two styles on two rows. The ☰ was a bare transparent glyph pinned above a pair of secondary buttons. All three are secondary buttons in one row now, stretched to a common height so they line up exactly. The ☰ stays outside the collapsing group because it is the control that brings the group back — it fades rather than sliding away, and comes to full strength the moment a pointer or the keyboard finds it. `tests/fullscreenbar-drive.mjs` measures the boxes rather than looking at them, because "on one line" and "1px apart" are the same screenshot.

- [x] **v0.0.79 — a share link is a base plus a diff, and provenance became evidence** — two items off the same v0.0.62 foundation.

  **Share links.** His note: *"share links should use the same base-and-diff. They still base64 the whole tree into the fragment, which is the same fault in a place that has not bitten yet — a fragment is not sent to a server, so it fails later and quieter (a link too long to paste into a chat window)."* A link now carries `{o: origin, d: diff}` — the style's id and seed, plus only what the sender changed. **A clean link off a genre is 116 characters**; the same setup used to be a base64 of the whole tree. After a key and scale change: 150.

  **The whole-tree branch is permanent, not transitional.** Every link already in the wild carries the full tree and lives in other people's chat logs forever, so a payload with no `o`/`d` is still read exactly as before. If a compact link names a style this build cannot rebuild — an unknown slug, a compiler that refused — the diff is applied to the plain defaults and **the arrival note says so**, because a listener hearing a different piece under the right name with no explanation is the failure that matters.

  **A test that was quietly proving nothing.** The first cut of `tests/sharelink-drive.mjs` followed each link with `page.goto`. A goto to a URL differing only in its fragment is a *same-document* navigation: nothing reloads, no payload is read, and every assertion after it passed against the page that was already there. Three of the four checks were vacuous. They force a real reload now, and the round-trip check is what actually proves the base is rebuilt byte-for-byte rather than approximately.

  **Provenance as evidence.** TODO's wording: *"a piece that is one of our genres plus a human's edits is a different object from one that arrived as an opaque blob."* The Submit panel now states, in a sentence, what this piece grew from and how many settings are the listener's own — *"Built on our Techno Tools style, with 11 settings changed by you"* — and the same block travels with the submission: the style id, the seed that rebuilds it, whether it IS rebuildable, and the count. It decides nothing about the AI-free label: that remains a claim a person signs about the music, exactly as CONTRIBUTING.md says. It puts the evidence beside the claim so a reviewer can tell the two kinds of submission apart. The line is kept current from the settings funnel, because a provenance line that was right when the panel opened and wrong ever after would be worse than none.

- [x] **v0.0.78 — ⌘Z undoes whatever you did last** — his ask, verbatim: *"cmd-z undoes the last user input, whatever it was — including Next. Not the setup-only stack Back uses; a single universal undo over every input, unlimited within a session. Back stays as the coarse control."*

  The v0.0.58 note beside the Back button said a general undo would mean "every commit path would have to opt in, and a drag would have to coalesce". Both were true and neither needed solving on its own, because the page already has **one funnel every committed change goes through**: `persistSettings()`. Every dial, every select, every grid edit, every genre pick and Next itself all call it — and it already debounces, which is exactly the coalescing a drag needs. So the undo watches the funnel rather than instrumenting a hundred call sites, and **one drag is one entry** because one drag is one persist. The test drags the Tempo dial through forty moves and asserts a single ⌘Z puts it back.

  **Entries are the previous value of whichever top-level keys changed**, not whole snapshots — "unlimited within a session" over a tree this size would otherwise mean tens of megabytes. Top-level granularity rather than per-field is deliberate: a partial patch cannot express "this key used to be absent", and restoring a whole subtree always can.

  **Redo is included** (⇧⌘Z, or Ctrl-Y). Not asked for, and here for one reason: an undo with no redo makes an accidental ⌘Z unrecoverable, which is the same trap the single-click dial reset was — and that one was already ruled against.

  **Inside a text field the browser's own undo wins**, because there the person is looking at their typing, not at the app's state.

  One bug worth recording because it took the whole page down for a build: naming `noteUndoPoint` directly inside `persistSettings` read the undo's `let`s before they existed, since `persistSettings` runs during boot and the undo state is declared much further down. It is an explicit `onSettingsCommitted` hook now, assigned once the state is real.

- [x] **v0.0.77 — chord length in any number of bars, or one chord per section** — his note was *"chord length still has no custom option in beats, bars or sections."*

  The list was Auto / 1 / 2 / 4 / 8, which left **3, 5, 6 and 12 bars unreachable for no musical reason** — a whitelist was simply the wrong shape for a bar count, not a deliberate restriction. It is now every whole bar count up to sixteen.

  **One per section** is the other unit he named. It holds a single chord for a whole structure block and reads the length from the block itself as it begins, so a six-bar verse and an eight-bar chorus each get one chord of their own length without anyone typing either number. A chord that starts mid-section runs only to that section's end, which is what makes the next one land on the boundary rather than one bar past it. On the shapeless presets (drone, waves) it degrades to one bar, which is more honest than pretending a boundary exists.

  **Beats are not offered, and the reason is in the code rather than in a promise.** Harmony advances once per BAR here — every instrument plans a whole bar against one chord — so a sub-bar chord is a change to the harmony frame and to every scheduler that reads it, not a new entry on a list. The tooltip says so, and TODO carries the shape of the change rather than a vague "later".

  The select is now **built from the engine's own `HARMONY_RHYTHMS`** rather than written out in the markup: a hand-kept copy of an engine list is exactly how a control comes to offer a value the engine then silently drops. The browser test asserts the two agree.

  The section test is the one worth keeping: two blocks of *different* lengths, so no fixed bar count could produce the same pattern by accident — the changes have to land on 5, 8, 13 and 16 rather than on any regular grid.

- [x] **v0.0.76 — chords you can hear, not only read** — his amendment to step 2 of the agreed plan, verbatim: *"that only works for people who can hear chords in their head when they see the names, we need an audible version of this with a nice visual manipulator for those who can't."*

  v0.0.68 shipped the numerals, which was his own instruction and the right first move — roman numerals are the vocabulary the twelve genre files are written in. But a numeral is a name for a sound, and naming a sound to someone who cannot summon it is not showing it to them.

  **Every chord in the loop is now a card.** Press its face and it sounds, through the piece's own pad voice on the same live-note path a played key uses — not a second synth that would drift out of agreement with the music. Printed on the card: the numeral, the chord's honest name from the semitones it actually contains, and **the notes themselves in the current key**. Arrows move the chord through the scale, a button widens it (triad → 7th → 9th), × removes it, Add a chord copies the one you were just listening to, and **Hear the loop** plays them all in order.

  **The text field stays.** It is faster for anyone fluent in numerals, and deleting the expert route to serve the beginner is the trade this project keeps refusing to make. The two are views of one value, which the test proves by writing through the field and reading the cards.

  **Nothing new is stored.** The loop is still degrees; everything on a card is derived from those degrees plus the key and scale currently set, so a share link written before this version reads exactly the same. Change the key and every card re-colours while the stored loop sits still — which is the engine's model made visible rather than papered over. It is the same re-colouring the hint has warned about in words since v0.0.68.

  **One honesty fix the test forced.** The widen button writes a real change every time, but the engine treats the extension as a NUDGE relative to Complexity rather than an absolute width — so at some Complexity settings a triad and a 7th land on the same chord. A button that silently changes nothing is the thing this app keeps ruling against, so when that happens the hint now says why and what to move instead. The test asserts *either* the chord changed *or* the app explained itself, which is the honest pair of outcomes.

  `tests/chordchip-drive.mjs` counts the sounded notes at the ENGINE rather than listening: a card that lights up and plays nothing is precisely the failure this feature exists to fix. It also pins the scale before writing a loop — a fresh visit draws a random genre, and a five-note scale refuses a loop containing vii outright, which would have made the test pass or fail on which genre was drawn.

- [x] **v0.0.75 — the grid shows which note the app chose, not just when** — the other half of the agreed plan's step 1. v0.0.67 made the app's rhythm visible and his next line was the obvious one: *"a bass line's rhythm is now visible and its notes are not."*

  The engine's note event has carried `midi` since v28 and nothing on the page was reading it here, so the grid could tell you WHEN the app plays and never WHICH NOTE. Each chosen step now draws a **tick positioned by pitch** inside its own cell, one per note, so a line's shape reads across a bar without reading a single name — which is what a grid is for. The **names are on the cell** (tooltip and accessible label: *"the app plays C2"*), because a contour is not a readout on its own, and because "show what the app is doing" is not a sighted-only promise.

  **The window the ticks are drawn in only ever widens.** A range that re-fitted itself every bar would move every tick the moment one new note arrived, and a contour you cannot compare with the bar before it is not a contour. It never narrows below an octave either, so a track playing one note does not draw it at full scale. What the window currently is gets stated under the grid rather than left to be inferred.

  **Percussion draws nothing, deliberately.** A kit lane's midi number is a slot in a kit, not a pitch, and drawing it as one would invent a melody out of an implementation detail. Its v0.0.67 rhythm readout is untouched, and the test asserts that too — an exclusion that took the rhythm with it would have gone too far.

  `tests/pitch-drive.mjs` plays for real rather than injecting a note stream, because the handler under test reads what the engine emits and a fake stream would only prove the painter works on input the app never produces. It asserts the ticks exist, sit inside their cells, and have **more than one height in them** — if every tick sat at the same height the picture would be a rhythm again. Scoping the queries to one editor is load-bearing: closing an editor hides it rather than removing it, and the first run failed on the bass track's own ticks while checking percussion.

  One tidy-up in passing: there were two note-name spellings in the page and there is now one.

- [x] **v0.0.74 — every dial that describes an amount now takes a range, and the two that do not say why** — the owner's instruction was *"either tell me why you won't add variation to all dials, or add it to them all, including OSCs and picker dials like filter type."*

  The v0.0.56 reason did not survive contact and is withdrawn. It said a span between two named switch positions would mean nothing, and named the shape morphs, octave and filter type as the params the engine cannot walk. Two thirds of that was wrong about the app's own code: **the shape morph was always continuous** — fractional positions between sine, triangle, saw and square have been legal since v5, and the dial has drawn the intermediate waveform all along — and **an oscillator jumping octave bar by bar** is an ordinary synth behaviour, not a nonsense one. What was actually blocking both was that `PATCH_SCHEMA` read them with `oneOf`/`numberIn` instead of `sanitiseRangeValue`, which is an engine gap and not a design objection.

  **Now spreadable, engine and dial together:** the two shape morphs, octave (rounded at RESOLUTION rather than at sanitise, so the stored span keeps the ends the dial drew while the walk only ever lands on a stop the switch has), per-track **glide**, and per-track **swing** and **density**. Swing and density needed a new sanitiser helper rather than a swapped one: `null` on those two means "follow the global dial", which is a different KIND of answer from a small amount, and flattening it into the bottom of a range would have lost it.

  **Still single, and this is the reason that does survive:** the filter **Type**, the **Processor** tier and — as their own case — the **Drift rate**. The first two are string enumerations with no numeric axis for a span to mean anything along; a `{min, max}` there is not a smaller claim than the engine can honour, it is a claim about a scale that does not exist. Drift rate is the speed at which every other span walks, so spreading it asks a walk to set its own step size.

  **The feel dials have one extra rule.** Swing and Density are drawn as 21 steps whose bottom stop is **Auto**, and Auto is not less swing — it is "follow the global dial". A span is therefore pushed up off that stop rather than allowed to include it, because a range reading "Auto to heavy" would claim the track alternates between following and leading, which the engine has no way to do.

  **A browser test now holds the line.** `tests/spread-all-drive.mjs` opens the voice editor, drags every visible dial sideways, and asserts there are exactly two categories: dials that spread, and named enumerations that do not. It checks the **engine's stored value**, not only the dial's readout — which is what caught the two real bugs in this change: track glide was collapsing its span to a midpoint in `commitGlide` before it ever reached `setParams`, and the octave dial was doing the same through `Math.round`. Both drew a range the engine had never been given, which is exactly the silent drop the owner has objected to twice. 31 dials are proven spreadable end to end.

  The manifest compiler needed no edit at all: it derives `rangeable` per field by round-tripping a probe value through the schema rather than from a hand-kept list, so it followed the engine change on its own — and there is now a test asserting that it did.

  Docs updated in the same commit per the standing rule: the RangeValue section of `docs/engine-v2-contract.md` carries the amendment and the withdrawn reason, and the guided tour's dial step says plainly that every dial takes a range except the few that pick a named thing.

## 2026-07-28

- [x] **v0.0.60 — you can see when a dial is still listening to you** — the owner's first and most repeated complaint on reviewing v0.0.59: *"far too often I set the dial as I want it then move to go elsewhere and I've ruined it."*

  A pointer capture that outlives the gesture is invisible — the dial looks identical whether or not it is still following the cursor — so the next movement anywhere on the page silently drags it. The indicator now more than doubles in thickness while a drag is live (2.4 → 5.2) and reverts the instant the button comes up, which is the only thing that lets someone notice before the setting is gone.

  **The reset goes back to double-click**, reversing the v0.0.56 model, on the owner's own reasoning: if a dial IS left tracking, clicking is the one reflex available to fix it — and a single-click reset made that reflex the most destructive act available. A single click now does nothing at all. His original objection to double-click (motor control) has not gone away and is answered by Backspace/Delete, which stays and is documented as an equal route rather than an afterthought.

  **The span between the two ends is filled** with the indicator's own colour at 50%, replacing a thin accent rule at 35% that read as a third mark rather than as the region the value lives in. **The live-value mark runs from the centre to the rim** as a thin line, instead of a six-pixel tick at the edge that was easy to mistake for a tick mark.

  **The guided tour was wrong and is now right.** It still taught double-click reset four versions after that gesture was deleted, and never mentioned the horizontal drag at all. The owner asked for a standing rule off the back of it — docs, including the in-app tour, change in the same commit as the behaviour — and that rule is now written down.

  Two protocol fixes in the same pass, both his: the fromClaude channel carries **one action per number** (six things had been bundled into one), and nothing is ever appended to a number he has already started reading — new work gets a new number that **names the build it landed in**.

- [x] **v0.0.59b — the onset click measured, the fullscreen chrome, and rows that split evenly** — the second half of the run, after the loop was restarted for having stopped on an exit reason that was not real.

  **The note-onset click, measured rather than deferred.** "Nobody in the container can hear it" was filed as a reason not to work on it, and that was wrong: a click is a step in the sample stream and a step has a size. `tests/onset-render.mjs` imports the built voice library into a real browser, plays a note through `VOICES[track][id].play` — the same function the engine calls, not a reconstruction — and reports the largest sample-to-sample step at the onset against the largest in the note's own body. Two hypotheses died. **The diagnosis previously written into TODO.md was wrong**: it said `fingered`'s filter envelope "snaps to full in 8 ms with `envAmount: 1`", but `envAmount` is pitch tracking, not an envelope — there is no filter ADSR to slam, and following that note would have cost an afternoon. And there is no step at an isolated onset at all: `fingered` and `sub` both measure a ratio of 1.00, because the amplitude envelope starts at `SILENCE` and ramps exponentially. So the click is an interaction — a release tail, a legato takeover, or the sum of several tracks — and TODO now points there. A follow-up commit retracted a "second note rendered silence" lead from that same investigation after checking it: `fingered` renders an identical peak at every duration from 1 s down to 60 ms. The zero was a measurement window, not the product.

  **Full screen clears itself properly.** The oscilloscope's legend and spread control stayed on screen for the whole of full screen: the scope module is *moved* into the stage rather than re-rendered there, so its overlay travelled with it and the 2.6 s auto-collapse never touched it. One flag on the stage, set by the same function that collapses the bar, so the two cannot fall out of step.

  **Voice-editor rows split evenly.** `.knob-row` used `auto-fill`, which packs as many columns as fit — six at the editor's width, so the Source row's seven dials rendered 6 + 1. Four fixed columns now, two below 520 px: 4 + 3, then 2 + 2 + 2 + 1. The measurement corrected itself on the way — the first probe said 5 + 1 + 1 because the Osc-2 toggle is 34 px tall against the dials' 109 px and took a different box top while sitting on the same line, which is a trap the TODO note now names for whoever measures a row next.

- [x] **v0.0.59 — Back, Poly | Mono, a real fader law, and two genres researched** — the second half of the "compile and complete all the TODOs" run.

  **Back.** Next has always been destructive — it recompiles the genre at a fresh seed or re-rolls the current params, and the setup it replaced was gone, with no history kept anywhere. There is a 24-deep snapshot stack now, pushed by the two gestures that replace a whole setup at once (Next, and picking a genre) and by nothing else. Deliberately not a general undo: a stack over every dial move is a much larger feature, and a button that sometimes undoes a dial and sometimes a whole piece would mislead. The tooltip says so, and Back disables itself at the end of the history.

  **Poly | Mono.** The Mono control was one button captioned "Mono", distinguished only by fill-versus-outline — which says nothing about which state you are in as opposed to which state pressing would put you in, and the caption never changed either way. Now a segmented pair on the same rule as Universal | Per instrument.

  **The volume dial gets a fader law.** The consult measured its top three 10% steps at 1.97, 1.74 and 1.56 dB against a 2 dB audible floor. The smallest step anywhere is now 4.00 dB and none falls under the floor. **The TODO's own suggested fix would have made it worse** — an exponent nearer 1.5 lowers the top step rather than raising it, because a power law gives 0.916·n dB there. The fault was the power law itself: dB = 20n·log₁₀(t), so a step near the top is a small ratio however n is set. Replaced with what a console fader does — dB linear in travel, in two segments, 20 dB over the top half and 40 over the bottom. `params.volume` is untouched, so a stored preset plays at exactly the loudness it always did; only where the thumb sits for it moves.

  **Minimalism and New Age, researched rather than guessed at** (brain2 `Ambi4-genre-research-minimalism-new-age`). Two different answers. New Age matches its genre definition on every point measured — tempo, all-major modes, near-static harmony, a 0.6 extension bias giving the major sevenths that ARE "light melodic harmonies", and flute / bells / choir voices — so the verdict on it is taste, not accuracy, and the recommendation is to spend nothing. Minimalism has the whole surface right and is missing the one thing that defines it: **process**. Phasing and additive transformation are not variation, and the engine has only variation. Reclassified from "needs a voicing pass" to "needs a process mechanism", with two costed candidates that would serve more genres than this one.

- [x] **v0.0.58 — the explanations, the labels and the scales** — three clusters of TODO items that had accumulated because none of them was individually big enough to be a version.

  **Four things that were never explained.** Structure gets a ⓘ with one line per option and what a Custom block means; info boxes are `white-space: pre-line` now so a list reads as a list. The three-word link name gets one saying that the preset name and the words are independent, that renaming does not change the words and that editing any setting does. The Custom builder's two controls are labelled — "bars" and "intensity", with a live percentage — after the owner reported he could not identify them: both carried an `aria-label` and nothing a sighted user could read, and the full-width BPM slider sitting immediately above made an unlabelled range control actively misleading. And the preset-submission terms go in behind a ⓘ beside Submit, in the owner's own words with nothing reworded. **The anonymous checkbox ships in the same commit because the terms refer to it** — publishing terms that describe a control the page does not have would be worse than publishing none. The legal-tone read before public release is still open.

  **Fifteen scales, in two groups.** Six added to the engine: blues, harmonic minor, melodic minor, locrian, diminished (whole-half octatonic) and chromatic. Additive only — no key changed, so every stored preset and share link keeps its scale, and `audio-reference` is unchanged across all 24 configs. The list is a short primary set plus Other as two `<optgroup>`s, the HTML convention for this rather than an invented reveal. Each is named for the scale it is rather than for a region, and the label stays "Scale" because the list is mixed. Maqamat, gamelan and ragas are deliberately still absent, with the reason written beside the table: they need the pitch pipeline rebuilt around cents or ratios, which is the alternate-tunings item.

  **One quiet structural fix.** The page probed a hand-written list of "the new modes" and appended them at runtime while the markup carried the rest — an arrangement that had already begun duplicating three options as the markup grew. The build-time list is the single source now and the probe only ever takes away, pruning any scale the engine rejects and any group left empty by that.

- [x] **v0.0.57b — the tempo A/B, and Simple teaches the gesture it just learned** — two more of the owner's own questions, answered as built behaviour rather than as prose.

  **"Should only the Simple dial get next-beat quantisation? Advanced users would rather it sounded better than immediate. Let's try that for now if easy and I can AB them."** New engine param `tempoLanding`: `beat` re-reads tempo every pulse so a change is heard within one beat, `bar` waits for the barline, which is musically tidier and is what every build before v0.0.48 did. Simple's Energy macro writes `beat`; the Advanced Tempo dial writes `bar`. Turning one dial against the other is the A/B, and it needs no switch to run. Both directions have their own engine test measuring the interrupted bar — 4.00 s under `bar`, cut short under `beat`.

  **The Simple tab teaches the spread gesture, once.** Two instructions had to be reconciled: "give a message instead" on Simple (27 Jul, out of a worry that a horizontal drag would confuse new players) and "everything should be spreadable with horizontal drag" (28 Jul, which is the consistency principle). The gesture works everywhere and Simple explains it the first time it happens — which answers the worry without teaching anybody that a gesture does nothing. The line holds its slot for seven seconds against the v0.0.51 effect announcements, which were overwriting it a tenth of a second later because opening a spread on Energy also moves complexity.

  **Two latent span bugs the tests found on the way.** `syncUIFromSettings` pushed every stored param back onto its dial through scalar mappings, so a spread `bpm` became `NaN` and `knob.set(NaN)` silently kept the old number — a spread that collapsed a moment after being made, on preset load and share arrival. And the Energy macro mapped only its tempo half through the span, feeding `NaN` into the complexity half. Both mapped properly now. The Processor dial is opted out of spreading: five named tiers, and `setProcessorTier` takes one of them, so a span would round silently.

  `tests/dial-drive.mjs` grew to 18 checks and got two corrections of its own, both of which had made it pass or fail for the wrong reason: every interaction now scrolls its target into view before reading coordinates (`page.mouse` is viewport-relative, so a dial below the fold receives a drag delivered to nothing, which looks exactly like a dial refusing the gesture), and dial lookups are scoped to the visible panel (Volume exists twice, and the unscoped lookup was aiming at the hidden Simple copy). The Simple teaching check now taps the hub first, because Energy arrives already spread from the Tempo drags above and widening a full-range span emits nothing at all.

- [x] **v0.0.57 — Universal | Per instrument, and a sweep for text a box cuts off** — the toggle the owner asked for on 2026-07-27 and named again on 2026-07-28 ("I know I don't have the universal dial implementation"). It sat unbuilt through five versions because the TODO line listed five dials and carried two `[flagged]` notes, which made the whole item read as blocked. Both flags are now resolved rather than waived.

  **Three of the five cannot have it, and saying so is the fix.** `repetition` drives one hook and one motif bank per piece; `reverbTail` drives one shared convolver. A per-instrument version of either is a new engine feature — per-track sends, per-track convolvers — not a toggle. Randomness has been per-track since v21 via Variation. That leaves Swing and Complexity, and those two are built.

  **The mechanism was already there and simply invisible.** Every track's editor has carried its own Swing or Density dial since v21, with an auto detent at the bottom writing `null`, and the engine reads null as "follow the global dial". What was missing was any way to see from the global dial whether it was being followed, and any way to switch every track at once. So the control is a readout as much as an input: moving a single track's own dial off its detent flips it to Per instrument by itself, because that is what has become true. Switching TO Per instrument seeds every applicable track with the value it is playing right now, so nothing changes audibly at the moment of the switch — it only becomes editable. Switching back to Universal writes null everywhere, which does change the sound; the two directions are deliberately not symmetrical.

  **The layout flag** — "wider than the dial it governs" — is answered by styling it as a third caption rather than a button: 10 px, sentence case, no letter-spacing, hairline divider, no filled pill (a filled half on a 10 px control beside a dial face reads as a lit indicator, i.e. as something the engine is reporting rather than a choice the user made). That fits 147 px into a 143 px four-column cell with a 2 px bleed into the gutter — the same rule the v18 dial contract already applies to a long readout.

  **`.vibe/measure.sh` gained a third sweep, because the first two both missed a real fault.** The uppercase first cut of this control was clipping "Per instrument" by 8 px inside its own `overflow: hidden` group, and both the collision sweep (which skips ancestor/descendant pairs, correctly) and the overflow sweep (which only looks at text leaves, whose own boxes were the right size) reported clean. `clipped` walks containers that cut and names the child they cut and by how much. The collision sweep also stopped crying wolf: a wrapped inline's bounding box is the union of its line boxes, so the footer's two-line licence link was "colliding" with every sibling on the first line — comparison is now line-box by line-box, and all five viewports read zero across all three sweeps.

  `tests/scope-drive.mjs` — ten checks in a real browser, including that both options are always shown (never one button that swaps its own label, per the owner's wording) and that the readout half works. Its per-track half opens every editor until it finds one with a Swing dial rather than assuming the first, after the first cut reported "skipped" and left that contract untested while still reading green.

- [x] **v0.0.56 — every dial answers the same four gestures, and the spread is real all the way down** — the owner's 2026-07-28 instruction was "go ahead and build the dials that you think will work best. Everything should be spreadable with horizontal drag."

  **The gesture model.** Drag up and down for the value, left and right for the spread, tap the centre to reset, type in the readout for an exact figure. Axis lock is Scheme A, as ruled on 2026-07-27: nothing moves until the pointer has travelled 6 px, the larger of |dx| and |dy| at that moment wins, and bringing the pointer back within 4 px of the origin re-arms so one press can change its mind. The diagonal-does-both idea stays rejected — a knife-edge band that ordinary drift falls out of.

  **Where the press lands decides what the vertical drag moves**, which is what finally makes an asymmetric span like 20–30% reachable: the centre hub carries both ends together keeping their width, anywhere else grabs the nearer end alone by sweep angle. "Nearer" is a half-plane test, not a hit target, so there is nothing small to miss with a thumb. Push-through survives — an end driven past the other carries it along rather than wedging.

  **Deleted: double-click, click-to-toggle-range, and the inside/outside-face zone scheme.** The owner's ruling was that a double-click is a gesture nobody with a motor-control difficulty can rely on producing, and that a single and double click meaning different things is a trap. A tap on the hub resets with no timing requirement at all; a tap anywhere else is inert rather than destructive. Keyboard gets the same two powers it was missing: Shift+Left/Right narrows and widens the spread (a keyboard user could not previously CREATE a span at all), Backspace/Delete resets.

  **A dial at its ceiling still spreads.** The first cut opened spans symmetrically about the value and therefore did nothing whatsoever on a Volume or a Reprise sitting at 100% — silently, on exactly the dials someone is most likely to want variation from. A span at a bound now grows one-sided and keeps the width the gesture asked for.

  **Zeroed and live states.** A dial at its own minimum with no spread draws its pointer and hub in muted grey, so "off" is legible across a panel without reading a single number (the owner's "make zeroed a muted version of the dial"). Inside a span, a bright tick marks where the value actually is this bar, fed from the existing 4 Hz `getResolved()` poll — a span whose drift cannot be seen is a control with no feedback.

  **The engine half, without which the rest would be a lie.** The seven global dials wrote plain numbers: `NUMERIC_RANGES` accepted no spans, so a spread on Tempo or Swing would have been a gesture the engine discarded. Any of those keys now takes `{min,max}`; the span is stored beside the value in `params.spans` and walked into `params[key]` once a bar on the same bounded reflecting walk the per-track ranges already use. `params.bpm` stays a plain number at every one of the dozens of places that read it, which is why this did not need an audit of all of them. Volume and reverb tail are pushed into the audio graph when the walk moves them; the power governor's cap applies to both ends of a spread tail. `getResolved().globals` reports the drifting ones, and only those.

  **Spreadable is now the default** — a caller opts out, never in. Five dials do: Octave, filter Type and the two shape morphs are enumerations the engine itself names as unrangeable, per-lane Feel is an enumeration of named grooves, and track Glide and Drift rate are plain numbers in the engine (Drift rate is also the rate at which every other spread walks). Every main dial gained the third tooltip line — `↔ Vary the tempo` and its equivalents — which ships *with* the gesture and not a version before it, since a tooltip advertising a drag the dial ignores teaches the user the control is broken. On touch screens the small dials go up to 88 px so the hub is a real target rather than a notional one.

  **Verification.** `tests/knob-gesture.mjs` (35 assertions) specifies the contract against a mock DOM carrying real geometry — written before the rebuild, as the ladder asked. `tests/dial-drive.mjs` (14 checks) drives real pointer gestures on the built page in headless Chromium and proves the whole chain: the drag opens a span, the readout shows it, every main dial answers, a tap resets, and all seven spans reach local storage. The thirteen tests in `knobscope-smoke.mjs` that described the deleted gestures were rewritten rather than dropped. `audio-reference.mjs` is unchanged across all 24 configs, so nothing here alters how an existing preset sounds.

- [x] **v0.0.55 — the Stop key actually stops, and the Randomise row stops smearing** — two owner reports from the fromMartin channel, both reproduced and both fixed with a mechanical witness rather than a screenshot.

  **"It doesn't appear to do anything."** True, and the cause was a stale line rather than the stop logic. v0.0.41 deleted the separate Stop key and made Play/Finish the whole control — Play → Finish → Stop, with the second press cutting the outro short — and wrote that second press inside `finishOrStop()`. But the click handler above it still opened with `if (finishing) return;`, a guard that made sense when a separate Stop key existed and re-entering the button could only have meant "finish again". So the branch was unreachable: the caption changed to Stop, and pressing it did nothing while the 8-second outro played on. The guard is gone; the handler now routes a press to `finishOrStop()` whenever the engine is running, paused **or** finishing.

  **The icon never swapped.** The button carried two glyphs, ▶ and the final barline 𝄂, for three captions — so "Stop" was drawn beside the finish glyph. It has its own square now, one glyph per caption.

  **The overlapping text**, found by measuring rather than looking: inside any track's editor, the four Randomise dials read `Auto (follows Randomness)` at the Auto detent. That readout renders 148 px wide in `.vary-knob` cells that are 84 px on a 92 px pitch, and the v18 dial contract deliberately gives readouts `overflow: visible` so a long value bleeds into a gutter instead of pushing the dial faces out of line — with no gutter here, each caption was painted 56 px over both neighbours and the row read as one smear. The detent now reads `Auto`; what it follows is already the first thing the dial's tooltip says.

  **New: two verification tools, because "I looked at a screenshot" is how all three of these shipped.** `.vibe/measure.sh` runs real JS in the same headless Chromium `shot.sh` photographs with — `overlaps` sweeps every visible text leaf for collisions and overflow and prints the rects, `box`/`eval` measure on demand, and `drive` ships a local `.mjs` over to script the page. `tests/transport-drive.mjs` is the first of those: eight assertions over the full Play → Finish → Stop cycle, including that the stop lands in under two seconds rather than at the end of the outro. Proven to fail on the pre-fix code before being trusted.

- [x] **v0.0.53 — the sound gets a frozen reference** (7b7e61a) — tests/audio-reference.mjs digests eight settled bars of every factory preset and stock genre at a fixed seed (note count, track set, sha256 over quantised note tuples) against a committed baseline pinning v0.0.52's sound. Any commit that deliberately changes how presets sound regenerates the baseline in the same commit and says so; drift without that declaration is a regression. 24 configs, ~13 s, determinism proven byte-identical across runs; the drift path proven by deliberate tamper before being trusted. Built ahead of the consult tuning follow-ups on the ladder's own "before any version that changes how existing presets sound" rule.

- [x] **v0.0.52 — the Energy dial gets measured** (357c63e) — the consult's §D suite (tests/energy-measure.mjs): monotonicity across the sweep PASS; tempo 0.96 s and volume 0.15 s inside their latency budgets; two targets measured honestly SHORT and gated behind ENERGY_MEASURE_STRICT=1 with follow-ups filed in TODO § Consult follow-ups — Energy's complexity half is swallowed by the frozen plan for ~2 bars, and a quartile of Energy travel moves the piece no more than the structure preset's own intensity envelope already does (~1× against the 3× perceptibility target). Also measured: the volume taper's top three 10% steps sit under the 2 dB audible floor, and the track ladder saturates by Energy 0.5.

- [x] **v0.0.51 — the dials say what they just did** (6b9ef7a) — consult C5: a three-second plain-English line under the Simple dials names each discontinuity as it happens ("Percussion joined", "Now moving in longer waves"), computed by diffing the pure engine deciders (autoActiveTracks, resolveStructure) around every complexity-bearing commit; the affected track's lamp blinks at the same moment. Reserved space, no reflow; no confirmation sound by design.

- [x] **v0.0.50 — Simple asks three plain questions** (dd8176f) — the consult's dial set: Energy / Change / Volume. Energy is a real macro (bounded 42–124 bpm log sweep + a piecewise complexity map spreading the audible thresholds evenly; double-click rests at 0.35 = 61 bpm so the default sound is unchanged); Tempo and Complexity become Advanced component views that visibly follow it. Change is the fused macro renamed, with a snapped bottom detent for the hold (repetition 0.90 there — a mid-length hook, not an endless loop) and randomness 0.15–0.85 decoupled from strict 1−x; sub-floor genre values read just-above-Never, never as a hold. Volume reads % on Simple, dB on Advanced. Structural commits ride a 120 ms trailing debounce (C8) so a drag can't shred the banks.

- [x] **v0.0.49 — Total joins the legend, and full screen goes bezel-free** (f1b020d) — Total reads as one more legend entry, after Percussion, ALLCAPS, white dot; double-click shows the mix alone, mirroring track solo. Full screen drops page background and stage padding (black edge to edge, incl. the iOS pinned fallback; viewport-fit=cover), hides the collapse twisty, and the scope overlay clears the menu bar it previously sat behind. Verified on rendered shots.

- [x] **v0.0.48 — a tempo change lands on the next beat** (02d7927) — the other consult engine defect: secPerBeat was bar-snapshotted, so a bpm change waited up to a barline (12 s at the slow end) — far past gesture-attribution. tick() now re-reads tempo per pulse (only LOOKAHEAD is committed); harmony, swing and structure stay bar-quantised. The new engine test measures the interrupted bar: exactly 4.00 s on the old engine (red), early completion after (green).

- [x] **v0.0.47 — the volume dial gets its own effect back** — the first of the two engine defects
  the psychologist consult turned up, and a real one.

  The listening-level fader was `master.gain`, which sits *inside* the glue compressor
  (threshold −18 dB, knee 24 dB, ratio 3). That soft knee spans −30 to −6 dBFS, which is exactly
  where this material sits, so the compressor ate a large share of every fader move —
  differentiating the knee, roughly 28% of it lost at −20 dBFS programme and over half at −10.
  The dial was weakest precisely where people actually listen. It also meant the compressor's
  glue behaviour changed with the volume knob, which is a second bug hiding inside the first.

  A gain node now sits *after* the compressor and carries all level automation — the volume
  target, the start and stop fades, the outro, and the live-chain open. `master.gain` becomes a
  fixed headroom trim that nothing automates. The master analyser deliberately stays on the
  compressor: the scope should show the music, not how loud it is being played.

  *Taper*: loudness roughly doubles per +10 dB, so perceived loudness goes as gain^0.6 and a
  linear fader is compressive — the top half of the travel barely changes anything. Dial position
  is now `position^1.7`, so half travel is about half as loud. This is a UI mapping only;
  `params.volume` is stored and sent exactly as before, so every share link and saved preset
  sounds identical.

  *Test*: the v26 routing check asserted the output route leaves the compressor. It now leaves
  the post-compressor gain, so the test follows the corrected graph — and it still proves the tap
  hangs off the compressor, which is now a genuinely different node.

- [x] **v0.0.46 — track labels, paired harmony rows, and a consent notice that actually informs**

  *Track labels*: `.track-name` was a fixed 84px, narrower than what it holds. "Percussion"
  overflowed it and rendered as "Percussio", and the `— off` suffix pushed the longer labels onto
  a second line inside the button, so a switched-off Arp read as "Arp —" over "off". Wide enough
  for "Percussion — off" now, with `nowrap`, and still fixed so the voice pickers stay aligned
  down the column.

  *Harmony rows*: Root and Scale share a row, Time signature and Chord length share the next.
  Six full-width selects stacked down the page for values two or three characters wide was most
  of what made Advanced feel long and empty. The BPM slider went with them — it was a third view
  of the tempo the dial already owns, with `setTempo` driving all three. Its standalone readout
  went too, since the dial prints "52 bpm" under its own face and is click-to-type in its own
  right. Both references are guarded rather than deleted outright.

  *Consent*: the mechanism was already sound — nothing reaches localStorage before consent, and
  the choice is recorded in a first-party cookie that is itself the strictly-necessary kind. The
  WORDING was not. "Remember your settings and presets on this device?" gives the purpose but
  never says what is stored, never mentions that answering sets a cookie at all, and never links
  the privacy page — so the consent it collected was not informed. `consentPrompt` now takes
  `{ message, detail, privacyUrl }` (a plain string still works) and the notice says what goes
  into local storage, discloses the cookie set either way, states that nothing is uploaded and
  there is no tracking, and links Privacy.

  *Test*: page-boot's genre checks read the tempo off the Tempo dial's readout now that the
  slider is gone — the bpm-range assertions are what prove a genre's declared tempo reaches the
  engine, so they were worth keeping a reader for rather than deleting with the input.

- [x] **v0.0.45 — the dial end-words come off the faceplate** — they hung under each dial in a
  caption box a fixed 148px wide (`--knob-size` 96 + 52) centred over grid cells that floor at
  120px, so on the Advanced row of seven they overlapped into one run-on line: "Fast Calm Complex
  Hold Vary Quiet", with Randomness contributing three words of its own. Screenshotting Advanced
  made it obvious in a way reading the CSS never did.

  They now live in each dial's tooltip, laid out vertically with arrows — the dial is dragged up
  and down, so its labels read up and down. `.ui-tooltip` becomes `white-space: pre-line` to keep
  the newlines. Every main dial gained a range tooltip in the process, including Swing and Reverb
  tail, which had prose but no statement of which end was which.

- [x] **v0.0.44 — the `?` lines up with the panels** — it was pinned to the layout wrapper's right
  edge, which is 720px minus 32px of padding, while `#generator-app` is capped at 640px. So it
  overhung everything below it by 48px. The header's action row now shares the content column,
  which is the general rule: a right-aligned action has to sit above the column it belongs to,
  not the wrapper that happens to contain it.

- [x] **v0.0.43 — the transport row, checked with eyes instead of inference** — and the reason the
  three attempts before it failed: there is no browser in the container and no route to the
  deployed site, so every layout judgement was being made by reading CSS. `.vibe/shot.sh` fixes
  that — it drives the Mac test account over SSH, renders with headless Chromium and copies the
  PNG back, and `shot.sh local` ships the freshly built `./dist` over and shoots that, so a
  layout is checked BEFORE it is pushed.

  What the first screenshot showed: `.play-toggle` was `flex: 2 1 200px`, taking nearly half the
  row on its own, which is what pushed Next onto a second line. Now `1 1 150px` with a 260px cap
  — still the widest control and still obviously primary, but it stops.

  The staircase (Play, Pause, then the clock half a button low, then Next lower again) came from
  nesting: `.transport-buttons` is its own wrapping flex box, so when the row tightened it became
  two rows tall and `.transport-icons`, centred against it, floated to its middle. `display:
  contents` dissolves both wrappers so every button, icon and the dial are direct children of one
  row and can only wrap as equals.

  The dial sat ~7px high because it carries a value caption, making its block taller than a 56px
  button; centring the block lifts the dial. Both are 56px, so aligning tops is what actually
  levels it with Play. The grid and its reserved column are gone — the processor items are just
  the last thing in each row, pushed right with `margin-left: auto`. And the "huge gap" under the
  genre picker was an empty `<p>` carrying the user agent's default ~16px margins, which nothing
  in the sheet resets.

- [x] **v0.0.42 — four layout faults, four separate causes** — all found in the cascade rather than
  guessed at, and each verified against the compiled CSS afterwards.

  *Icons on their own line*: `.transport-main` was `flex-wrap: wrap` and `.transport-buttons` is
  `flex: 1 1 auto`, so with the panel now two columns the button group grew until it pushed the
  clock and keyboard onto a second row. The outer row is `nowrap` and the button group gets
  `min-width: 0` so it can actually shrink; the buttons still wrap internally.

  *Dial not level with Play*: `.transport-main` carried `margin-bottom: 8px` and the processor
  cell beside it carried none, so with `align-items: center` the left cell's contents sat 4px
  high. The grid's `row-gap` owns that spacing now.

  *An alarm clock for both timers*: the merged icon kept the alarm glyph, bells and all, when it
  stands for sleep as well. Plain clock face now.

  *Bags of space under the genre picker*: roughly 50px of it, from four sources stacking — the
  genre row's own 8px margin, the grid's row-gap, and a status band reserving 22px with a 12px
  margin under it for a line that is usually blank. The reservation stays (status text appearing
  must never reflow the panel) but at 18px with no margin; the two redundant margins are gone.

- [x] **v0.0.41 — the transport panel is two aligned columns, and Stop goes** — TRANSPORT and
  PROCESSOR share the heading row, the play buttons and the processor dial share the row below,
  and the genre picker sits left of the processor readout on the last. Everything in the right
  column is right-aligned to the panel edge, so the label is level with TRANSPORT, the dial level
  with Play, and the numbers sit directly under the dial that produces them — and they stay that
  way as the panel narrows, because they are grid cells rather than three things that happened to
  be near each other.

  The guided-tour `?` moved out of the transport panel entirely and up to the site header, level
  with the Generator nav item, via a new `header-actions` slot on the layout. It had been sitting
  beside the Processor label, where it read as documentation for the processor control.

  The Stop button is deleted. Pressing the dual button again while it is finishing now stops
  outright, making one control the whole transport: Play → Finish → Stop. That also means the
  button stays enabled while finishing where it used to disable itself and read "Finishing…" —
  pressing it again is the point. This is the one form of double click worth having, since two
  presses reach the same place however slowly they are made.

- [x] **v0.0.40 — the transport row reads as three jobs, not five glyphs** — sleep and schedule-start
  were two identical clock faces sitting side by side; they are the same kind of thing (when does
  the music happen) and are now one clock opening one Timers popover with both inside. The
  play-along keyboard joins it as a peer icon at the same 24px size, its panel a popover rather
  than a strip below the genre picker. Closing that popover disarms the keys, via a new `onClose`
  hook on `setupPopover` — every route out (the toggle, Escape, an outside click) lands in one
  place, so a caller needing to stand something down says so once.

  The Processor heading, dial and readout are one column. They were three separate places — the
  label in the panel header, the dial last in the button row, the readout a full-width line at
  the foot after the genre row and the whole play-along block — which is what let narrowing the
  viewport scatter them. The genre picker drops to half width; it was full width for a single
  short name.

  Test note: an earlier attempt watched the popover's `hidden` attribute with a MutationObserver,
  which does not exist in the page-boot harness's environment and took the whole init down with
  it — caught by the gate, replaced with the explicit hook. The play-along boot assertions moved
  with the design: they now prove the ICON is present and reachable when the engine can sound
  live notes, and that the panel starts closed.

- [x] **v0.0.39 — the dial panel opens with dials and nothing else** — dropped the "Main dials"
  heading (seven dials with their own names under them do not need a word above them saying they
  are dials, and the panel is the first thing on the tab so there is nothing to distinguish it
  from), and moved the explanatory line about which dials are shared with Simple out of
  permanent prose and into an ⓘ at the end of the grid. That line answers a question you ask
  once; it was taking a row of height on every visit. Second call site for `infoButton`, which
  had exactly one.

- [x] **v0.0.38 — play-along folds behind one keyboard button** — it sat open on the front page
  with five controls and a five-line hint showing at all times, for a feature most listeners
  never touch. Now a single icon-only keyboard button opens it; the open state persists, so
  someone who plays along every session gets it open every session and someone who never does
  never sees it. Closing it disarms the keys deliberately — leaving them live under a shut panel
  would mean stray keystrokes making noise with nothing on screen to explain why. The original
  rule that no control may move once the panel is open still holds inside it; what changed is
  that the panel itself is now closed until asked for.

- [x] **v0.0.37 — the Main dials stop stacking** — the Advanced tab's Main dials were rendering
  as one preposterous vertical column instead of a row. Cause: `#advanced-dials` carries
  `.sliders-module`, which is `display: flex` so the Simple tab's four dials sit in a row — so
  the grid inside it was a flex item with no width of its own, shrink-to-fit, and its
  `repeat(auto-fit, minmax(120px, 1fr))` resolved to the single narrowest column it could fit.
  The panel wraps a label, a grid and a hint; it was never meant to be a flex row itself.

  Now `display: block` on the panel and an explicit four-column grid, so the four dials that
  mirror Simple fill the first row and the extra three — Reprise, Swing, Reverb tail — sit
  beneath them, which is both the shape asked for and the one that reads. Two columns below
  760px, one below 400px. The flex-context `max-width` is dropped inside the grid, where a cell
  already centres its dial and the cap would only strand the label to one side.

  Suites green: knob/scope 106, visualiser 45, blocks 41, governor 12, prefs 22, share-name 13,
  genre 29, tutorial 8+8, page-boot.

## 2026-07-27

- [x] **v0.0.36 — the oscilloscope and piano roll say what they mean** — the increment that
  needed no owner input, and the one worth looking at on a real device.

  **The total trace no longer waits for another trace.** Not the latch first diagnosed: the
  guard `if (!allowed || !open || !ids.length) return` bailed out before any attach whenever no
  *track* trace was selected — and the total is an analyser of the finished mix that owes
  nothing to any track. An empty track list is now a legitimate attach when Total is what was
  asked for (`normaliseTracks` has always accepted `[]`), the attach key distinguishes "no
  tracks" from "no tracks plus total", and toggling Total re-attaches rather than only calling
  the setter.

  **Oscilloscope labels stopped pretending to be buttons.** Pill border, radius, uppercase and
  0.06em letter-spacing all gone — that styling is why 11px read visibly larger than the piano
  roll's own 11px lane labels sitting right beside it. The dots went tri-state to match the
  roll's lamp: the track's own colour for "will show when it plays", white for "sounding now",
  a bar for "playing but its trace is hidden". Spread became a vertical double arrow, because
  the word sat between the track traces and the total trace and read as another trace.

  **The section label got its own strip below the lanes.** It was drawn at `height - 4` with
  nothing reserved for it, so it landed inside the lowest lane — under the kick, invisible —
  while chord names always cleared the top pad because TOP_MARGIN reserved theirs. Lanes now sit
  between a top strip and a bottom strip, and the label is full size again.

  **Chord labels stack instead of overdrawing.** There was no `measureText` anywhere in
  visualiser.js; the only crowding guard was an 8px bar-tick gate, narrower than "Cmaj7", so
  adjacent names simply painted over each other. Each name now measures itself, takes the first
  row it clears and drops to a second row if it does not — with a per-character fallback if a
  context ever lacks `measureText`, and the smoke suite's mock context gained the method.

  **Maximise works on iPhone.** The capability gate required `Element.requestFullscreen`, which
  iOS Safari does not implement at all, so both fullscreen buttons were hidden outright on the
  device that most wanted them. The native API is now preferred rather than required; without it
  the stage is pinned over the viewport at `100dvh`, so Safari's collapsing address bar cannot
  crop the display, and Escape is wired by hand since only the native path gets it free.

  Also adds a staging environment: `wrangler.jsonc` gains an `env.dev` serving the same assets
  as `ambi4-dev` on mcdev.ambi4.work, and a `PUBLIC_AMBI4_ENV=dev` build stamps the footer and
  adds noindex, so a staging copy cannot compete with the real site for its own name.

  Suites green: engine 222, voices 283, knob/scope 106, visualiser 45, blocks 41, governor 12,
  prefs 22, share-name 13, genre 29, tutorial 8+8, page-boot.


- [x] **v0.0.35 — the two randomness dials come apart, and share links learn their own version** —
  Three things, all of them things that were quietly wrong.

  **Share payload versioning.** Links have shipped since v0.0.33 carrying the raw settings
  tree, base64url'd, with no schema field at all. An older client meeting a newer link
  therefore dropped every key it did not recognise and played a *different piece* — under the
  *right* three-word name, because the name is a hash of the payload bytes. Silent divergence
  with a matching identity is the worst failure a serverless share scheme can have. The payload
  now carries `v: 1`, read and stripped on decode so it can never reach the settings tree;
  an absent `v` reads as version 0, so every link minted before today keeps working untouched;
  and a link from a newer build now says so in the arrival note instead of pretending. One-time
  cost, taken deliberately: the same settings hash to different words than they did yesterday.

  **Randomness and Repetition de-fused on Advanced.** The owner reported that cranking
  Repetition dragged Randomness from 96% to ~46%. It did: the Advanced Randomness dial was a
  `buildMirroredDial` view of the *Simple tab's fused macro*, so it wrote
  `repetition = 1 − randomness` plus every track's randomness, and displayed
  `(1 − repetition + mean(randomness)) / 2` — two engine params that share no code path,
  presented as one control. Advanced now has its own **Variation** dial writing only per-track
  randomness and reading `trackRandomnessValue()`, and **Reprise** owning `repetition` alone.
  Simple keeps its fused macro on purpose: one knob for "how much does this change" is what
  makes the Simple tab simple.

  **Names and end-marks.** Repetition → **Reprise** (Rarely ↔ Always), Advanced Randomness →
  **Variation** (Hold ↔ Vary), each with its own marks instead of the mirror-image word sets
  (Repetitive/Evolving/Random against Random/Evolving/Repetitive) that made the pair read as
  one dial in the first place. Reprise rather than Recall because in every mixer and synth
  "recall" means restoring saved settings, and this app has presets and share links. "Never"
  was dropped as an end-mark because it is untrue — at Reprise 0 the hook still loops every
  eight chords. Wire keys are frozen: `randomness` and `repetition` stay the serialised names
  forever; these are labels only.

  Advanced also now opens with the main dials **above** the Tracks list, so tempo is where a
  Simple-tab user expects to find it.

  Scope set by a Fable review of the whole v0.0.35–v0.1.0 span, which found the original
  v0.0.35 was four plan phases plus eight unrelated UI items plus two new engine params plus an
  unresolved owner gate. Its findings are folded into TODO.md and
  `docs/dial-control-plane-plan.md`; the share-versioning gap above was its most serious catch.
  All suites green: engine 222, voices 283, knob/scope 106, visualiser 45, blocks 41,
  governor 12, prefs 22, share-name 13, genre 29, tutorial 8+8, page-boot.


## 2026-07-25

- [x] **v0.0.34 — the guided tour tells the truth again** — TUTORIAL_STEPS rewritten 10→14 for the shipped product (genres + Surprise me + favourites + Pause/Next, play-along + Capture, spread/fullscreen displays, Add Track, share flow with its three-word name); the closing step's stale claims corrected. New tutorial-smoke suite (every step's target resolves to exactly one element in the BUILT page, tabs real, copy brand-free UK English, arc Simple→Advanced→share; 8 checks + 8 mutations bit) + a page-boot tutorial gate for script-built targets. TODO: greyed paid-button item struck as superseded by the hidden-until-purchasable rule.

- [x] **v0.0.33 — three-word names for share links (free tier)** — every `#p=` link now has a
  deterministic three-word name drawn from the 938-word vetted list
  (`misty-harbour-lantern`): FNV-1a/32 over the fragment payload, three frozen seeds,
  murmur3 finaliser, indices made distinct — no clock, no randomness, no server, so the
  sender and the receiver read the same three words off the same link forever. Shown
  when the link is copied (reserved line under the Share row, in the share note, and
  offered as the preset name only while that box is empty) and again when a link
  arrives. Display only — a handle for a wall of base64, not a claim: `ambi4.work/[name]`
  stays the paid registry tier. New module `src/scripts/share-name.js` +
  `tests/share-name-smoke.mjs` (13 checks), 5 new page-boot gates covering both ends of
  a link (the receiving end is a second jsdom boot against a real share URL); 4/4
  mutation checks bit. Contract v29 addendum.

- [x] **v0.0.32 — play the instrument: live play-along + capture** — engine noteOn/noteOff/allNotesOff through each track's existing chain (custom tracks included, stopped or playing, never perturbing the deterministic stream — proven byte-identical over 3 seeds); QWERTY two-row keyboard with octave shift that never captures while typing; feature-detected Web MIDI on the same seam; Capture arms a take and quantises rhythm/velocity/length into the track's sequencer with one-click Undo (pitches follow the piece's harmony — step lanes hold no pitch, stated honestly in the UI). Transport row probe-gated; engine 211→222; ~20 new page-boot checks incl. a mocked MIDI device; 9/9 mutation checks bit. Free tier. Contract v28 addendum.

- [x] **v0.0.31 — custom tracks SHIP (registry commit 4/4)** — instrument manifests (sanitised, range-derived dials, code-shaped content rejected whole, zero-dial manifests refused, engine.getTrackManifest accessor) + the Add Track UI: name-and-family form, live rows built by the same wiring as the built-in six (pre-mortem blocker 4 cleared by extraction), kit grids for percussive user tracks, remove on user tracks only, --track-user-1…6 CVD-searched palette, genre/Blank-slate keep user tracks. Also fixed: a stored setup carrying userTracks would have blank-paged init. Engine 208→211, +3 page-boot gates, byte-identity clean, all suites green. Custom tracks are free-tier.

- [x] **v0.0.30 — user-track API (registry commit 3/4)** — engine.addTrack/removeTrack/canAddTrack: id generation from label with collision-suffixing, family-driven voice-set defaults, cap 12 total / 6 user, built-in removal refused, `tracks` event on registry change only, and a sparse quarter-note opening grid so a fresh track sounds intentional (stored blobs keep their own lanes). Engine suite 198→208; byte-identity clean over 3 seeds × 6 runs; 4/4 mutation checks bit.

- [x] **v0.0.29 — genre-signature voicings (owner verdict: genres sounded samey)** — eleven new voices (poly-saw pad; fingered/saw/squelch/upright basses; tine e-piano, nylon guitar, worn tape keys, organ-stab melodies; muted comping arp; worn lo-fi kit); all twelve genres re-voiced so no two share a pad/bass/melody triple and each leads with an instrument an expert could name in a bar; bass reined in everywhere (level 0.55–0.68, randomness 0.1–0.3 so the riff locks). Voices suite 214→283, genre 25→29 with binding uniqueness/restraint rules; contract v27 addendum. The short-release basses also close half of the v24 "true staccato unreachable" ceiling.

- [x] **v0.0.28 — user tracks in the engine (registry commit 2/4)** — `params.userTracks`: sanitised schema, live per-track graph chains added/removed mid-run with ring-out teardown, chord-tone line + percussion-kit grid planners for user tracks, unknown-id-safe voice/stats/analyser paths. Engine suite 184 → 198; byte-identity vs pre-change capture over 3 seeds; mutation-checked. Fullscreen menu glide + dismissable spread labels (aedcb49) rode along earlier today.

- [x] **Owner rulings actioned (fromMartin 22/25)** (ca150e8, dda9129) — front oscilloscope stays dark until Play (stopped-state composite retired, boot gate inverted to assert darkness); Blank slate button in Advanced silences everything (tracks off, sequencer lanes cleared + Manual, arp emptied, swing/complexity/repetition zeroed, genre cleared) with its own boot gate — groundwork for AI-free pledge labelling. Confidential strategy/VC/SWOT and FTO/IP teams dispatched to brain2.

- [x] **v0.0.25-27 — bass craft, genres audible** — bass groove machinery fixed at root (never engaged under waves/build) + pocket/articulation/ghosts/fills; twelve director-reviewed genre grammars; deterministic genre compiler; harmony.seed carries genre chord degrees into the hook; genre picker with mood-grouped favourites, Surprise me, weighted opening genres; true Pause and Next; master analyser + white Total trace; rainbow track colours (CVD-verified); borderless fullscreen oscilloscope with spread mode; advanced layout rows; registry commit 1/4.

- [x] **v24 — bass craft pass (second failed verdict)** — diagnosis-led feel work on the bass, harmonic contract untouched. Root cause of "low-pitch random": `ensureBassGroove` keyed its cache on the section intensity to three decimals, and `waves`/`build` hand out a fresh intensity every bar, so the v14 groove never engaged at all under either preset. Also fixed: per-note timing humanisation replaced by ONE per-groove lay-back (`bassPocketSeconds`, never early, scaled by vary.timing so 0 still means machine-tight); per-role gates replaced by per-step articulation cycles (`BASS_ARTICULATIONS`) plus a committed phrase ending (held rings across the barline, anything else lifts); the flat 0.1 s duration floor — longer than a clipped sixteenth at most tempos, so every short note rendered identically — replaced by a span-relative one; ghosts 0.42 → 0.25 and the anchor accent 0.85 → 0.92; a turnaround `fill` op on the last bar of each eight-bar count, intensity-scaled, never repeated; the octave pop held inside MIDI 28–55 instead of climbing into the tune's register; and a drummerless line now draws its own anchor stride instead of coin-flipping every pulse. Contract in docs/engine-v2-contract.md § v24, including the ceiling this pass does NOT clear (the bass voices' 0.12 s attack / 0.75 s release makes true staccato unreachable from the engine). Engine suite 164 → 173 tests; also killed the long-standing `finish()` outro flake at its root — humanisation could push a note forward over the barline into the closing bar, and `playNote` now holds every note inside the bar it was planned in at both ends.

- [x] **v0.0.23-24 — harmony depth + track registry core** (adfcbfe, f99c224) — chord-length control, pad breath, Ionian/Mixolydian/Phrygian; TRACK_REGISTRY single-source with identity proofs and display-order views; page/visualiser/scope registry-driven; v23 user-tracks spec + adversarial pre-mortem authored.

- [x] **v0.0.20-22 — sculpting, shape model, rhythm depth** (cf9b076..HEAD) — parametric noise-sculpting voices (colour/cloud/call) with sixteen dials; skew-based shape morph + wavefolder; offline waveform rendering everywhere; per-track swing/density/driftRate; per-step gate/length; dynamic percussion kit lanes; reverb tail control; drifting randomness defaults; ghost pointers; psychology presets refreshed (far-rain, dawn-song); playlists provenance machinery; governor fps/reverb budgets live.

- [x] **v0.0.19 — design polish + naming infra** (dc554f5) — hardware-panel rule (no state-text reflow), aligned baselines, track-coloured rings, docked oscilloscope with functional legend, true repeat barlines, percussion Pitch/Noise dials, engineType + Custom-(engine) selector honesty, 938-word vetted share wordlist, semver adoption.
- [x] **v0.0.18 wave 2 — presets/blocks/routes** (259820b..ce3f236) — psychology-grounded factory presets (headless-auditioned), block editor v1 (pattern blocks incl. tie-to-beat), preset URL routes + gallery below the Simple dials, knob push-through ranges, piano-roll chord de-overlap, ranged-patch resolution fix, paid features behind one launch flag.
- [x] **v0.0.18 — the everything build** (6105e0d, fix bb29670) — silence-floor guarantee, groove bass (kick-locked, per-section development), swing, honest chord events, dissonance, multi-sequencers with ties + probability groups, randomness-0-as-hold, transport strip, fused Randomness dial, tutorial, factory presets, power governor + cost meters, sharper canvases; TDZ blank-page fix with permanent boot gate.
- [x] **AGPL + public-flip prep** (ae6068b..1a19db6) — LICENSE, strategy scrubbed to brain2, AI imagery removed, playlists parked, history rewrite branch (main-clean), .vss untracked, brand-name scrub.
- Earlier v1–v14 engine/UI history: see git log 2026-07-24 (Hugo→Astro conversion through the six-track generative engine, voices, sequencers, musicality waves).
